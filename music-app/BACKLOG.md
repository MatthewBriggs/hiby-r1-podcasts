# Backlog

"Verified" means seen working on the device, not merely built.

## Needs you

Nothing here can be closed from this end — each one needs hands on the hardware
or ears on the output.

| # | Item | Notes |
|---|---|---|
| U1 | **Bit-perfect over USB-C** | Working and verified on the internal DAC: `hw:0,0` opened with no conversion layer at the source's own rate and depth — `S24_LE` at 96 kHz and at 192 kHz, `S16_LE` at 44.1. The same code serves a USB card but has never run, because the only USB port is the one adb uses. Plug the amp in and read its own rate/depth display; that is the only honest confirmation. The Karajan 24/192 cycle is the one to try. |
| U2 | Headset skip buttons | Play/pause works — those arrive as PLAYCD/PAUSECD from the AVRCP device and are handled. Skip sends nothing that reaches the log, so the codes are unknown. Every press is logged as `[music] key N`: press next/previous on the headset once and the log will name them. Headset *volume* needs no keys — bluealsa applies it to its mixer, which the app now polls. |
| U3 | Volume overlay smoothness | Mixer writes are throttled to ~5/s and it repaints only its own strip. If it still feels heavy the next lever is decoupling gesture tracking from the 30 Hz redraw loop. |
| U4 | **Two-level index, by finger** | R11 is built, loaded and running on the device, but the gesture cannot be verified by injection — and as of 2026-08-07 that is no longer a suspicion but a known mechanism: an injected gesture arrives entirely within one ~33 ms poll, so `index_active` never latches (see R9 for the detail). A real finger is the only way to test this. Drag down the strip to a busy letter, then slide left: the letter should fan out into the second letters that exist under it, and the list should follow. |
| U5 | **Music formats: OGG, AIFF, APE, DSD** | Test playback for alternative audio formats: OGG Vorbis, AIFF, APE, and DSD. For each format, check: (1) whether the scanner indexes them, (2) whether they play at all, (3) whether audio quality/gaplessness is acceptable, (4) whether seek works. If support is already wired in via bundled decoders, these are verification tasks; if not, they become feature requests. Priority: OGG > AIFF > APE > DSD. |

## Scanner: why tracks go missing

The stock scanner's path field is **256 bytes**. Measured against the card:
every indexed file has a path of 255 characters or fewer; every music file it
missed is 256 or more. There is no overlap, so this is a hard buffer limit, not
a corruption or a timing problem — 76 tracks across the long-titled classical
sets were simply never seen, and "Update database" would not have helped.

The other 99 files it skips are `._` AppleDouble sidecars left by copying on a
Mac. Skipping those is correct; this app skips them too.

**Since fixed by renaming:** 94 files clipped, over-long files down from 76 to
**24**. The remaining 24 are over the limit because of their *folder* paths, so
the filename alone cannot save them. The mapping is at
`hiby-fw/restore-point/rename_plan_20260801.tsv` and every rename is reversible.

**Still open:** the index holds the 94 files under their old names, and those
rows are dead until a real rescan. The app skips rows whose file has gone and
the per-album folder sweep picks the files up under their new names, so albums
look right — but by sweep, not by index.

## Replacing hiby_player entirely — investigation, for next week

Prompted by BG6: the standby fight is a symptom of hosting inside a process
we only partly control. The question raised was what booting straight into
our own binary (the combined Library/podcast app) would need to recreate.
Power management is not on this list — BG5 already covers it, and once
`hiby_player` is gone there is nothing left for it to fight with.

Not a commitment to do this. Investigate #1 and #3 before treating it as
more than an idea — everything else is moot if #1 fails, and #3 is the one
mistake that isn't recoverable.

| # | Item | Notes |
|---|---|---|
| RP1 | ~~**Framebuffer/HGL DMA init, standalone**~~ **done — genuinely flashed and booting, 2026-08-23/24 (v0.32)** | First proven 2026-08-15 with a minimal ~90-line test binary (`open("/dev/fb0")` -> mmap -> `FBIOPAN_DISPLAY`, `killall hiby_player`, ran foreground): no `Access hgl dma failed`, panel went solid magenta, confirmed by `fbgrab`. Established a process that isn't `hiby_player` can own `/dev/fb0` through plain kernel fbdev, no HGL/DMA-pool/KMS needed (this device has no `/dev/dri` at all). **Follow-up, the real boot-replacement release**: `build_standalone.sh` (was badly stale -- missing podcast.c/lastfm.c/spotify.c/mseb.c/recent.c/index.c/scanner.c/zlbm.c/miniz, and `SQLITE_THREADSAFE=0` when index.c/scanner.c's background threads need =2 -- caught up to match `build.sh` exactly) builds `library_standalone`, a real executable (`standalone_main.c`'s `for(;;) music_entry();` loop, no LD_PRELOAD). `patch_firmware.py` gained `--standalone DEVICE_PATH`: replaces `usr/bin/hiby_player.sh` entirely with a crash-counted loop launching that on-device path directly -- no hiby_player, no DEV_HOOK/LD_PRELOAD at all (`build_standalone_supervisor()`). `verify_firmware.py` updated to recognise this shape too rather than just failing its old DEV_HOOK-specific check. Flashed together with the interleaved-DMA kernel (`kbuild-il2.sh`'s `xImage-r1-il2` -- see NOTES-kernel-optimisation.md's SHELVED section: its only flaw was stock's own HGL-based native screens, which nothing in Library touches) via the SD-card `r1.upt` mechanism, live-verified: `ps` shows `library_standalone` with no `hiby_player` process at all, `kernel_build_id` reads `4.4.94_r1_il2`, real touch input, real navigation, real library browsing (4748-file card). **One real bug found and fixed in the same pass**: the SD card was never mounted by anything -- turns out `hiby_player` mounted it internally at startup (not any init.d script; grepped the whole rootfs and came up empty), so every album showed 0 tracks (`tracks_query()` skips any row whose file doesn't `stat()`) even though the album *list* itself still worked (index.c's own stale cache, which doesn't re-verify file existence). Fixed with an explicit `mount -t exfat /dev/mmcblk0p1 /data/mnt/sd_0` baked into the new supervisor script, confirmed live afterward (scanner found all 4748 files again). Bluetooth pairing/scanning and quick navigation both confirmed live working standalone too -- `bluetoothd`/`dbus-daemon` are independent `init.d` services, untouched by any of this. Released as `hiby-r1-library-podcasts-0.32-standalone.upt` (`LIBRARY_VERSION` bumped 0.31 -> 0.32 to match). **Second follow-up, same day**: `lib_open()` had the fallback order backwards -- it preferred the stock database whenever it was non-empty, but the stock db never goes empty once hiby_player stops writing it, it just freezes, so the scanner's own (actually current) database was never being read at all. Flipped to prefer scanner.c's database outright, stock only as a fallback before a device's first scan. Also **consolidated per user request**: "Rebuild library index" and "Scan SD card directly" were two Settings rows/passes; merged into one ("Scan library", `scanner_rescan_now()` now kicks `index_rescan_now()` too) and both background scans' 30-minute auto-rescan timers removed in favour of manual-only, since with hiby_player gone for good scanner.c is the sole way anything new gets discovered and a silent timer neither the UI nor the user can see coming is worse than a user-controlled "check now" tap right after copying music onto the card. |

**Why it works, not just that it works — corroborated 2026-08-16 from `github.com/Starnished66/R1-open-source-player`** (a second, independent, real-hardware-tested replacement project — see its `HARDWARE_DRIVERS.md`, built from reading the vendor's own `/module_driver/*.sh` loader scripts and kernel module strings, not guessed). The framebuffer (`soc_fb.ko`) and the mysterious HGL DMA pool (`sa_hgl_dma.ko`) are documented there as **two separate out-of-tree kernel modules**, loaded in a fixed order at kernel boot — `soc_fb` comes well before `sa_hgl_dma` in that sequence, and both load "before `hiby_player` (stock or [their] build) ever starts, and is identical regardless of which player app is flashed." In other words: `/dev/fb0` was never actually gated on `hiby_player` doing anything — the kernel makes it available unconditionally, every boot, to any process. The `Access hgl dma failed` error this project has chased for a long time was almost certainly from code trying to use HGL-specific userspace calls into `sa_hgl_dma`'s pool specifically, a genuinely separate subsystem from plain fbdev — not from `/dev/fb0` itself being unready. This retroactively explains yesterday's result rather than just adding a second data point to it: plain fbdev was never blocked by anything HGL-related in the first place.

**Follow-on, same day: the actual app, not a test pattern.** Pushed further than "can we draw a colour" to "is Library itself functional standalone" — the real question RP1 exists to gate. `music_entry()` turned out to already be fully self-contained (its own `/dev/fb0` open/mmap, its own touch grab, its own key scan, its own BlueALSA thread — none of it depends on anything the LD_PRELOAD constructor sets up beyond the tile-hijack trampoline, which is meaningless with no launcher to hijack a tile from). The only real blocker was linkage: `music_entry` was `static`. Made it non-`static` (harmless — `is_hiby_player()`'s existing process-name guard already makes the constructor a safe no-op under a different binary name, so the normal LD_PRELOAD build is unaffected either way) and added `standalone_main.c` + `build_standalone.sh`, a plain executable build of the same sources calling `music_entry()` directly, no tile-hijack path involved at all.

Stopped `hiby_player.sh`'s supervisor first this time (`kill` the shell, then `killall hiby_player`) — RP1's original test hadn't needed this since it only ran a few seconds, but the supervisor loop respawns `hiby_player` within about a second of it dying otherwise, which would have meant two processes fighting over `/dev/fb0`/touch/ALSA rather than a clean standalone test. Confirmed via `ps` that only `library_standalone` remained before drawing any conclusions.

**Result: it worked, fully.** Main Menu rendered correctly (identical to the hooked build). Tapped into Music → Albums → a track list with real data from the actual on-device SQLite index — genuine library browsing, not a stub. The mini-player had already restored exact prior playback state (config/session persistence survived the process boundary cleanly). Tapped play: **real-time FLAC decode over ALSA**, confirmed not just by the pause icon but by the position genuinely advancing between two screenshots taken seconds apart (4:01 → 4:43, tracking real elapsed time) — a decode+output pipeline actually running, not a static UI state.

**One real gap found, and fixed on the spot.** Swiping back at the Main Menu — the top of the navigation stack — calls `go_back()`, which returns 0, which sets `running = 0`. In the hooked build that hands control back to the launcher; standalone, there is no launcher, so `music_entry()` just returns and the whole process ends, leaving the last frame frozen with nothing left to draw or take input. Not a crash, not a hang — a clean exit with nowhere left to go. Cost one reboot to recover from (confirmed clean: `hiby_player` and both hooks came back up normally). Fixed by wrapping the call in `standalone_main.c`: `music_entry()` returning now re-enters instead of exiting the process, the standalone equivalent of "hand back to the launcher" when there is no launcher. Redeployed with the fix and confirmed proper `BACK`-button navigation (not swipe) works correctly within the app.

**Bluetooth investigation, real finding, not a bug.** With the track playing, connected Bluetooth headphones over quick-settings mid-session — the Now Playing route label stayed on "3.5 mm" despite no wired jack detected (`/sys/class/switch/headset/state` = 0 on both nodes) and a confirmed live A2DP sink (`bluealsa-cli list-pcms` showed it). Traced this to existing, *intentional* behavior already in `audio.c` (not standalone-specific): the playback loop polls USB every ~1s mid-track because it's cheap (a readlink), but Bluetooth is deliberately only checked when a track *opens* (`bt_sink_connected()` is presumably a costlier dbus/bluealsa round trip) — comment at `audio.c:1376-1378` says so directly. Confirmed the system-level Bluetooth stack (`dbus-daemon`, `bluetoothd`, `bluealsa`) runs as independent init.d services, entirely unaffected by `hiby_player`/its supervisor being stopped. Was mid-way through confirming the pickup happens on the *next* track (skipped forward) when a **parallel Claude Code session deployed a fresh build to the same physical device**, rebooting it out from under the test. Recovery was clean and automatic either way — `ps`/`uptime` afterward showed a fresh boot, `hiby_player` and its supervisor back to normal, no bad state left behind — but the last check was never completed.

**Casualty of the collision**: the `static` removal on `music_entry` got overwritten by the other session's deploy (`music_hook.c` now matches `HEAD` exactly again). `standalone_main.c`/`build_standalone.sh` are still on disk, untracked, harmless, but won't build against `music_hook.c` as it currently stands until that one-line change is redone — deliberately not redone yet to avoid a second collision while the other session is actively mid-edit on `audio.c`/`audio.h`/`library.c`.

**RP3 (firmware recovery location) remains the one blocking question before this is more than an idea** — but "is Library itself functional as a standalone process" now has a real, mostly-affirmative answer: yes, convincingly, for UI/browsing/config/audio; Bluetooth mid-session pickup specifically is unverified but has a clean, non-alarming explanation and no reason to expect it behaves differently than the hooked build (it's the same code, same system services, unaffected by which process is calling it). |
| RP2 | **Bluetooth and Wi-Fi pairing UI** | Quick-settings shows connection state and toggles on/off — that's not the same as pairing a new headset (scan, PIN entry) or joining a new network (scan, password entry, saved-network list). Never built, because `hiby_player`'s own settings screens have always covered the rare first-time case. No fallback if it's gone. |
| RP3 | **Where does firmware recovery actually live?** — investigated 2026-08-16, real evidence gathered, not yet fully resolved | "System → Firmware update → Via SD-card" is the mechanism the whole restore-point workflow depends on — a bad image has been recovered this way for real. Investigated via static analysis only (strings on the local `hiby_player.orig` copy, `/proc/mtd` and `/proc/cmdline` on the live device) — nothing flashed, nothing in `restore-point/` touched. **Confirmed: `hiby_player`'s own binary does contain the literal low-level flash commands** — `flash_erase -q /dev/mtd5 0x20000 1` and `printf %-256s \| nandwrite -q -s 0x20000 -p /dev/mtd5 -`, found directly in its strings. **But refined once the actual partition table was checked**: `/proc/mtd` shows `mtd0 uboot`, `mtd1 kernel` / `mtd2 rootfs`, `mtd3 kernel2` / `mtd4 rootfs2`, `mtd5 ota` (512 KB — far too small to hold a kernel or rootfs image), `mtd6 userdata`. `hiby_player`'s strings reference **only `mtd5`** for the direct `flash_erase`/`nandwrite` calls — never `mtd1`–`mtd4` directly. Dumping `mtd5` on the live device shows plain text: `ota:kernel` padded with spaces — a small state/target marker, not payload data. So `hiby_player`'s own *direct* low-level involvement looks to be limited to writing a status flag into that small OTA region; how the actual multi-megabyte `rootfs`/`kernel` payloads get written was not found in its strings (no `mtdblock` device path, no `dd if=`/`dd of=` pattern for the big images) — still genuinely unknown, not just unstated here. A live device does expose `/dev/mtdblock0`–`6` (major 31) and a second set `/dev/mtdblock_bbt_ro0`–`6` (major 50, presumably a bad-block-table-aware read wrapper) for every partition, so a block-device-level write path is clearly available to whatever does perform it — just not confirmed to be `hiby_player` itself via string evidence alone. **A real correction made before writing this down**: `/proc/cmdline` shows `root=/dev/mtdblock_bbt_ro2` for the currently running system, which was first misread as evidence of a resolved "whichever bank is active" symlink — it is not; it is a literal, hardcoded reference to partition 2 specifically. The dual `kernel`/`kernel2` and `rootfs`/`rootfs2` partitions make an A/B failover scheme plausible, but **there is no direct evidence yet that u-boot (`mtd0`) actually can or does choose between the two banks** — that would need either u-boot console access (a real serial console exists per `/proc/cmdline`'s `console=ttyS2,3000000n8`, just not hardware this investigation has) or dumping and analysing `mtd0` itself, neither done yet. **Net effect on the original question**: less catastrophic than originally feared, but not resolved. If the large-payload write goes through generic block-device I/O rather than something baked uniquely into `hiby_player`, the low-level *mechanism* isn't lost by replacing `hiby_player` — but `hiby_player`'s UI is still the only thing that currently knows how to find a `.upt` on the SD card, verify it, and drive that mechanism at all; nothing yet says a standalone replacement would need to fully reimplement that specific flow rather than something equivalent, and nothing yet confirms a botched write is actually recoverable via bank fallback rather than just "less immediately catastrophic before you find out it isn't." Next step, if this stays a priority before anything more permanent: find a way to read `mtd0`'s own logic (dump + strings at minimum; real u-boot console access if a serial cable is ever in reach) rather than guessing further from the rootfs side.

**Resolved, 2026-08-16, from a second, independent, working prior-art project.** `github.com/Starnished66/R1-open-source-player` (GPLv3, LVGL, "fully usable on real R1 hardware" per its own README — further along than `open_hiby_player`, closer in spirit to what these RP items are scoping) ships `src/core/firmware_update.c`. It does **not** reimplement low-level flashing at all: `firmware_update_scan()` finds a `.upt` on the SD card exactly like the stock UI does, and `firmware_update_enter_recovery()` is four lines — run `/usr/bin/bootmode.sh Recovery`, then `reboot`. That's the whole mechanism on their side. Checked what `bootmode.sh` actually does **directly on our own device** (this is HiBy's own stock script, not their code): its `Recovery` case is `flash_erase /dev/mtd5 0 1` then `nandwrite`s the literal text `ota:kernel2` into it; its default case writes `ota:kernel` instead — exactly the same `mtd5` partition and exactly the same `flash_erase`/`nandwrite` pair already found in `hiby_player`'s own strings above. **This closes the loop**: `mtd5` is a boot-target flag, `kernel`/`kernel2` name the two banks (`mtd1` vs `mtd3`, and by extension `rootfs`/`rootfs2`), and choosing between them is u-boot's job at the next boot, triggered by nothing more than this tiny flag write plus a reboot. Confirms the A/B scheme is real and does fail over — this was the one piece static analysis alone couldn't prove.

**What this means for "replacing `hiby_player`": the fear was overstated.** The recovery trigger is `flash_erase`+`nandwrite` (ordinary system utilities, not unique to `hiby_player`) plus a plain reboot — four lines any replacement can call directly, already proven working by a second, independent, real-hardware-tested project. **Still open**: exactly how a *new* `.upt`'s multi-megabyte payload gets written into the currently-inactive bank in the first place (this script only flips the flag to fall back to whatever is *already* in the other slot — it doesn't populate it), and whether `mtd0` (u-boot) has any failure mode of its own that isn't bank-dependent. Neither blocks the conclusion that the *recovery* path specifically survives `hiby_player` going away, which was RP3's actual question. |
| RP5 | Misc system settings | Date/time and timezone, USB mode switching (storage vs. charge vs. today's adb-debug mode), factory reset, an About/storage screen. Individually easy, just unbuilt because there has always been a fallback until now. |
| RP6 | **Replace the scanner** — built, cross-compiled, installed on-device 2026-08-23; not yet observed running an actual full pass live | `scanner.c`/`scanner.h` (new files, in `build.sh`) walk the SD card directly with their own `opendir()`/`readdir()` recursion, skipping `Podcasts`/`Audiobooks` (those have their own subsystems already). Populates `MEDIA_TABLE` — same table name and column set `library.c`'s 15 existing query sites already expect — in a database of its own, `SCANNER_DB_PATH` (`/usr/data/scanned_media.db`), never the stock `usrlocal_media.db`: `lib_open()` (`library.c`) now tries the stock database first and only falls back to this one if the stock table has zero rows, so this never runs in competition with `hiby_player`'s own live scanner while it's still present. Threading/pragmas mirror `index.c`'s own scan-worker exactly (low-priority thread, batched commits, `journal_mode=off` on UBIFS, not the card). Tag extraction is a new `tag_read_meta()` in `tags.c` — extends the existing per-container FLAC/ID3v2/MP4 parsers (previously track/disc/title only) to also pull artist/album/album_artist/genre, including real ID3v2 text-encoding decode (ISO-8859-1 and UTF-16 → UTF-8). Format/bits/rate/bitrate come from a new `audio_probe_format()` in `audio.c`, reusing the existing `dec_open()` container-header parse (FLAC/WAV/AIFF/Vorbis/Opus/M4A all get exact values for free) plus a hand-written MPEG frame-header reader for MP3 specifically (dr_mp3's own container open deliberately leaves bitrate/frame-count unknown to avoid a full decode — see `dec_open()`'s own comment — so MP3 needed its own cheap answer). Reachable today via a new Settings row, "Scan SD card directly", same progress-readout shape as "Rebuild library index". **Not yet done**: an actual end-to-end live test of a full-card pass (verified it links and installs; the scan loop itself hasn't been watched run to completion against the real card yet), and genre's legacy ID3v1 numeric-ID form (`"(17)"`) is deliberately left undecoded — passed through as literal digits, since modern taggers write plain text. |
| RP8 | **HGL DMA pool (6MB) and standalone RAM headroom — measured, 2026-08-24: no measurable win, wrong metric for the real goal** | Went in assuming `hiby_player`'s removal (RP1) would free real RAM, specifically the 6MB `sa_hgl_dma` DMA pool (`insmod sa_hgl_dma.ko sahd_hgl_mem_size=6291456`, loaded from `module_driver/sa_hgl_dma.sh`). Live-tested three ways on the same device, same fresh-boot conditions each time: forcing `open()` on `/dev/sa_hgl_dma` changed `MemAvailable` by nothing; `rmmod`-ing the module live changed nothing (36392 -> 36384 kB, noise); disabling the module's `insmod` entirely from the boot script (`patch_firmware.py --standalone` now writes a no-op over `sa_hgl_dma.sh`, shipped in `0.44-standalone.upt`, `lsmod` confirms the module genuinely never loads) changed nothing either — fresh-boot `MemAvailable` 36180 kB, against a pre-fix fresh-boot baseline of 36184 kB, a ~4 kB difference that's pure noise. `MemTotal` (56564 kB) is identical in every reading, before and after, ruling out a devicetree/CMA boot-time carveout being affected by any of this. Likely explanation: the pool is allocated lazily behind an actual HGL *use* ioctl (the kind `hiby_player`'s own native rendering would issue), not by `open()` or module load — and nothing in `library_standalone` ever drives that sequence, so the pool was never allocated in the first place once `hiby_player` stopped running, independent of whether the module is even loaded. Broader comparison (standalone vs. the older hooked LD_PRELOAD build) shows the same null result: fresh-boot standalone `MemAvailable` (36184 kB) essentially matches an earlier hooked-build reading (36.4MB) — no measurable system-wide RAM win from removing `hiby_player` either, at least not visible in this metric. **Boot-script disable kept anyway** (harmless, one less module load, `fbgrab` after flashing confirms UI still renders correctly) but the "6MB back" framing is retired — corrects the framing under RP7/RP1 above that implied this was free money. **Real motivation for RP1 was never actually RAM** — clarified 2026-08-24: the goal is a snappier, less stutter/freeze-prone experience than stock, and `MemAvailable` was the wrong metric for that from the start. Confirmed this device is single-core (`nproc`=1), which makes CPU scheduling contention across processes — not memory headroom — the more plausible lever: `hiby_player` (an entire other process wanting the same one core) fighting `library_standalone` for cycles is a much better stutter story than free RAM ever was. Standalone already eliminates that contention by construction; whether it's *felt* is genuinely subjective so far ("I think it does, but that can be entirely subjective... not as big a change as when we created our own scanner/database" — the user's own assessment after living with 0.44-standalone). If chased further, the next useful measurement is CPU/scheduling-latency-shaped (e.g. `top` samples or run-queue length during an actual observed stutter), not another memory reading — and other single-core-competing daemons (`bluealsa`, `bluetoothd`, two `dbus-daemon`s, `sys_server`, all still resident in standalone) are candidates worth a look if stutter is ever pinned to a specific moment (e.g. Bluetooth activity) rather than chased as a vague overall feel. |
| RP7 | **musl recompile candidates** — **(a) closed out, 2026-08-24: musl is a dead end for this app, tested and confirmed, not just theorized** | **The "(a) is free once standalone is committed to" framing below was wrong.** Tested directly rather than assumed: a small standalone test program (`alsa_bench.c`, dlopen's `libasound.so.2` the same way `audio.c`'s own `pcm_open()` does, opens `plughw:0,0`, plays a second of tone) built clean for `mipsel-linux-musleabihf` via `zig cc` -- but at runtime, `dlopen("libasound.so.2")` fails outright: `"Dynamic loading not supported"`. Root cause, more fundamental than a glibc/musl ABI mismatch: zig's musl MIPS target links **fully static** by default, and a static musl binary has no working dynamic loader linked in at all -- `dlopen()` isn't ABI-incompatible with the target library, it's simply not a functioning code path in this configuration, full stop. Forcing dynamic linking instead doesn't rescue it either: this device has no musl runtime (`ld-musl-*.so.1`) installed anywhere, only glibc, so a dynamically-linked musl executable would have nothing to load it in the first place. Since this app's own two system-library dependencies (`libasound` for audio output, `libjpeg` for cover art) are both reached via `dlopen()`, not static linking, and both are only ever going to exist on this device in their stock glibc-built form, **musl cannot run this app at all**, regardless of how (a)'s own vendored/single-file code (SQLite, the decoders) would have compiled. A real compiler-choice question got tested alongside this and stayed open: `zig cc` (clang/LLVM) vs. the vendor SDK's own `mips-linux-gnu-gcc` 7.2.0, same benchmark, glibc both times -- six runs each, `close_ms` averaged 108.52 (zig-cc) vs. 109.36 (vendor gcc), well within noise, and zig-cc's own binary came out smaller (5720 vs 6368 bytes for the identical source) -- no meaningful difference found for this I/O-bound (ALSA syscalls, not decode-loop-shaped) workload; doesn't speak to whether GCC's more mature MIPS backend would show up on a genuinely CPU-bound benchmark (FLAC/MP3 residual decode), which this test wasn't. (b)-(e) below are about system libraries/daemons, not this app's own compile target, and are unaffected by this finding -- kept for reference. **Correction to this closeout's own count, 2026-08-27: five system libraries, not two.** Rechecked by grepping every `dlopen()` call site rather than recalling them -- `libasound.so.2` (`audio.c`), `libjpeg.so.9` (`cover.c`), `libfdk-aac.so.2` (`aac.c`), `libopus.so` (`opus_dec.c`) and `libvorbis.so` (`vorbis_dec.c`). All five are stock glibc-built rootfs files reached the same way, so the finding above holds harder rather than less: a static musl build loses every one of them, which is all audio output, all cover art, and the AAC/M4A, Opus and Vorbis format coverage on top. **Corroborated independently** -- `Starnished66/R1-open-source-player` hit the identical wall from the opposite direction: its own notes record planning to `dlopen()` this device's system `libfdk-aac.so.2` and finding it impossible because musl's `dlopen()` refuses to work from statically-linked binaries (confirmed by them via QEMU against the real device library), which is why they vendor FAAD2 instead and why that project carries a GPL obligation it would otherwise not have. **If musl is ever revisited, every replacement is substitution, not a linkage trick**: `tinyalsa` (github.com/tinyalsa/tinyalsa) for local output, a vendored AAC decoder, vendored Opus/Vorbis (we already carry their headers and `ogg/framing.c`), and a vendored IJG jpeg9 per (b). One architectural cost comes with `tinyalsa` specifically and is worth knowing before rather than after: it talks **only** to numbered `hw:` cards and implements no ALSA plugin layer at all, so it cannot reach `bluealsa` -- our whole Bluetooth path (`bluealsa:PROFILE=a2dp` with a `plug` fallback in `pcm_open()`, plus `amixer -D bluealsa` for volume) is unreachable through it. That project's own answer, from `audio_output.c`: keep tinyalsa for local hardware, and route Bluetooth and USB through a spawned `aplay -D bluealsa` subprocess with PCM piped into it, since `-D` resolves through the real ALSA rather than tinyalsa. Their header notes that path accumulated several real-device Bluetooth bugs one at a time, so adopting it means re-earning fixes we already hold (BG67/BG79's mixer re-push, format negotiation, underrun recovery). Why it works under musl at all is the transferable part: a **subprocess** boundary is safe across libc implementations -- a musl process may freely spawn a glibc binary -- which is precisely the property `dlopen()` lacks, so "shell out to it" stays viable everywhere "load it into our own address space" does not. | Only matters once `hiby_player` is genuinely replaced, not for today's LD_PRELOAD build — that build shares one process (and one heap, one TLS layout) with `hiby_player`'s own glibc, and mixing glibc and musl inside a single process is unsafe, not just untested. `zig cc` (already the toolchain, see `build.sh`) has musl MIPS targets built in, arguably less fiddly than the current glibc-2.22 version pin. Candidates, split by who actually controls the source: **(a) our own binary** — `music_hook.c` + everything `build.sh` compiles into `libmusic_hook.so`/`library_standalone` (including the vendored single-file libs: `dr_flac`/`dr_mp3`/`dr_wav`, `ogg`/`vorbis`/`opus`, `sqlite3.c`, `miniz` as of R33) — moves to musl automatically the day the standalone build's target triple changes; no separate action needed. **(b) libjpeg** — **correction, 2026-08-24: the "borrows from hiby_player" framing below was wrong**, caught rereading `cover.c` directly rather than from memory. `dlopen("libjpeg.so.9", ...)` (falling back to `/usr/lib/libjpeg.so.9`) is a plain by-path load our own process makes for itself -- `dlopen()` has no mechanism to reach into another process's address space regardless, so there was never a real "shortcut" to lose. Confirmed working standalone the same way it always did: `/usr/lib/libjpeg.so.9` is a rootfs-shipped file, loaded independently by whichever process calls `dlopen()` on it, with or without `hiby_player` ever running. Not vendored into our binary (currently *not* vendored, dlopen'd instead so a missing library degrades to no cover art rather than a crash), and `vendor/jpeg9/*.h` (present, currently just headers for the dlsym signatures) suggests IJG jpeg9 was already scoped for a real vendored build if one is ever wanted for its own sake (musl portability, avoiding the rootfs dependency) -- just not because standalone *requires* it. **(c) separate system daemons, open-source, but not ours today** — confirmed this session (RP1's Bluetooth investigation) that `dbus-daemon`, `bluetoothd`, and `bluealsa` run as independent init.d services untouched by `hiby_player`/its supervisor being stopped; `wpa_supplicant` (WiFi, relevant once RP2 is built) is the same shape. All are real open-source projects (BlueZ, bluez-alsa, hostap) that could in principle be rebuilt against musl too, but that's a materially bigger undertaking than (a)/(b) — BlueZ alone usually wants D-Bus and glib, and taking over daemons that currently just work, unmodified, trades a working dependency for a self-maintained one. **(d) not recompilable at all** — `hiby_player` itself and whatever closed-source vendor binaries handle PMIC/charging, backlight, and WiFi/BT firmware loading (the `/module_driver/*.sh` scripts RP1's corroboration read already point at kernel modules here, not userspace binaries, so this is probably a short list, but unconfirmed). **(e) already-separate tooling, unrelated to the on-device app** — the static `curl` bundled for the R1 podcatcher project (see memory: busybox's own TLS was too old to use) is worth checking against whatever libc it's currently built with; if it's already musl-static this is moot, if not it's a trivial rebuild. Net: real savings, but they scale with how much of the stack gets taken over — (a) is free once standalone is committed to, (b) is a bounded one-time cost, (c) is its own separate project that trades working infrastructure for maintenance burden and shouldn't be bundled into "replace `hiby_player`" scope casually. |

## Library app refactoring

The goal: rename and restructure `hiby-music-app` as "Library", a unified hub
for music, audiobooks, and podcasts with a six-item main menu and persistent
playback. The mini player stays at the bottom while the user browses; all
sections share the same status bar, output routing, and volume control.

This is foundational work: the S-series unification items (S1–S8, below) are the
per-section implementation. Start with L1–L4, then the S-series, then L5–L10.

| # | Item | Notes |
|---|---|---|
| L1 | **Rename hook: music_hook → library_hook** | The hook binary stays in `/usr/data/libmusic_hook.so` for the launcher tile to find, but the code becomes a unified router for all sections. No device-side change; the launcher tile already points at the binary, not the name. Done when `library_hook.c` exists and the build produces the same `.so` name. |
| L2 | ~~Library main menu: reorganize to avoid crowding~~ **done, scoped to what exists** | Podcasts and Bluetooth aren't real screens in this codebase yet — Podcasts is a genuinely separate chain-loaded binary (see L1, still unbuilt), and Bluetooth has a quick-settings toggle but no dedicated screen — so those two couldn't be added as top-level items without fabricating something. Built the achievable part instead: the five library-browsing facets (Album artists, Albums, Artists, Genres, Playlists) moved out of the top-level menu into a new "Music" submenu (`SC_MUSIC_MENU`), cutting the top level from 9 items (after R20 added Settings) to 5 — Music, Audiobooks, Parametric EQ, Radio, Settings, under a "Main Menu" header (EXIT on the right, same as before). `SC_ARTISTS`/`SC_PLAYLISTS`/the no-facet `SC_ALBUMS` case now `go_back()` to `SC_MUSIC_MENU` instead of straight to the top level, one level deeper than before; everything else (`SC_ARTISTS` reached via a facet, `SC_ALBUMS` reached via an artist) was already relative to `cur_facet` and needed no change. First pass had the labels backwards (top level said "Music", the submenu said "Main Menu") — corrected per direct feedback. Verified on-device: top level reads "Main Menu"/EXIT with a "Music" row; tapping it opens "Music"/BACK with the five facets; BACK from any facet screen lands on "Music", not the top level. When Podcasts/Bluetooth become real screens (L1, RP2), they slot into the top-level list `top_menu[]` the same way Audiobooks/EQ/Radio/Settings already do. |
| L3 | **Navigation and routing** | Tapping a menu item loads that section's screen. Back from a section returns to the menu, not to the previous screen within that section. Menu is always one tap away. |
| L4 | **Mini player persistence** | The mini player (current track, play/pause, skip) stays at the bottom of the screen at all times, including in the menu and in every section. Playback continues while the user browses. Tapping it opens the full Now Playing screen (the old SC_PLAYING). |
| L5 | **Podcasts section** — *needs S1, S2, S3* | Merge `hiby-podcast-app` into Library as the "Podcasts" menu entry, keeping its functionality and Library's look and feel. Uses the shared core, output routing, and quick-settings dropdown from the S-series. Podcast playback and browsing logic stays in its own `.c` files; only the entry point and top-level state move into library_hook. **Scoped in detail 2026-08-16 by reading the podcast source directly:** **(a) What must be carried over**, none of which Library has an equivalent for: feed/episode browsing by directory walk (mtime-ordered newest-first, deliberately *not* SQL -- the index carries neither download state nor date order); the `UPDATE FEEDS` screen, which streams live `podsync` subprocess output into its own screen; per-row download markers (dimmed = not downloaded, tap downloads rather than plays, live percentage on the row being fetched); and +/-30s transport (Library's audiobook screen is +/-10s -- 30s is for skipping ads, so keep both). **(b) The big free win, not obvious from the outside:** the podcast app has its *own* `audio.c` built on `minimp3_ex`, entirely separate from Library's decoder stack, which is why "Podcasts plays MP3 only" has been a standing limitation. Merging drops it and podcasts inherit FLAC/WAV/M4A/AAC/ALAC/Vorbis/Opus, the parametric EQ, Bluetooth routing, gapless and hires handling. That limitation comes off the known-issues list. It also drops four duplicated files (`audio.c`, `wsola.c`, `cover.c`, `text.c`) and the `dlopen` chain -- worth having on a device sitting at ~1.5 MB free. **But podcast playback then runs through a completely different code path than it does today, so it needs real testing rather than assuming.** **(c) Resume state: keep both systems, deliberately.** Library's `.position` (one per book folder, `<file_idx> <file_ms>`) is O(1) to write and self-cleaning, but stores position only. Podcast's `.podsync/resume.txt` (`<ms>\t<dur>\t<path>`, capped at 128) additionally stores duration and a `POS_FINISHED` sentinel, which is what drives the `47%`/`DONE` markers in the episode list -- Library's format cannot express those. They also key differently (folder vs path) and mean different things (`file_idx` is meaningless for a single-file episode). Unifying would mean migrating real listening positions from both for no user-visible gain; explicitly out of scope for the merge. If ever revisited, generalise the *podcast* model (single index, outside media folders) and fix its `resume_lookup2()` rescanning the whole file per episode on list load. **(d) Packaging:** killing the chain-load retires `libpodcast_hook.so.real`, which touches `patch_firmware.py`, the README install steps and the release-notes install block -- all currently document a two-file push. **(e) Show notes — agreed design.** Today they occupy the bottom ~40% of the podcast Now Playing screen, scrollable, up to 200 wrapped lines from a `.txt` sidecar; Library's Now Playing is edge-to-edge cover art (`ART_PX == FB_W == 480`, art at y=0..480, title at 500, seek bar at 618) and has no room for that. **Design: a small circle with an `i` in it, top right of the cover art (so around (448,32), r~16). Tapping it replaces the cover art in place with the show notes, scrollable within that same 480x480 region -- transport controls stay put. Swiping back restores the artwork.** Three implementation notes from checking the gesture code: (1) the swipe-back must be *intercepted*, not guarded -- put it in `go_back()`'s `SC_PLAYING` case so notes close and it returns 1, which keeps the file's existing "one definition of back, so the header control and the edge swipe cannot drift apart" property and makes the header `BACK` close the notes too; (2) no drag conflict -- the art region has no touch handler today (title drag starts ~y=484, scrub at ~618), so vertical drag there is free for scrolling, and a horizontal drag from the left edge still reads as back because the gesture code already tests `dx > abs(dy)`; (3) the icon should only appear when a notes sidecar actually exists, so it never shows for music or audiobooks. |
| L6 | **Audiobooks section** — *needs audiobook-app* | Built into Library from the start (unlike the separate audiobook mod). Playback and browsing in separate source files; accessed via the "Audiobooks" menu entry. Shares output routing and status bar with Music and Podcasts. |
| L7 | **EQ controls** — *build our own* | A settings panel under the EQ menu item that controls treble, midrange, bass, balance. Stores settings to device (likely `/usr/data/user.ini` or a new file). System EQ is available but we own the UI. |
| L8 | **MSEB-like effects investigation** | MSEB itself is not reachable from our hook (no ALSA controls, `hiby_player`'s logic is static/stripped, state is an undocumented binary blob at `/usr/data/eq.ini`). But the goal of L7 and L8 together is to deliver bass/treble/balance controls that feel native. Investigate: spatial enhancement, sub-bass boost, presence peak, loudness curve — anything that bridges what MSEB users expect without needing access to MSEB's internals. Folds into L7 implementation-wise, but worth exploring the feature space separately. |
| L9 | **Bluetooth settings panel** | A menu item leading to a settings screen: show paired devices, connect/disconnect, show signal strength and battery if connected. Does not duplicate the quick-settings BT toggle (which is in every section via S3); this is the full control. |
| L10 | **One status bar across all sections** — *needs S1, S4, S5* | Battery, headset, Bluetooth, Wi-Fi across the top in Music, Audiobooks, Podcasts, and in menu screens. Managed in the core (S5). Same facts everywhere: format, rate, output, codec, headset battery on Now Playing. |

## Unification across music and podcast

**Scope note:** The S-series items below are the building blocks for L5, L6, L10.
Each section (Music, Podcasts, Audiobooks) uses the shared core and routing
they define. Do S1 first; every other item is cheap afterwards and expensive
before — separate ports would double the duplication that is already drifting.

| # | Item | Notes |

| # | Item | Notes |
|---|---|---|
| S1 | ~~Shared core, `common/`~~ **done** | `hiby-music-app` and `hiby-podcast-app` both now compile `text.c` and `cover.c` from `~/Git/common/`. The music app's versions are the survivors since they are the ones that have moved. `cover.h` has a `cache_key` parameter that podcast's didn't have — the signature mismatch is now resolved, with podcast's art keyed by feed name. `status.c` and `status.h` are in common but only music uses them (podcast has no output routing yet). |
| S2 | **Output routing into the core** | Lift the jack/USB/Bluetooth priority out of the music app's `audio.c`: the `st_headset()` → `usb_card()` → `bt_sink_connected()` ladder, the `bluealsa:PROFILE=a2dp` pinning that avoids the narrowband HFP PCM, and the pause-on-loss/auto-resume path. Podcast currently has none of this, so this is the item that actually gives 3.5 mm → USB-C → Bluetooth everywhere. Bit-perfect opening stays music-only for now — a podcast does not need `hw:0,0`. |
| S3 | **One quick-settings dropdown** | Same gesture, same contents, same layout in both: brightness, Bluetooth and Wi-Fi as buttons that colour when on, with the connected device and SSID beneath. Lives in the core with a small per-app hook for the rows that differ — the music app's rescan (R10), the podcast app's refresh. Requires S1 for `status.c`. |
| S4 | **One Now Playing information row** | The same facts in the same order wherever audio is playing: format, bit depth and rate, the output actually in use, the Bluetooth codec, and the headset's battery. All of it already exists in the music app's `status.c`; podcast shows none of it. |
| S5 | **One status bar** | Not in the original ask but the same data and the same drift risk — battery, headset, Bluetooth and Wi-Fi across the top. Cheap once S1 and S4 are in; listed separately so it can be dropped without stalling them. |
| S6 | **One volume behaviour** | Whatever R4 settles on has to be settled once, in the core, or the two apps disagree about what 60% means. Blocked on R4. |
| S8 | **Handing over between the two apps** | Both hooks open ALSA. Today the music hook chain-loads the podcast hook (H2), so they coexist by accident rather than design. Switching from a podcast to an album mid-playback should stop the first cleanly rather than leave two players contending for the DAC. Needs a decision on what *should* happen before any code. |

## Requested

| # | Item | Notes |
|---|---|---|
| R10 | **Rescan button in the dropdown** — *next* | Should **clip over-long filenames first, then scan**: the scanner can only store a path that fits its 256-byte field, so clipping has to happen before it looks. The renaming logic is ~60 lines of plain string work (keep a leading track number, drop a segment repeating the folder, middle-elide with `…`) and ports to the device without regex. It must also shorten *folders* when the filename alone cannot get under 255 — that is the whole reason 24 files are still over — rewrite any `.m3u` pointing at the old names, and log every rename to the card so the operation is reversible on device. **Blocked on the trigger.** Deleting the index does *not* make the scanner rebuild it: the device came back with an empty database and no library until the backup was restored. `/data` is a symlink to `/usr/data`, so there is no second copy shadowing the delete, and the card's `.temp/` holds only `most_played.db` — the `cp /data/usrlocal_media.db …/sd_0/` in the binary is an export, not a cache. Leading hypothesis: the scan is driven by the **card mount event**, the one moment it is known to fire, so the button unmounts and remounts the card with the clipping done in between. Testable without a flash. |
| R4 | One volume control, not two — *after R10* | See `NOTES-volume-and-ota.md`. The firmware has its own volume and this app applies its own on top, so wired output is scaled twice. Recommendation there is (c): force the firmware's to maximum once at app entry and own the rest, which needs finding where it is persisted — `/usr/data/user.ini` is the likely home. Note that software scaling is also what stops the wired output being bit-perfect below 100%. |
| R7 | OTA updates from GitHub | **Restated:** use the *built-in* updater, fed from GitHub — fetch your own `r1.upt` from a release with the static curl on the card, verify its checksum, place it at the card root, and let the stock updater flash it from System → Firmware update. No custom install path and no rollback machinery; the stock updater keeps doing the risky part. `NOTES-volume-and-ota.md` still describes the app-library design and needs rewriting to this. |
| R8 | Duplicate-looking tracks in one boxed set | *Symphonies Nos. 1 – 9* shows 75 where the index has 72, and a few movements appear twice with the same title. Duplicate **paths** are already dropped, so these are genuinely separate files on the card — probably two rips of the same disc. Worth checking the folder rather than changing code. |
| R9 | Vertical swipe does not scroll lists — **cause identified 2026-08-07** | The doubt is settled: an *injected* gesture genuinely does not look like a finger, and now we know why. `tap.py` sends its eight press frames and the release back-to-back, so the whole gesture is delivered inside a single ~33 ms poll of `read_gesture()`; `touch_down` goes 1 and back to 0 within one tick, so the live-tracking block never sees a finger that is still down. `index_active` therefore never becomes true and `index_shown` stays −1, which is also exactly why the A–Z strip ignores injected taps (`if (index_shown >= 0) index_jump(...)` never fires). Observed directly tonight — tapping the strip left the footer reading "1-9 of 290". **This says nothing about a real finger**, which holds for many ticks; it means injection cannot test either gesture, and U4/R9 still need a hand. To test by injection the tool would have to spread its frames across several polls (≥100 ms of press events with syncs). |
| R14 | ~~List scrolling still isn't smooth~~ **measured and improved; partial redraw still open** | Profiled on device before touching anything, per this item's own warning. A painted frame on the Artists list cost **5379 us average, 40693 us worst** (clear 1479, text 2301, other 1599). Against the loop's 33 ms sleep that is 16% of the period, so **the redraw was never the bottleneck** -- the premise both earlier guesses shared was wrong. The period was: ~33 ms sleep + ~5 ms frame means touch is sampled only ~25 times a second, and since BG2 made the list page a whole row at a time, a moving finger gets a coarse row-sized step at 25 Hz. **Fixed** by sampling at 8 ms while a gesture is live (`list_dragging || inertia_active || vol_dragging || qs_dragging || index_active || scrub_active`); idle stays 33 ms and locked stays 100 ms, so there is no sustained extra load of the kind BG27 cares about. Nearly free rather than 4x the work, because `dirty` is still only set when the visible row actually changes -- the extra iterations read the gesture and go back to sleep. Also replaced the three `/255` in `blend()` (once per antialiased glyph pixel, on a core with no fast divide) with a shift-based exact equivalent, verified against `t/255` across the full 0..65025 range rather than assumed. Re-measured after: **3937 us average, 4986 us worst** -- 27% off the average and **8x off the worst case**, which is what was visibly stuttering. Text alone went 2301 -> 1581 us. **Two negative results worth keeping:** (1) writing the clear as 32-bit pairs instead of 16-bit stores changed it by *nothing* (1479 -> 1481 us), and (2) a standalone on-device benchmark shows why -- this framebuffer is ordinary cached memory, not uncached device memory, running at ~490 MB/s written / ~347 MB/s read, the same as malloc'd RAM. The clear moves 768 KB in 1.48 ms = 519 MB/s, i.e. already at the memory's own limit. No store-width or instruction-level trick can beat that. **The one remaining lever is therefore to touch fewer pixels, not to touch them faster**: skip the full-screen clear and repaint only the row band (header/status/mini-player/index do not change during a list drag), worth up to ~1.5 ms of the remaining ~3.9 ms. Left open as the next step, now with numbers attached. |
| R17 | DAC/Amp controls (play/pause/skip/rewind) | Some external DAC/Amp combos have front-panel buttons for transport control but no way to address the R1 itself — they send IR or USB HID commands expecting a particular device driver. Investigate whether there's a standard HID interface the R1 could expose (or emulate) so an amp's own buttons can control playback. Lower priority than core playback, but worth scoping if the audience includes people with fancy external gear. |
| R18 | Show all podcast episodes, downloaded and undownloaded | Podcast feeds currently show only downloaded episodes. Enhance to display all available episodes from the feed, with downloaded episodes in normal text and undownloaded episodes greyed out or otherwise visually disabled. Allows browsing the full episode archive without having to download everything first. |
| R19 | **Status bar layout, app-wide** — *liked from the audiobook mockup* | `draw_status()` today crams battery% and volume% together on the right, nothing on the left, no clock anywhere. The audiobook design mockup used a plainer, balanced layout — volume icon + level on the left, the clock centred, battery icon + percentage on the right — and that reads better than what shipped. Redo `draw_status()` to that layout and it is one function, so every screen gets it at once: `SC_PLAYING`'s own status strip and the shared header in `draw_screen()` both call it already. Needs a live clock where there currently is none (`st_*` has no time-of-day getter yet — check what the podcast app or `hiby_player` itself reads for the top-of-screen time). Related to S5 (one status bar shared by every app) but distinct from it: S5 is about not drifting apart across apps, this is about the layout itself being better, in this app first. |
| R20 | **New Settings menu within Library app** | Add a dedicated Settings section to the Library main menu (6-item menu becomes 7, or moves to page 2). Initial features: (1) **Power button lock** — double-press power to lock screen, disable all hardware button presses, and turn screen off. When locked, only a double-press wakes/unlocks (single press does nothing). Double-press again to unlock and re-enable normal button function. (2) **Accent colour themes** — allow user to select accent colours that match their R1 hardware colour variant (black, gold, etc.), so the UI branding feels cohesive with the device. |
| R21 | **Shuffle and Repeat modes** | Add playback mode controls to the Music player: (1) **Shuffle** — randomize track order within the current album/playlist/queue. (2) **Repeat** — cycle through repeat modes: off → repeat all → repeat one → off. Display current mode in the Now Playing screen or mini player. Settings should persist across app restarts (store in user.ini or similar). |
| R22 | **"Fix Library" button in Settings** | Add a button in the Settings menu that runs automated fixes for common problems preventing tracks from showing up and rescans the library. Should include: (1) trigger the HiBy database scan, (2) handle filename clipping for paths over 256 bytes (see Scanner section), (3) any other common fixes. Present as a single "Fix Library" or "Rescan & Fix" button that handles the full recovery flow. Related to R10. Location: Settings menu. |
| R23 | **Pull cover art from Last.fm** | Add a Settings button to fetch missing album cover art from Last.fm and save it as `cover.jpg` in each album folder. Requires Last.fm API key (user provides via `settings.txt` on filesystem root). Process: (1) identify albums with missing artwork, (2) query Last.fm for album covers by album name + artist, (3) download and save as `cover.jpg` in the folder, (4) reload cover cache. Location: Settings menu. |
| R24 | **Use settings.txt for all app configuration** | Migrate all app settings (power lock timeout, EQ profiles, Library paths, Last.fm API key, accent color, shuffle/repeat state, etc.) to a single `settings.txt` file on the filesystem root (`/settings.txt` or similar). Current settings are scattered across `.ini` files and in-memory state. This provides a unified, user-editable config location and persists across reboots. |
| R25 | **Setting for automatic or manual track name scrolling** | Add a Settings toggle to control whether long track names scroll automatically or require a tap/gesture to scroll. Useful for users who prefer to read titles without animation, or vice versa. Default: TBD (auto or manual). |
| R26 | ~~**About screen**~~ **done** | Added an "About" row at the bottom of Settings (`SC_SETTINGS_ABOUT`), showing Library version (`LIBRARY_VERSION`, currently "0.1"), firmware version, kernel version, serial number, SD card free space, and free memory. Firmware version reads `/usr/resource/config.json`'s product `"version"` field directly (the file has a second, unrelated `"version"` key earlier for a different entry -- a bare integer, not a quoted string, which is what distinguishes them) -- this is the same field `patch_firmware.py`'s `stamp_config_json()` already writes a build-identifying value into (confirmed live: reads "1.6a" on the current vanilla-based build, not the vendor's plain "1.6"). Kernel version comes from `uname()` directly, no shelling out. Serial number reads `/proc/jz/efuse/efuse_chip_id` -- but the raw 32-hex-digit chip ID is *not itself* the serial number: the first attempt showed it in full and the user caught that it didn't match stock. Compared directly against stock Settings -> About on this same unit (chip ID `7b42423542a0c500afa0560804000001`, stock's own "Serial number: R17B424235") and confirmed the real format is `"R1"` + the first 8 hex digits, uppercased. Free SD space uses `statfs()` on `/data/mnt/sd_0/` (same path `library.c`'s `SD_ROOT` already uses; `/data` is a symlink to `usr/data`, resolving to the same mount `df` sees at `/usr/data/mnt/sd_0`), shown as `f_bavail * f_bsize` in GB. Free memory reads `/proc/meminfo`'s `MemAvailable` (not `MemFree` -- see the comment at `about_ram_free()`: `MemFree` alone reads alarmingly low on this device even with plenty available, since reclaimable cache is usually the largest share of the 56 MB budget, the same thing BG44's investigation ran into). Accent colour's own trailing divider (it used to be the last row and closed the list itself) moved to the new About row instead; the six rows (94px header + 6*72px) fit the 800px screen with no scrolling needed. Verified live on-device after the serial number fix, cross-checked against stock Settings -> About directly. |
| R27 | **Accent color based on album cover** | Add a Settings toggle to automatically derive the UI accent color from the dominant color(s) in the currently playing album's cover art. When enabled, the accent color changes with each album. Can coexist with manual theme selection (R20). |
| R28 | ~~**Track duration display in album tracklist**~~ **done** | The earlier "already done, entry stale" closure was wrong -- called out live by the user, durations were genuinely blank across the library, not a documentation gap. `fmt_dur()`'s formatting was fine; the actual bug was upstream of it. `t->dur_ms` came straight from `MEDIA_TABLE.end_time`, and pulling the on-device database (`/usr/data/usrlocal_media.db`) showed why: of 4,857 tracks, only 15 have `end_time > 0` -- and every one of those 15 is a cue-sheet split track (`begin_time`/`end_time` mark a slice's boundaries within one shared physical file, e.g. an audiobook; `cue_id >= 0`). An ordinary one-file-one-track file, `cue_id = -1`, is left at `end_time = -1` by the stock scanner *forever* -- not stale, never populated. That's 99.7% of a real library. Fixed with `audio_probe_dur_ms()` (`audio.c`), a header-only duration probe called from `library.c` wherever `dur_ms` comes back <=0: FLAC/WAV/Vorbis/Opus state total sample count in their header, so `dec_open()+dec_close()` answers exactly with no PCM decode. M4A (AAC/ALAC) deliberately does **not** go through `dec_open()` -- that path primes the first access unit through `alac_decode()`/`aac_decode()` into `g_aacpcm`, the same buffer the playback worker's `dec_read()` writes into for whatever's actually playing on another thread, a real cross-thread hazard, not a hypothetical one. `mp4_duration_ms()` (already used by `audiobook.c` for the same reason) answers from the moov atom instead, touching neither decoder. MP3 has no cheap exact answer for VBR (`dec_open()` deliberately leaves `d->frames` at 0 -- see its own comment, decoding the whole file to count is "seconds of stall on this CPU"), so it falls back to `filesize*8/bitrate` using the bitrate already in the database -- exact for CBR, a close estimate for VBR, which is all a list-row label needs. Covers ~96% of this library outright (FLAC 83% + MP3 12.7% exactly or near-exactly) and ~99.7% including M4A (3.5%). Verified live: Bowie's *"Heroes" (2017 Remaster)*, blank before, every track showing a real duration after (3:36, 3:08, 6:11, ...), same album, same device. |
| R29 | **Waveform display for progress bar (Music only)** | Add a waveform visualization to the progress bar on the Music player's Now Playing screen, showing the audio waveform of the currently playing track. The waveform should indicate the track's overall shape and let the user see their position within it visually. **Music only — do not add to audiobooks.** Scope: (1) waveform data extraction from the audio stream or pre-computed/cached, (2) efficient rendering at the progress bar's resolution, (3) color coordination with the UI accent color. Consider whether to pre-compute waveforms on first play or on-demand, and whether to cache them on the SD card. |
| R30 | **Recently added and recently heard albums in Music menu** | Add two new browsing options to the Music submenu (alongside the existing Album artists, Albums, Artists, Genres, Playlists): (1) **Recently added albums** — albums sorted by file/folder modification time, newest first (recently added to the SD card). (2) **Recently heard albums** — albums sorted by last playback time, most recently heard first. Both should integrate with the existing album list UI (A–Z index, paging, cover art). Requires tracking file modification times and last-play timestamps (already available via `most_played.db`). |
| R31 | **Font size multiplier in Settings** | Add a Settings slider to adjust the font size of list text (album titles, artist names, track titles) in the range 1.0x to 1.5x. Default: 1.0x (current size). Larger sizes improve readability, especially on smaller screens or for users with visual accessibility needs. Apply the multiplier globally to all text-rendering calls in list contexts (`draw_text()` with the multiplier applied to the font size parameter). Persist the setting to `settings.txt`. |
| R32 | **1-pixel progress bar on mini-player** | Add a thin 1-pixel progress bar at the bottom of the mini-player (the persistent playback control at the bottom of every screen) showing the current track position. Display only — not interactive (scrubbing remains on the full Now Playing screen). The bar should span the full mini-player width and use the accent color to indicate progress. Simple visual feedback of playback position while browsing. |
| R33 | **Implement zlbm (zipped-album)** — core parsing done, library-browsing integration still open | Reviewed the spec (`zlbm` README: a ZIP holding tagged FLAC/Opus, an optional cover, an optional PDF booklet, an optional XSPF for custom order) and the reference player `zap` (GPLv3, Python) for technique only — its `_sort_images()` cover-priority heuristic (front/cover/folder first, back last) was reimplemented from scratch in C; its `_create_booklet_page()` renders a PDF's first page via PyMuPDF, which confirmed PDF-cover support needs a real PDF renderer and is disproportionate here. **v1 scope, deliberately narrowed**: audio tracks (any format this app already decodes, not just FLAC/Opus) plus a direct JPEG/PNG cover. PDF-booklet covers and XSPF custom ordering are out — tag/filename-based ordering (same disc/track precedence as `cmp_track()`) covers the normal case, and a zlbm carrying only a PDF gets no cover, not a crash. On branch `zlbm-support`: vendored `miniz` (single-file, MIT, ZIP reader) as `app/vendor/miniz/`, added `app/zlbm.c`/`zlbm.h` — `zlbm_open()` classifies every entry by extension (no decompression at classification time; track/disc numbers are parsed from filenames, not tags, to avoid inflating audio just to sort it), `zlbm_extract_track()`/`zlbm_extract_cover()` inflate straight to a caller-given path so the existing decoders in `audio.c` (`dec_open()`) and the existing `ART_SCRATCH` pattern in `art.c` pick it up unmodified. Verified: cross-compiles clean via `build.sh` (mipsel-linux-gnueabihf.2.22), and a native host build against a hand-built `.zlbm` correctly sorted "01 - Opening"/"02 - Second Track" and picked `front.jpg` over `back.jpg`. **Not yet done**: wiring `.zlbm`/`.zip` files into the actual browsing UI (`library.c`/`music_hook.c`) as pseudo-albums — the stock scanner has no knowledge of these files at all (same gap RP6 documents), and the natural fix depends on whatever scanner/index work is landed for RP6, which was mid-flight in a separate uncommitted checkout when this branch was started and deliberately not touched to avoid a repeat of the earlier session collision. |
| R34 | **Add Sennheiser HD 660S autoEQ profile** | Add the Sennheiser HD 660S parametric EQ profile to the bundled EQ profiles. Profile: Preamp -9.77 dB, 10 filters (LSC, PK, PK, PK, PK, PK, PK, PK, PK, HSC) with specified frequencies, gains, and Q values. Save as `EQProfiles/Sennheiser HD 660S.txt` and integrate with the profile picker's fuzzy-match logic (BG38). |
| R35 | ~~**Switch to Noto Sans font**~~ **done, verified live** | Bundled Noto Sans Regular (`fonts/font.ttf`, OFL-licensed) as `/usr/data/podcast_res/font.ttf`, pushed by `tools/deploy.sh` alongside the `.so`. `text.c`'s `FONT_CANDIDATES` now tries it first, falling back to the stock `msyh.ttf`/`Korean.ttf` if the bundled font is ever missing. Cyrillic support added: the glyph cache's codepoint ranges (`cp_slot()`) grew a third block for U+0400-U+04FF alongside the existing Latin and General Punctuation ones. CJK glyphs were never in scope either before or after -- `cp_slot()` returns -1 outside its three tracked ranges regardless of what the underlying font itself supports, so this is unaffected by the font swap. Verified live via screenshot: clean rendering including Latin Extended (Þorvaldsdóttir). |
| R36 | ~~**Display disc numbers in album tracklist**~~ **done, verified live** | Multi-disc albums now show a disc number, in accent colour, on the first track of each disc — track numbers stay in a fixed column so they line up whether or not the row next to them carries a disc marker. Single-disc albums are unchanged; audiobook chapters and podcast episodes never carry disc data so they're unaffected too. |
| R37 | ~~**Disable PEQ, MSEB and Bluetooth when playing over USB**~~ **done, verified live** | A Settings toggle ("Bypass DSP on USB"). A pure runtime override, engaged/released purely by whether output is actually USB right now -- never touches `eq_enabled()`/MSEB's own saved state, so it always comes back exactly as the user left it the moment USB output stops. Bluetooth radio is turned off only if it was actually on when USB engaged (and restored, only then, when it disengages) -- same "note it, then put it back" pattern `deep_suspend()` already uses for its own Bluetooth teardown. |
| R38 | ~~**Own track-detail index, so album opens don't re-read tags every time**~~ **done, verified live** | A SQLite cache (`index.c`) this app owns, on internal UBIFS storage (`/usr/data/music_index.db` -- not the SD card: its exFAT driver doesn't honour POSIX locking properly, confirmed live via `SQLITE_READONLY` then `SQLITE_IOERR` then a main-thread stall before moving it). Caches per-track disc/track/duration (previously re-read from every file on every open) plus the whole-library Albums/Album-artists/Artists/Genres lists (previously re-sorted from the stock DB's `MEDIA_TABLE` on every visit, no index on `album`). Manual only, via Settings' "Rebuild library index" row -- an earlier auto-start-at-boot version made the whole HiBy launcher sluggish at startup, competing with everything else initialising at once. |
| R39 | **USB-C play/previous/next controls** | Implement HID transport controls (play/pause, previous, next) over USB-C connection. This allows external DAC/Amp devices or USB-connected controllers to command playback from the Library app. Requires: (1) USB HID device mode initialization, (2) recognizing standard HID control codes from the USB host, (3) routing them to the existing `play_index()`, skip functions, and pause/play toggles used by hardware buttons. Scope: music player only (define behavior for audiobooks/podcasts separately if needed). Related to R17 (DAC/Amp controls). |
| R40 | **Migrate app preferences to settings.txt** | Move the following settings from `/usr/data/music.conf` to the unified `settings.txt` on the filesystem root (per R24): (1) accent colour, (2) auto-shutdown time, (3) button lock (power lock timeout), (4) USB DSP bypass (PEQ/MSEB/Bluetooth disable on USB). These are currently scattered across `music.conf` and in-memory state; consolidating them in `settings.txt` provides a single, user-editable config location and ensures persistence across reboots. Deprecate the relevant `music.conf` entries once migrated. |
| R41 | ~~Light theme~~ **done, verified live** | The six base UI colours (`COL_BG`/`COL_HEADER`/`COL_TEXT`/`COL_DIM`/`COL_ROW`/`COL_LINE`) became variables behind their existing macro names -- same pattern `COL_ACCENT`/`g_accent` already used -- so no draw call anywhere needed to change. `apply_theme()` sets all six from `light_theme`, as an exact per-channel invert of the dark palette (light = 255 - dark), which is what guarantees every contrast relationship dark mode already relies on survives the swap unchanged rather than needing independent re-balancing. Accent presets are untouched, as specified -- they're saturated enough to read on both. New "Light theme" toggle row in Settings, between Auto shutdown and Accent colour. Persisted to `/usr/data/music.conf` (`light_theme = %d`), the same file every other Settings toggle already uses -- R41's own text asked for `settings.txt`, but that's still WiFi/Radio/Last.fm/Spotify credentials only pending R40's broader migration, and splitting this one setting into a third storage format alongside `music.conf` and `settings.txt` would be its own inconsistency. Confirmed live via framebuffer grab: Settings and Main Menu both re-render correctly in light mode immediately on toggle, no restart needed. |
| R42 | ~~Retire the standalone Podcasts app; move Library onto the About tile~~ **done** | Library has had its own podcast support (`podcast.c`) since well before this change, making the separate standalone Podcasts app (`podcast-app/`, hijacking the About tile, chain-loaded from Library's own constructor via `dlopen`) pure redundancy. Retired it entirely: deleted `podcast-app/` from the repo (its `podsync_once.sh`/`parse_rss.awk`/`feeds.txt` fetcher assets, genuinely still needed by Library's own `podcast.c`, moved to `music-app/podsync/` first). Library's tile hook moved from Stream media onto About in its place -- `ABOUT_CB_1`/`ABOUT_CB_2`/`ABOUT_CB_ORIG`/`DATA_PAGE` replace the old `TILE_CB`/`TILE_CB_ORIG`, arming both callback slots (About shows up twice in `hiby_player`'s data) the way the retired app used to, rather than the single slot Stream media needed. Label/icon shadowing moved from `sys_set.ini`'s `<stream_media>` tag to `settings.ini`'s `<about>` tag and from `stream_media.png`/`_s.png` to `about.png`/`_s.png` (same artwork, `make_icon.py` just writes the new filenames now). Stream media is untouched -- no code targets it any more, so it reverts to stock automatically. README/THIRD_PARTY.md updated to match (single app, single tile, no more chain-load or second `.so`); `patch_firmware.py`'s two dangling references to the deleted `podcast-app/app/hiby_player.sh` (error-message text only, never actually read from disk) repointed at the still-live code they were describing. |
| R43 | **Grey theme** | Add a grey theme option positioned between the dark and light themes. The grey palette should provide a middle ground — readable enough in daylight without the starkness of full white (light theme), and easier on the eyes in moderate lighting without the depth of full black (dark theme). Implement: (1) define grey colour values for all six base UI colours, (2) integrate into the existing `apply_theme()` system, (3) allow selection via Settings toggle. |
| R44 | **Timezone setting** | Add a timezone setting to the Library app, stored in `settings.txt` (per R40). This setting enables time-aware features (e.g., theme auto-switching based on sunrise/sunset). Settings becomes scrollable to accommodate the new row. Implement: (1) add a timezone picker in Settings (list of common timezones or geographic selection), (2) validate and persist to `settings.txt`, (3) provide a getter for other subsystems to query the current timezone. |
| R45 | **Theme auto-switching with four options: light, dark, grey, auto** | Expand the theme system to support four modes: (1) **Light** — always light theme, (2) **Dark** — always dark theme, (3) **Grey** — always grey theme, (4) **Auto** — automatically switch between light and dark based on sunrise/sunset times calculated from the device's timezone (R44) and current time. Implement: (1) extend Settings to show a four-option picker, (2) calculate sunrise/sunset via the timezone and current date (can use a simple library or algorithmic formula), (3) switch theme at dawn/dusk automatically (check during the UI refresh loop), (4) allow manual override in the dropdown to temporarily switch themes without changing the Auto setting. Persist theme choice to `settings.txt`. |
| R46 | **New album and artist layouts** | Design and implement new layout options for albums and artists browsing. Scope to be discussed during development — possible directions include: cover-art-heavy grid views, list variants (compact/expanded), sort/filter options, or alternative display modes. File this as a placeholder; detailed requirements and mockups should be added when the ticket is picked up. |
| R47 | **Shuffle and Repeat modes in Music player** -- **needs another visual pass, flagged to revisit** | Queue logic is real and in place: `next_track_index()`/`prev_track_index()` drive every path a track can advance through (natural end-of-track roll-forward, the fallback restart, hardware keys, on-screen transport, mini-player) -- shuffle keeps a Fisher-Yates order anchored on whatever's currently playing (toggling on mid-album doesn't jump). Simplified from the original two-control spec to a single button per live direction while testing: one control, cycling Normal -> Shuffle -> Repeat -> Normal, in the same slot the audiobook player already uses for its own extra control past the skip rings (`mid-96-70`, then nudged further left and thinned to a 1px ring per live feedback -- may want another pass once seen fresh). Ring is COL_DIM (matching the artist line's weight), letter inside (S/R/-) brightens to COL_ACCENT when engaged. **Repeat means the current track (`REPEAT_ONE`), not the whole queue** -- an explicit call made live: looping one song is what a single dedicated button is actually for, looping a whole album barely differs from letting it play normally. (First cut mapped it to `REPEAT_ALL`, which only ever wraps at the *end* of the queue -- reported live as "repeat doesn't loop" simply because nobody had sat through a whole album to see it wrap; repeat-one sidesteps that by looping immediately, every track.) State also shown as a short suffix on the mini-player's subtitle (the Now Playing format-line prefix from a first attempt was removed per live feedback -- redundant next to the button itself). Persisted to `music.conf` (`shuffle_enabled`, `repeat_mode`), not `settings.txt` as the ticket says -- same reasoning as R41/R43-45's theme keys, pending R40's actual migration. Verified live: queue logic and button all work; positioning/sizing went through several live-feedback rounds already and is explicitly flagged here for one more look rather than called finished. |
| R48 | **Remove tracks from playlists** | Add the ability to remove individual tracks from playlists. Implement: (1) **UI**: long-press or context menu on a track within a playlist to reveal a delete/remove option. (2) **Storage**: update the playlist file (or database entry) to remove the track reference. (3) **Confirmation**: optionally show a confirmation prompt to prevent accidental deletion. (4) **UX**: clear feedback (toast or visual update) when a track is removed. Track should disappear from the playlist view immediately. (5) **Scope**: applies to user-created playlists; consider whether removal from generated playlists (e.g., "Recently Played") should be prevented or handled differently. |
| R50 | **USB Transport Mode: disable VBUS power (data-only), self-powered DAC/amp** | Reported live: USB audio out drains the R1's own battery unnecessarily when the connected DAC/amp already has its own power source, because the R1 supplies 5V VBUS to the port regardless. Wanted as a third effect under the existing "USB Transport Mode" toggle (R37, renamed from "Bypass DSP on USB" -- see music_hook.c/audio.c, already ships PEQ/MSEB/Bluetooth-off and volume-locked-at-100% for that same toggle), alongside a new "lock volume at 100%" which **is done and shipped**. **VBUS control is not a userspace knob on stock** -- traced to `drivers/usb/phy/phy-ingenic.c`'s `usb_phy_set_vbus()`, called automatically by the OTG/USB core via the `gpiod_drvvbus` GPIO; nothing exposes independent control. A kernel patch was written (new `vbus_manual` sysfs attribute, `auto`/`off`/`on`, same string-command convention as the existing `sw_switch_hsotg` attribute in the same file; `usb_phy_set_vbus()` checks a new `drvvbus_manual`/`drvvbus_manual_value` pair on `struct usb_phy_data` and overrides whatever the OTG core asks for while set) -- built clean, all vendor symbols present, boots fine. **Reported live: crashed during a podcast feed update on this build.** Not yet root-caused -- this patch combination (the two already-shipped kernel patches plus the new VBUS one, no interleaved-DMA) had never been soak-tested for the WiFi/DMA lockup class of bug documented in the hiby-r1-linux-kernel-compiling repo's own `docs/06-known-issues.md` before this crash happened; unknown whether the new VBUS patch is actually implicated or whether this is the same still-unresolved kernel-level issue surfacing in a build that happened to not be HiBy's literal stock binary. **Reverted**: device is back on the known-good stock kernel (`STOCK_KERNEL_ABTEST`) as of this entry. Next step before resuming: soak-test (see known-issues doc's own recipe) on a build with the VBUS patch but re-confirm it isn't the interleaved-DMA-adjacent bug recurring via a *different* trigger, before assuming the new patch itself is at fault. The patch source is preserved in this session's history, not yet committed to the kernel-compiling repo (deliberately -- it hasn't been shown safe). |
| R49 | **Add album year to index and display on album page** | Implement album year support: (1) **Parsing**: extract release year from track metadata (`DATE`/`TYER`/`TMT1` tags or equivalent per format). When multiple tracks in an album have different years (e.g., deluxe editions with bonus tracks from another year), use the earliest year for the album as a whole (representing original release). (2) **Index**: add a year column to `music_index.db`'s album cache (R38), computed once at index-rebuild time. (3) **Display**: show the year on the album's header/title bar on the album-detail page (e.g., `Album Name (1985)` or `Album Name — 1985`). Scope: music albums only; audiobooks and podcasts do not need year support. (4) **Browse**: consider adding a "Year" facet to the Music menu for browsing by decade or individual year (scope TBD during implementation). |
| R50 | ~~**Display track count and total playtime in queue header**~~ **done, verified live** | Added a fixed summary line ("N tracks · H:MM total") directly under the "Queue"/"BACK" header, above the scrolling rows -- track count is the whole queue (matching what the list itself has rows for), playtime is summed from the currently-playing track onward, not the whole queue, since "how much music remains" is what a summary line is actually for. New `QUEUE_SUMMARY_H` constant threaded through `vis_rows()` (one less row's worth of scrollable height on this screen) and the row-tap hit-test (`idx` math, which otherwise resolved every tap one row short of what was actually under the finger -- same class of bug as BG2/BG61). Updates automatically on every redraw, so it tracks Play Next/skip live without any separate invalidation. **Verified live**: queue header reads "12 tracks · 50:13 total" for a full untouched album queue; tapping a row still opens the right track (confirmed on "Wonderwall", landed on "3 of 12" correctly) once the header's own vertical layout was corrected (first pass drew the summary line too low for its allotted height, clipped by the first row's background -- fixed by widening `QUEUE_SUMMARY_H` 36->44px and adjusting the text's y-offset to fit). |
| R51 | ~~**Add settings cog icon to quick-settings dropdown**~~ **done, verified live** | First attempt added a whole extra row below MSEB (hand-drawn cog, "Settings" + a description line) -- corrected live twice: (1) too much visual weight for a shortcut, moved to a small icon in the top-right corner, level with "Brightness" opposite it (`qs_gear_x()`/`qs_gear_y()`), `QS_H` reverted to its original 490 since the extra row is gone; (2) the hand-drawn cog itself replaced with the real thing -- vendored Font Awesome's `gear-solid-full.svg` through the existing `gen_icons.py` pipeline (same rasterize-and-bake process wifi/bluetooth's icons already use), baked at a small 20x20 to match its new corner placement. Tapping it closes the panel and opens `SC_SETTINGS` directly. Confirmed working live. |
| R52 | ~~**Clear queue button**~~ **done, verified live** | Added a "Clear" header action to `SC_QUEUE` (`queue_clear_x()`, same pattern as MSEB's "Reset"/Podcasts' "Sync", dimmed when there's nothing to clear). Tapping it drops everything queued after the currently-playing track; that track keeps playing to its own natural end, it just has nothing queued up behind it once it does. No confirmation dialog -- no precedent anywhere else in this app, and nothing here is lost for good. Gap from "BACK" widened live (20px -> 40px, `header_back_x() - 40 - clear_w`) -- felt too close for an action that actually discards something, unlike Reset/Sync next to it. Surfaced its own bug in testing, filed and fixed as BG77 (see the Bugs section). |
| R53 | ~~**Rubber banding 25% quicker**~~ **done, confirmed fine live** | The "deceleration/return-to-bounds animation" is the spring-back pull in the inertia tick (`list_velocity += (bound - cur) * 0.15f`) -- raised to `0.1875f` (+25%). Friction decay and live-drag overscroll damping left alone (out of scope -- see the code comment). Confirmed fine live, no further changes requested. |
| R54 | ~~**Artist page: photo, bio and album list, reachable by tapping an artist's name**~~ **done, verified live (photo fallback pending real network)** | New `SC_ARTIST_PAGE` screen, same continuous-scroll shape as the album page (R46): artist photo (own independent buffer, `artist_art_bits`, same BG70 reasoning as the album page's `view_art_bits`), name, "N albums", the artist's own album list (`lib_albums("album_artist", ...)`, tapping a row opens that album), then a word-wrapped bio underneath if one exists. Reached by tapping the artist's name on the album page (a new tap zone in the previously-dead header); "back" returns to the specific album it was opened from, not a generic fallback. Photo and bio come from Last.fm's `artist.getinfo` (one call for both, new `lastfm_fetch_artist()`); Spotify's artist search (new `spotify_fetch_artist_image()`) is the same image-only fallback pattern `spotify_fetch_cover()` already is for albums. Cached to a dedicated `.artist_cache/` directory on the SD card, keyed on a filesystem-sanitized artist name, with the same `.no_match` sentinel convention `art_worker()` uses. Reuses the plain-album screen's scroll/drag/spring-back machinery via a new `artist_page_max_px()` sibling to `tracks_max_px()`. Bio wrapping (`artist_bio_layout()`) shares `pod_draw_notes()`'s word-wrap logic, split into a measure/draw dual-mode function since the artist page needs the bio's total wrapped height up front for scroll bounds, which podcast notes (self-scrolling in a fixed box) never needed. **Verified live** navigating Oasis's album -> tapping the artist name -> correct name/album count/album list, confirming both albums and back-navigation to the right album. Two real bugs surfaced and fixed in the same live pass: (1) **Last.fm's "no artist photo" response isn't an empty field, it's a generic placeholder graphic served as PNG** -- this device's decoder is JPEG-only (`cover.c` dlopens libjpeg, nothing else, the same gap BG52 found for a podcast cover), so a "successful" download that doesn't decode was being counted as a real photo anyway, permanently skipping the Spotify fallback and silently caching a PNG under a `.jpg` name forever. Fixed by only counting a photo as found once `cover_load()` actually decodes it -- a cached file that fails to decode is deleted and re-attempted (Spotify included) rather than trusted forever. Confirmed live: the bad cached PNG was correctly detected and removed on the next visit. (2) **Bio text was clipped mid-word at the right edge of every line** -- `draw_text_clip()`'s clip-boundary parameter is an absolute screen x-coordinate (`right_edge - x` is the available width internally, confirmed by reading `text_draw()`), not a width the way every wrap decision in `artist_bio_layout()` was measuring against -- passing the bare width `w` instead of `x + w` left the actual drawable area ~24px short of what the wrap loop had allowed, silently truncating the last word or two of nearly every line. Confirmed fixed live -- full lines now, e.g. "Oasis are an English rock band formed in Manchester in / 1991. The original lineup consisted of Liam Gallagher on...". The real-photo path was confirmed once the device actually joined a network (the earlier attempt had the WiFi radio on but not associated, `wlan0` had no IP -- a device state, not an app bug): Led Zeppelin and Queen both came back "photo yes, bio yes" live, and Oasis's own photo (Spotify's fallback, since Last.fm's own image for Oasis turned out to be the placeholder PNG from bug (1) above) rendered correctly as a real 640x640 band photo. Two more real bugs surfaced testing multiple artists back to back: (3) **Some artists' bios cut off mid-sentence** -- checked live against Queen's actual Last.fm response: `bio.summary` (what was being read) is Last.fm's own pre-truncated preview and isn't guaranteed to end on a sentence boundary -- Queen's genuinely stops at "...Mercury, a fan of Smile" right before the "Read more" link, which is Last.fm's own data, not a parsing bug. Switched to `bio.content`, the real complete biography (~3KB for Queen against summary's ~250 bytes) -- confirmed unique in the response so needs no bounding, unlike the removed attempt to bound `summary`'s search to the `"bio"` object by assuming `"similar"` was the next sibling key: checked against a real response and that assumption was simply wrong (`similar` appears *before* `bio`, near the top, not after), which made the bound a permanent no-op rather than actively harmful. Bio buffers grown 4KB -> 8KB (matching `pod_notes_text`'s own precedent) since a full biography can run longer than a summary ever would. (4) **Navigating to the artist page took a couple of seconds, every time** -- `artist_art_request()` synchronously joined any still-running previous fetch thread before starting a new one; unlike `view_art_request()`/`art_request()` (where this same join rarely costs anything, since most album covers resolve from a local file in milliseconds), the artist page has no local fallback at all -- every visit is a real multi-second network round trip, so re-entering the screen while an earlier fetch was still in flight blocked the UI for however long that fetch had left. Fixed by detaching the old thread instead of joining it -- its eventual result is silently discarded by the same staleness check (`strcmp` against `artist_art_want`) that already existed, so nothing depended on the join for correctness, only tidiness. Both fixes build clean in both repos, deployed; stale cached bios (the old, truncated `summary` text) cleared from `.artist_cache/` on-device so the fix actually takes effect on the next visit rather than serving the old cache forever. Pending live re-confirmation of the speed fix specifically and the fuller Queen bio. |
| R55 | ~~**Tapping an artist row in the Artists list should go straight to that artist's own page**~~ **done, build clean, pending live verification** | The Artists list (Music -> Artists or Album artists) previously always landed on the plain Albums-filtered-by-artist view on tap -- now goes straight to `SC_ARTIST_PAGE` (R54) instead, since that page already shows the same album list plus the photo/bio R54 added; nothing is lost, just a now-redundant stop skipped. Queries with `cur_facet` (not a hardcoded `"album_artist"` the way the album page's own artist-name tap uses) since this list can be reached via either "Artists" or "Album artists", and the two need different columns. Surfaced a real gap while building it: an album opened from the artist page's own album list had no way to get "back" right -- go_back()'s default case unconditionally restored the Albums list, skipping straight past the artist page the user was actually just on. Fixed with a new `tracks_from_artist_page` flag (same pattern as `browsing_is_playlist`), set at both `SC_TRACKS` entry points and checked first in go_back()'s default case; and a matching `artist_page_from_list` flag on `SC_ARTIST_PAGE` itself distinguishing "opened from the Artists list" (back -> that list, `artists_scroll_saved` restored the same way `SC_ALBUMS`'s own case already does) from "opened from an album's artist name" (back -> that album, R54's existing behaviour, untouched). Build clean in both repos, deployed. Not yet live-verified. |
| R56 | ~~**Disc banners instead of inline disc numbers**~~ **done, build clean, pending live verification** | Replaced the old inline disc-number-next-to-track-number column with a full-width banner (compact-disc icon, "Disc N", that disc's own total playtime right-aligned in line with track playtimes) inserted before each disc's first track on a multi-disc album -- vendored `compact-disc-solid-full.svg` through the existing `gen_icons.py` pipeline. Real architectural change, not just a draw tweak: a row's on-screen position no longer maps to its track index by a flat `idx * ROW_H` once banners are interspersed, so the old `first_idx` scroll-position shortcut and the tap handler's plain division both had to go. Replaced with a shared-shape pair -- the draw loop walks every track from `idx == 0` accumulating a running position (banners add `DISC_BANNER_H`, tracks add `ROW_H`), and a new `track_index_at()` walks the identical layout to turn a tap's content-space y back into a track index (or -1 for a tap that lands on a banner itself), so the two can never disagree about where a row actually is -- the same class of bug BG2/BG61 already fixed once for scroll_px. `tracks_max_px()` grown by `tracks_banner_px()` (total banner height) so scroll bounds account for the added content. Single-disc albums are entirely unaffected (`tracks_multi_disc()` gates the whole thing, same rule the old inline numbering used). Build clean in both repos, deployed. Not yet live-verified. |
| R57 | ~~**Real icons for the shuffle/repeat mode button, replacing the "S"/"R"/"-" letters**~~ **done, build clean, pending live verification** | Vendored `bars-solid-full.svg` (default/off), `repeat-solid-full.svg`, and `shuffle-solid-full.svg` through `gen_icons.py`, grouped as one family (not three independent singletons) since all three swap for each other at the exact same on-screen spot and need the shared-crop-box treatment the family mechanism exists for -- fit independently, bars' wide/flat bounding box against shuffle's/repeat's more square ones would've come out visibly different sizes for what's supposed to read as one button in three states. Swapped straight in for the old `draw_text()` letter, same ring, same COL_DIM/COL_ACCENT on/off colouring. Build clean in both repos, deployed. Not yet live-verified. |
| R58 | ~~**Queuing for Podcasts: play next, add to queue**~~ **done, build clean, pending live verification** | Its own stated blocker (L5, unified audio pipeline) turned out to already be satisfied -- confirmed while working BG84 that podcasts and music have shared one `audio.c`/`worker()`/`queue[]` for a while now, not two separate pipelines. Once that was established, (1) and (2) turned out to already work: the episode list's own long-press sheet (`music_hook.c` ~line 7761) was never actually excluded for `pod_list`, so "Play next"/"Add to queue" already called the exact same `queue_play_next()`/`queue_insert()` music uses. What was still genuinely missing/broken: the Now Playing queue button special-cased `podcast_mode` straight to `go_back()` (item 3, on the old "a podcast's queue is always just its one episode" assumption) instead of opening `SC_QUEUE` -- now removed, since `SC_QUEUE` already draws `queue[]` generically (name + duration, nothing podcast-specific needed) and that assumption no longer holds once an episode can queue another. Deeper problem surfaced by that fix: `play_index()` (the generic queue-advance function -- Next/Prev, a `SC_QUEUE` tap, the natural end-of-track fallback restart) unconditionally forced `podcast_mode = 0`, which would have silently dropped back to the plain-music Now Playing UI the moment playback actually reached a queued episode. Fixed by no longer resetting `podcast_mode` there, moving that reset to the one caller that legitimately means it (`play_from_list()`, a fresh Albums/Playlists browse). Two more latent gaps caught while wiring this up, before they could bite: entering a podcast feed's episode list never reset `browsing_is_playlist` (stale from an earlier Playlists visit would make a podcast queue wrongly skip Play Next's leftover-truncation, the one thing that made it behave like an album's queue instead of a playlist's) -- now explicitly cleared there, same as Albums' own entry already does. And an undownloaded episode has an empty `path` (documented convention: callers must check `.downloaded`, not rely on `audio_play()` failing gracefully) -- the long-press sheet didn't check this, so queueing one silently would have gone nowhere; now shows "Not downloaded yet" instead of a fake "Playing next"/"Added to queue" note. **Follow-up, same pass (explicit: "I don't want mixed queues"):** because `queue[]` is deliberately one generic array for everything, the newly-reachable podcast queueing also made a cross-kind mix reachable -- browsing a podcast feed while *music* played and hitting "Add to queue" would have spliced an episode into the music queue (and the same hole already existed in the other direction for audiobooks, whose queue *is* their chapter table: `ab_follow()`/`ab_save_current_pos()` index straight into `ab_book.chap[]` by `cur_track`, so a foreign entry desynchronises both). Nothing crashed on a mix -- it just played as an entry the surrounding UI describes wrongly, which is worse than refusing. Added `queue_kind_conflict()`, checked at the sheet's two queueing actions (the only callers): refuses whenever an audiobook is playing at all, or whenever the browsed kind (`pod_list`) differs from the playing kind (`podcast_mode`), with a note naming what's already playing rather than a generic refusal. Radio deliberately isn't covered -- it has no queue to mix into (`queue_follower()` hands the worker NULL outright while `radio_mode`), so it's treated as a cold start like any other. Build clean in both repos. Pending live verification: queue a second episode from a feed while one plays, confirm the queue button shows both, confirm playback rolls into the second keeping podcast-mode UI (skip arcs, speed control) rather than reverting to the plain music transport -- and confirm the cross-kind refusal actually fires (play music, browse a podcast feed, long-press an episode). |
| R59 | ~~**Holding rewind/fast-forward button should skip in multiples**~~ **done for audiobooks/podcasts, build clean, pending live verification** | "Design TBD" per the ticket -- went with its own literal first example verbatim: holding a skip arc repeats it every 350ms, with the per-repeat amount keyed on total held time (<3s: 10s, <6s: 30s, 6s+: 1 min), so the first repeat lands at the same amount a plain tap would. New `skip_zone_at()` mirrors the release-tap handler's own hit-zone math exactly (audiobook's thirds, podcast's BG47 midpoint zones) so a hold targets the same button a tap there would. Reuses the existing `hold_fired` mechanism (already how the track-list long-press sheet suppresses its own release-tap) to stop the hold's *last* repeat from also firing the release handler's single skip. Also reuses R60's new seek-toast so an escalating hold shows its amount changing, not just the progress bar jumping. Deliberately NOT done for plain music, despite the ticket listing it: music's transport has no seek/skip button to hold in the first place, only next/prev-track buttons -- "hold to skip in multiples" there would mean auto-advancing through several tracks unattended, a materially more disruptive miss (skipping past 4 songs) than overshooting a seek by 20s (trivially correctable), and a genuinely different feature from what this ticket's own example describes. Left out rather than built speculatively; flag if that's wanted too. Build clean in both repos. Pending live verification: hold the audiobook/podcast skip arcs and confirm the escalation actually feels like the ticket's own example, and that a hold doesn't also fire a stray tap-skip on release. |
| R60 | ~~**Hardware rewind/fast-forward buttons should correspond to on-screen controls**~~ **done, build clean, pending live verification** | Two concrete mismatches found in `handle_keys()` (both KEYS_BUTTONS -- this unit's own physical buttons, explicitly labelled "skip forward"/"skip back" per that function's own top comment -- and KEYS_REMOTE, a paired Bluetooth AVRCP remote): (1) audiobook_mode's skip-forward/back buttons did a full `ab_play_chapter()` jump, a different action than the on-screen transport's own +/-10s seek arcs (whose own comment explains the on-screen design deliberately chose a seek amount over a chapter-skip triangle) -- now seeks 10s to match. (2) podcast_mode's skip-back was -30000 against the on-screen arc's -10000, an asymmetric mismatch even though the forward side (+30000) already matched its own +30s arc -- now -10000 on both hardware paths. The dedicated `KEY_FASTFWD_`/`KEY_REWIND_` keys (a genuine seek-amount button, not a next/prev one) were a flat +/-10000 for every mode, silently wrong for podcast's own +30s forward convention -- now mode-aware the same way. Left KEYS_REMOTE's NEXTSONG_/PREVSONG_ chapter-jump for audiobooks alone deliberately: a generic remote's "next/prev" reasonably means "next chapter," a different and legitimate mapping from this unit's own "skip forward/back"-labelled buttons, not a bug to unify away. Visual feedback: added `seek_toast()`/`draw_seek_toast()` (same countdown-tick pattern as the existing volume HUD) -- every hardware-triggered seek now shows a "+10 s"/"-30 s" pill under the status bar for ~1.5s, addressing the ticket's explicit ask since a hardware press (unlike an on-screen tap) has no other confirmation the user is even looking at. Build clean in both repos. Pending live verification: press the unit's own skip buttons and a paired remote's FF/RW/next/prev in each mode, confirm the seek amounts now match their on-screen counterparts and the toast appears. |
| R61 | **Sleep mode for audiobooks** | Add a sleep/auto-stop timer to the audiobook player. When enabled, the playback will automatically pause after a specified duration (e.g., 5, 10, 15, 30 minutes, or 1 hour). Implement: (1) a sleep timer control in the audiobook Now Playing screen (similar to music's quick-settings approach or a dedicated button), (2) a countdown display showing time remaining before auto-pause, (3) option to disable/cancel the timer before it fires, (4) resume position should be saved automatically when the timer fires so the audiobook can resume from that exact point on next open. Scope: audiobooks only (music can use the system-level auto-shutdown already in Settings; podcasts can have their own separate timer if desired). Useful for listening while falling asleep. |
| R62 | **Continuous automatic title text scrolling, with manual mode as option** | Change title text scrolling behavior: implement continuous/automatic left scrolling (marquee style) as the **default**, replacing the current manual tap/interaction requirement. The scrolling should repeat continuously when a title is too long to fit on screen. Add a Settings option to toggle between (1) **Auto-scroll** (default, new behavior) and (2) **Manual** (current behavior, tap to scroll). Applies to track/chapter/episode titles in the Now Playing screen and mini-player. Scrolling speed should be consistent and readable (not too fast). This improves UX on longer titles by making them readable without requiring user interaction. |
| R63 | **Listening statistics and analytics** | Add a comprehensive stats/analytics feature tracking listening habits across all content types. Implement: (1) **Music stats**: total time listened by artist, album, genre; top artists/albums over time periods (all-time, this month, this week); (2) **Audiobook stats**: total time listened by author/book; books completed; average chapters/day; (3) **Podcast stats**: total time listened by feed; episodes completed; subscriptions; (4) **Radio stats**: total time on radio; favorite stations; (5) **Global**: total listening time across all types, breakdown by content type (music %, podcasts %, audiobooks %, radio %). Display: a dedicated Stats screen in the main menu or Settings, with views for different time periods (all-time, yearly, monthly, weekly) and sortable lists. Data persistence: track listening time (play duration) and store in a local database. Consider: privacy (no cloud sync unless explicitly enabled), accuracy of pause/skip detection, resetting stats. Scope: optional feature, data stored locally on device only. |
| R64 | **Maintain WiFi, Bluetooth, and brightness states between reboots** | Persist the device state across reboots for three key settings: (1) **WiFi radio state** — whether WiFi was on or off when the device last shut down, restore that state on boot (not force-on, which drains battery); (2) **Bluetooth radio state** — same as WiFi, restore previous on/off state; (3) **Brightness level** — save the last-set brightness value and restore it on boot (note: BG78 already addresses this but should be fixed as part of this ticket). Implement: save state to persistent storage (Settings or dedicated state file) whenever the user toggles these, and restore from that file on app startup (or system startup hook, depending on where these states are managed). This improves UX by avoiding unexpected state changes post-reboot and reduces frustration with manual re-enabling. Related to BG78 (brightness persistence). |

| R65 | **Add podcasts from within the app (podcast discovery and subscription)** | **Tried and reverted, 2026-09-01/02**: built as in-app search via iTunes Search + the T9 keyboard, deployed and tested live. Surfaced two real, unrelated bugs along the way (now fixed and kept, since they're general): the T9 keyboard's Done/Cancel buttons were being swallowed by the generic header-tap handler instead of reaching the keyboard's own dispatch (landed on Albums instead of committing), and `POD_NAME_LEN` (64) truncated a real feed folder name of exactly 64 characters, silently breaking its episode list. The search/subscribe feature itself was rolled back at the user's request -- back to editing the `Podcasts` section of `settings.txt` directly (or via the companion Android app) as the only way to add a feed. Original scope, unchanged: Implement in-app podcast feed discovery and subscription, similar to AntennaPod's UX. Implement: (1) **Search/discover**: a search screen in the Podcasts section that allows users to search for podcasts by name or keyword (integrate with a podcast directory API like PodcastAddict API, Podindex, or iTunes/Apple Podcasts search), (2) **Add feed**: ability to paste or type a feed URL directly, (3) **Subscribe**: one-tap subscribe to a discovered podcast, which adds it to the local `feeds.txt`/database, (4) **Unsubscribe**: remove a podcast feed from the list, (5) **Feed refresh**: automatic or manual refresh of feed updates. Design: a dedicated "Add Podcasts" or "Browse" screen accessible from the Podcasts main menu, with search results showing podcast artwork, title, description, and episode count. This removes the friction of manual feed URL entry and makes podcast discovery a native part of the app. Related to L5 (unified Library) and podcast integration. |
| R66 | **Resume playback and navigation after auto-shutdown** | When the device shuts down via the auto-shutdown timer (R24/Settings), startup should seamlessly resume the user's exact previous state: (1) **Playback resumption**: resume the same track/chapter/episode from the exact position where it was paused, (2) **Navigation resumption**: return to the exact screen the user was on (album detail, now playing, queue, etc.), (3) **Visual feedback**: instead of showing the stock Hiby logo or penguin splash screen after boot, display a screenshot of where the user was, with a loading indicator to show the device is resuming. **Bonus**: make the screenshot slightly blurred with a "loading..." message overlaid, to differentiate it from a frozen/crashed state. Implementation: save a framebuffer snapshot and app state (current screen, playback position, navigation stack) when auto-shutdown triggers, then restore both on startup. This creates a seamless, battery-saving experience where the device feels like it simply paused rather than rebooted. |
| R67 | **Upgrade and slim down BusyBox (rootfs), built with musl** | **Slated for 0.45.** Currently `/bin/busybox` on-device is 1.31.1, dynamically linked against the rootfs's own glibc 2.22, 868KB, 273 applets symlinked in `/bin` and `/sbin`. Two independent changes, planned as separate build/flash steps so a boot failure points at one cause, not two: (1) **version bump to 1.38.0** (current latest stable) with the *same* applet set as today, to isolate any behavior change in the new version by itself; (2) **switch to musl, fully static**, built via the same `zig cc` toolchain `build.sh` already uses (`-target mipsel-linux-musleabihf -static`, confirmed working against a real MIPS test binary). Unlike RP7's musl finding for `libmusic_hook.so` (a dead end there because it shares one process with `hiby_player`'s glibc and needs `dlopen()` against the device's glibc-built `libasound`/`libjpeg`/etc.), neither restriction applies to BusyBox: it always runs as its own separate process, and doesn't `dlopen()` anything itself (its own `passwd`/`group` lookups just parse the flat files directly, which musl does natively without glibc's NSS-plugin mechanism). A static build also stops BusyBox depending on `/lib/ld.so.1`/`libc-2.22.so` being intact at all -- a real robustness win given BusyBox *is* `/bin/sh` and half of `/etc/init.d`. **Applet trimming is a third, later, separate step** (see the RAM/CPU question this ticket also opens, below) -- not bundled into either build above, same one-variable-at-a-time reasoning. Audited via cross-referencing all 273 applets against every shell script in the extracted rootfs (`hiby-fw/work26/root`, init.d + OTA scripts + `module_driver`) and `strings` on every ELF binary (`hiby_player`, `sys_server`, `adbd`, `dbus-daemon`, `bluetoothd`) for anything they might shell out to invisibly -- about 100 applets show real usage, ~170 look unused, but the ELF-strings cross-check is inherently noisy (BusyBox's own binary lists all 273 applet names internally for its own dispatch, contaminating any "referenced in an ELF" signal) so this is not yet trustworthy enough to act on alone. **Bricking risk is real but recoverable**: BusyBox is boot-critical (every `sysinit` line in `/etc/inittab` calls into it before anything else runs, including the one thing that would normally run a recovery script), but the Volume Up + Power failsafe (documented in `NOTES-kernel-optimisation.md`, already used once for real during kernel work) reads from SD card independent of the rootfs, so a bad flash is recoverable rather than a true brick. **Open question raised alongside this, not yet measured**: does the musl/static switch actually save RAM or CPU, or just binary size and boot-time syscall overhead (skipping the dynamic loader on every one of the many short-lived BusyBox invocations `init.d` fires off sequentially at boot)? Reasoned answer pending a real measurement (per this session's own standing rule of measuring rather than guessing wake-latency-style regressions): static linking means BusyBox's copy of libc functionality is no longer shared with the *already-resident* dynamic `libc-2.22.so` pages that `hiby_player`/`dbus`/`bluetoothd`/`wpa_supplicant` map anyway, so total system RAM may not meaningfully drop and could tick up slightly (musl's static footprint is small, but it's a second copy) -- the more plausible real win is boot-time CPU, from skipping per-invocation dynamic-linker relocation work across the many BusyBox calls `init.d` makes in sequence, though that hasn't been isolated from the SD-card-mount/kernel-boot costs already identified as the actual dominant boot-time costs (RP-boot-time investigation, informal, not yet ticketed). Needs an actual before/after measurement once built, not assumed. |
| R70 | ~~**Editable queue: drag to reorder, swipe to remove**~~ **done** | All three parts done for `SC_QUEUE`, plus playlists and podcast episode lists. **Playlists, done 2026-09-03**: the full treatment, on `SC_TRACKS`'s continuous-scroll path (the one plain albums and playlists share) rather than `SC_QUEUE`'s flat one -- `track_index_at()` does the hit-testing instead of a plain division, since disc banners (never present for a playlist, but the function is shared) mean row position doesn't map to index that simply. `playlist_drag_active`/`playlist_drag_idx`/`playlist_drag_grab_dy` and `playlist_swipe_active`/`playlist_swipe_idx`/`playlist_swipe_dx` mirror the queue's own state shapes exactly; a reorder or removal calls the new `playlist_move_track()`/`playlist_remove_track()`, which edit `tracks[]` in place and then call `playlist_persist()` to rewrite the whole `.m3u` via the new `pl_write()` (playlist.c) -- a full rewrite rather than a patch, since a reorder touches every line anyway and this app's own `pl_append()`-created playlists carry no EXTINF/decoration to lose. The `.m3u` path is looked up fresh by name each time (`cur_playlist_path()`), not cached at the one call site that opens a playlist from `SC_PLAYLISTS`, since `browsing_is_playlist` can also become true via the "back from Now Playing" route (BG73), which never populates a cached path. **Podcast episodes, done 2026-09-03**: swipe-to-remove only, no grip and no drag -- episodes have no user-chosen order to reorder, just newest-first by mtime/pubdate. This is `SC_TRACKS`'s *other*, flat `CONTENT_Y`/`scroll` path (the one audiobook chapters also use), so the touch math matches `SC_QUEUE`'s own flat style, not `track_index_at()`. Swiping calls the new `pod_remove_download()`, which deletes the file via `pod_delete_download()` (podcast.c: `unlink()` plus dropping the file's resume entry) and then re-reads the feed from disk (`pod_load_episodes()` + `pod_rebuild_tracks()`) rather than patching `pod_eps[idx]` in place -- the same one-shot recompute pattern a completed download already used. Explicit product decision (asked live rather than guessed, since deleting a file is irreversible): the episode stays listed afterward, shown as not-downloaded, same look as one never fetched, rather than disappearing from the list. Gated to downloaded episodes only -- nothing to delete for a manifest-only entry, and the row doesn't visually react to a swipe that would do nothing. Both additions needed the same "was this release the end of a gesture, not a tap" guard `SC_QUEUE` already required, now also threaded through `list_dragging`'s own condition and the generic tap-dispatch for both `SC_TRACKS` paths. **Not yet live-verified**: built and deployed, but the device was disconnected for the whole session this landed in, and the `tap.py`/`key.py` synthetic-input tools were confirmed (also this session) not to actually reach the app on this hardware -- the on-device log only ever showed real hardware key presses, never an injected one, even though the writes to `/dev/input/eventN` succeed silently. So this shipped in 0.46 on code review alone; needs a real live test. Original step-by-step history, `SC_QUEUE`-only: (1) **done, 2026-09-02** -- a visual grip handle (`draw_grip_icon()`/`draw_grip_icon_clip()`, six dots in two columns) on the right edge of every `SC_QUEUE` row, marking what a drag will grab once dragging exists; title's clip width and the duration column both shifted left 40px to make room. (2) **done, 2026-09-02** -- drag via the grip to reorder, for music and podcasts (audiobooks structurally can't reach `SC_QUEUE` at all -- their own queue button goes straight to `go_back()` into the chapter list, so no explicit exclusion was even needed). `queue_drag_active`/`queue_drag_display_i`/`queue_drag_grab_dy` track a live vertical drag started from the grip's own hit zone (checked ahead of the ordinary list-scroll block so a grip-drag suppresses scrolling entirely, same as `scrub_active`/`qs_open` already do for their own drags); crossing a slot's midpoint calls `queue_move_display(from, to)`, which reorders `shuffle_order[]` when shuffle's on (matching `queue_display_index()`'s own branch, so a drag while shuffled moves the *display* position rather than fighting BG71) or `queue[]` directly otherwise, carrying `cur_track` along by path so the physically playing track is never lost track of mid-shift. The dragged row snaps between grid slots rather than floating continuously under the finger (no separate compositing layer for an overlay row exists in this drawing model) but is clearly marked while held -- full-row highlight plus a 2px accent edge top and bottom, and the grip glyph itself turns accent-coloured. A release is explicitly excluded from also registering as a tap-to-play on whatever track the drag ended up over (`queue_drag_active` is still true for exactly the tick a drag ends, before the live-tracking block clears it, which is what the play-on-tap guard checks). Not yet handled: dragging to the very top/bottom edge of the visible list doesn't auto-scroll, so reordering across a queue longer than one screenful needs multiple drags. (3) **done, 2026-09-02** -- swipe left or right to remove. `queue_swipe_active`/`queue_swipe_display_i`/`queue_swipe_dx` latch a candidate swipe the moment horizontal travel clearly leads vertical (only for a press that didn't start on the grip -- the grip is reorder-only, the row body is swipe-only, never both for the same touch), then track live_x - touch_x every tick until release; crossing a fixed 120px in either direction calls `queue_remove_display()`, otherwise the row just springs back with no change. 120px fixed rather than a fraction of `FB_W`, so the threshold doesn't need retuning if this ever runs on a different screen size. `queue_remove_display()` physically deletes from `queue[]` (shuffle or not -- shrinking `queue_n` alone leaves `shuffle_n` stale, which `queue_display_index()`'s own existing lazy check already treats as "regenerate", the same mechanism `next_track_index()` already relied on); removing the row that's actually playing auto-advances to whatever now sits in that same display slot rather than stopping or freezing on a gone track -- the decision this ticket's own open question asked for, resolved as "closes the gap and keeps going", the convention every mainstream player already uses. Drawing: the swiped row's content (title/duration/grip) all shift by the live drag offset, revealing a full-row `COL_ACCENT` fill (drawn first, unshifted) underneath -- the design spec given ahead of the build, distinct from the plain background a reorder-drag reveals instead. A release is excluded from also registering as tap-to-play, same guard shape drag-reorder already needed. **Two bugs found live and fixed, 2026-09-02**: (a) the swiped row's title/duration went fully accent-coloured (i.e. vanished) partway through the swipe, apart from the timestamp -- x was shifted by the live drag offset but each `draw_text_clip()`'s own right-edge clip bound was left fixed, so the available width shrank as the row slid and collapsed to nothing well before the delete threshold; the timestamp's own right edge happened to be generous enough (`FB_W`, not the title's tighter `FB_W - 150`) that it didn't hit the same wall, which is why only it stayed visible. Fixed by shifting the right-edge bound by the same offset as x, so the row's own content reads exactly as it normally does throughout the swipe, just moved -- not the display's own colour scheme swapping in underneath it. (b) backing out of an emptied queue (every track swiped away) showed a blank screen in the current theme's plain background colour, with nothing on it and no way back -- `SC_PLAYING`'s own draw code already bails out cleanly on an empty queue, but `draw_screen()`'s unconditional top-of-frame background fill runs before that bail-out gets a chance, and go_back() from `SC_QUEUE` was sending an emptied queue to `SC_PLAYING` regardless. There is nothing to show once every track is gone (`queue_remove_display()` already calls `audio_stop()` on the way there), so `go_back()` now lands on the main menu instead when `queue_n == 0`. **Also wanted on playlists, not just the Now Playing queue** -- a playlist's own track list is `SC_TRACKS` with `browsing_is_playlist` set (same screen an album's tracks use, just a different backing list), so the grip/drag/swipe all belong there too, gated on that flag, rather than being `SC_QUEUE`-only. Unlike the queue (session-only, rebuilt from whatever's playing), a playlist's order is persisted on disk, so reordering/removing there needs to write the change back to the playlist file, not just reshuffle an in-memory array -- an extra step `SC_QUEUE`'s own version doesn't have. **Scope clarified, 2026-09-02, across every screen that reuses `SC_TRACKS`**: podcasts get it too (`SC_TRACKS` with `pod_list` set -- a feed's own episode list); audiobooks explicitly do NOT (`SC_TRACKS` with `ab_list` set -- chapters have a real, fixed order from the book itself, and reordering or removing one is nonsensical the way it isn't for a playlist or a podcast's own episode list). Plain albums (`SC_TRACKS` with none of those flags set) are also out of scope -- an album's track order is the album's, not something to edit. So: **in scope** -- `SC_QUEUE` (done, step 1; already podcast-inclusive since `queue[]` holds whatever's playing regardless of source), playlists, podcast episode lists. **Out of scope** -- audiobook chapters, plain album track lists. This is exactly why the playlist grip wasn't bolted onto `SC_TRACKS` opportunistically alongside the queue's own step 1 -- that one screen now needs a flag check (`pod_list`/`browsing_is_playlist` yes, `ab_list`/neither no) before the grip/drag/swipe apply at all, on top of not disturbing the download-button/progress-bar layouts `pod_list` rows already use on the right edge -- worth doing once, correctly, alongside the actual drag/swipe interaction rather than piecemeal. |
| R69 | **Search episode titles and show notes across subscribed podcasts** | Re-filed 2026-09-02 -- wanted independent of R65 (which has since been reverted; this doesn't depend on it or its "Add" rename). Typed on the existing T9 keyboard. Needs an actual local index: `episodes.tsv` (one per feed, written by `podsync_once.sh`) is a flat per-feed manifest with no cross-feed lookup and no full-text capability, and show notes today only exist as `.txt` sidecars next to *downloaded* episodes (`pod_load_notes()`), not for manifest-only ones -- neither is queryable as-is. Likely shape: a SQLite database (this app already vendors and links `sqlite3.c` for `library.c`/`index.c`, so no new dependency), populated by `podsync_once.sh` itself right after each feed's `episodes.tsv` is written (it already has the parsed title/notes text in hand at that point via `parse_rss.awk`, so this is a second output of an already-necessary parse, not a second fetch) or by a one-time backfill pass over existing `episodes.tsv`/`.txt` files for feeds subscribed before this ticket. Query side: `LIKE`/`MATCH` against title and notes columns, C side reusing the same subprocess-and-poll shape `pod_update_start()` established if the query itself needs shelling out, or a direct `sqlite3_*` call from the UI thread if it's cheap enough not to need one (episode notes total in the low thousands of rows even across many feeds, so probably the latter). Results list can reuse `SC_PODCASTS`'s existing row-list machinery, but tapping a result opens that episode (in its feed, at its position) rather than subscribing to anything. Explicitly deferred by the user for now -- not started. |
| R68 | **Add OTG as a third USB mode (Settings + quick settings)** | Extends the existing `SC_SETTINGS_USB` picker from two options to three: **Device / ADB**, **Device / Storage**, **OTG**. Most of the plumbing already exists -- `st_usb_mode()`/`st_usb_mode_set()` (`status.c`), the Settings row (`set_row_usb_y()`), and the quick-settings USB row (`QS_H` was already grown by one `QS_ROW_H` for it). The current code deliberately stops at two: its own comment says *"DAC and OTG need exclusive gadget ownership the same way Storage does, and aren't exposed here at all"*. **The mechanism is now known and verified on hardware (2026-09-01).** The Type-C controller driver (`tcs1421_add.ko`) exposes `/sys/devices/platform/tcs1421/tcs1421_cfg`, accepting `Sink` / `Source` / `StrongDRP` / `NormalDRP`; the module params `tcs1421_cfg0_gpio`/`tcs1421_cfg1_gpio` are only the GPIO *names* (`PA09`/`PB24`), not the mode. Writing `Source` was confirmed to put dwc2 into host role -- the gadget driver logs `dwc2_hsotg_ep_disable: called in host mode?` -- and `Sink` drives both strap pins low (hi,hi -> lo,lo). Stock default is `NormalDRP`. So: **ADB** = `/usr/bin/adbon` + `NormalDRP`; **Storage** = `/usr/bin/adboff` + `NormalDRP` (that script starts mass storage itself); **OTG** = `S440adb stop` + `usb_dev_mass_storage.sh stop /dev/mmcblk0` + `Source` (note `adboff` is wrong here -- it *starts* mass storage, which OTG must not have). `st_usb_mode()` reads live gadget-bind state from configfs rather than a cached flag, so it needs a third case: neither gadget bound **and** `tcs1421_cfg` reading `Source` = OTG. **Prerequisite for OTG to be useful**: a kernel with `CONFIG_SND_USB`/`CONFIG_SND_USB_AUDIO`. Verified live that without it a connected USB DAC visibly charges (VBUS flows, host role engages) but never enumerates, because there is no `snd-usb-audio` to bind -- it looks "not seen". Built as `USBAUDIO_1`/`USBAUDIO_2` in the kernel repo; the stock kernel's status is unconfirmed. **Design constraint, learned the hard way**: entering OTG kills adb outright, because the board stops being a USB device. The on-screen UI still works, so switching back via Settings is fine -- but this mode must **never** be applied automatically at boot before the UI is up, and ideally should not persist across a reboot at all. A boot-time OTG apply cost a full lockout during investigation: `/usr/data` survives firmware flashes by design, so a bad setting there could not be cleared by reflashing, and with adb gone there was no way in. If it is persisted, pair it with an escape hatch on the SD card (a marker file the supervisor checks) since the card is the only surface reachable without a shell. Related: RP5 (misc system settings), and `docs/08`/`patches/0006` in the kernel repo for the separate VBUS-output question. |

## Inspiration

Ideas taken from other R1 player projects. Sourced by **reading their code for technique only** -- every project below is GPLv3 and this one is MIT, so nothing here is a port: the mechanism, register, or ordering is the takeaway, the implementation is ours. Same approach already used for `open_hiby_player` (see `NOTES-standalone-player-analysis.md`) and for RP1/RP3's corroboration.

Current source: `github.com/Starnished66/R1-open-source-player` at tag `Beta-1` (commit `7d0b92d`, analysed 2026-08-27). Its own `BATTERY_REPORT.md` is worth reading before trusting any battery claim from that project -- including its release notes' "battery life is now on par with Stock", which that report itself explicitly declines to support (27-minute window, 1% gauge resolution, taken right after a large gauge recalibration, mixed-radio workload). The mechanisms below stand on their own; the headline number does not.

**Not on this list, deliberately: suspend-to-RAM.** They ship it (`power_suspend.c`) with the same `bt_suspend` -> `fb0/blank=4` -> `/sys/power/state=mem` -> unblank sequence `deep_suspend()` already independently arrived at, plus one thing we don't do (BlueZ D-Bus adapter power-off *before* the raw `bt_suspend` UART teardown, because suspending with a live A2DP link reportedly reboots the device via watchdog -- a different failure mode than the recoverable link-drop `suspend_ok()`'s own comment anticipated). That is a real hazard, but in **dormant** code: the "Idle sleep" Settings row was removed, `deep_sleep_enabled` defaults to 0, and nothing reaches `deep_suspend()` without a hand-edited `music.conf`. Suspend stays withdrawn on its own evidence -- an unattended overnight run needed a hard power cycle and measured no real improvement -- which their release does not overturn. One thing has changed since that decision: it was paused "pending source" for the SDIO teardown worry (WiFi and the SD card share that bus), and source for both reference projects now exists, so that specific question is finally checkable if suspend is ever reopened. Not a reason to reopen it.

| # | Item | Notes |
|---|---|---|
| INS1 | **Screen-off audio low-power mode (bigger decode chunk)** | Cheapest item here by a distance. They double the decode chunk from 4096 to 8192 frames whenever the screen is off (`audio.c`'s `NORMAL_CHUNK_FRAMES`/`LOW_POWER_CHUNK_FRAMES`, flipped by `audio_set_low_power_mode()`, called from the same `screen_on_now != screen_was_on` edge that already toggles their input layer). Halves wakeups per second of audio, which is a race-to-idle win rather than a throughput one -- worth having even though our own decode CPU is already low (3-13% of one core on the Bluetooth path per `audio.c`'s own `apply_decode_priority()` comment), since the saving is in how often the SoC is woken, not how much work it does once awake. Note their buffers are sized to `MAX_CHUNK_FRAMES` (the larger of the two) at allocation time so the switch is just a read-size change, never a realloc -- worth copying that shape rather than allocating per mode. Ours would hook the same place the existing lock/screen-off handling already runs. |
| INS2 | **Dim the backlight before turning it off** | They dim to 40% of the user's configured brightness after 10s of inactivity (`SCREEN_DIM_AFTER_MS`, `backlight_set_dimmed()`), separately from and well before the full screen-off timeout. Their stated reasoning is simply that the panel dominates power while lit, and 40% is "visibly dimmer without making low user settings unreadable". We have brightness control and a screen-off path but no intermediate dim tier. Two details worth taking with it: the dim level is a percentage *of the user's own setting* (not an absolute), so someone already running dark doesn't get dimmed into unreadability; and it restores the exact prior brightness rather than a fixed default. Interacts with BG95 (brightness resetting on sleep/wake) -- check that fix's own restore path before adding a second thing that writes brightness. |
| INS3 | **85% charge limiter via the AXP2101 PMIC** | Biggest genuinely-new capability here, and the one where reading their work saves the most: `charge_limiter.h` documents **three** register-level attempts, two of them dead ends we would otherwise repeat. (1) `/sys/class/power_supply/usb/charge_control_limit_max` is wired to the *wrong* register -- it lands on REG16, the input-current limit (how much the USB port may supply), not battery charge current. (2) REG62 (ICC, fast-charge current) is the right register but the wrong mechanism: it only constrains the constant-CURRENT phase, and the charger enters constant-VOLTAGE well before 85% on this battery, so zeroing it stopped charging at ~91-92%, not the configured target. (3) What actually works: `chg_en`, bit 1 of `module_en`/REG18H, i2c bus 0 address 0x34 -- disables the whole charger state machine, CC and CV alike. **Must be read-modify-write**: that same register holds `gauge_en` and `watchdog_en`. Needs a discharge hysteresis window or noisy 84/85% readings restart the charger repeatedly and overshoot. Two hard-won negative results come with it: `axp_battery/current_now` is **frozen** (a static passthrough of a configured maximum, not an ADC reading) so no software wattage measurement is possible on this device at all; and the only trustworthy live charge state is REG01 bits[2:0] straight from the PMIC -- both `battery/status` and `usb/current_now` were caught reporting stale/wrong values during their investigation. They also expose a separate 500mA safe-charging cap. Ours would need its own i2c access; nothing in this codebase talks to the PMIC today, so this is the largest of the four. |
| INS4 | **Throttle battery/status sysfs reads** | Housekeeping-grade, but free. They cache battery capacity/status behind a 5s TTL (`BATTERY_CACHE_TTL_MS`, `battery.c`) rather than reading sysfs per call, add display smoothing on top so the shown percentage doesn't jitter with the gauge (`BATTERY_DISPLAY_STABLE_MS` 15s, `BATTERY_DISPLAY_STEP_MS` 60s, plus a jump threshold above which a gap is treated as a real change rather than noise), and drop visible status polling from every 500ms tick to every 2s -- their own comment notes that last one alone "removes three quarters of those reads". Worth auditing what we poll per tick while the screen is on before copying any specific number; the principle (cache with a TTL, smooth the display separately from the read) is the transferable part. Also note their rediscovery-on-failure detail: a driver can disappear and reappear across suspend, so a failed read clears the cached sysfs device name rather than pinning a stale one forever. |

## Refactoring

Code organization, cleanup, and architectural improvements. Lower priority than new features and bugs, but valuable for maintainability.

| # | Item | Notes |
|---|---|---|
| RF1 | **Integrate podcast code into Library** | Refactor `hiby-podcast-app` to be merged into `hiby-music-app` as the Podcasts section (L5). Currently a separate binary chain-loaded by the music hook. Work involves: (1) merging podcast source files into the unified structure, (2) routing podcast UI through the main Library menu, (3) sharing the core (S1–S5) for output routing, status bar, quick-settings, (4) unified settings storage (R24), (5) proper hand-off between music and podcast playback (S8). Unblocks the full unified Library release. |

### Done since this list was made

| # | Item | Notes |
|---|---|---|
| R11 | ~~Two-level A–Z index~~ **built, needs a finger (U4)** | Slide left off the strip and the letter subdivides: `B` becomes `Ba, Be, Br, Bu`. `lib_subkeys()` returns only second letters that exist, so every band leads somewhere. `lib_group_offset`/`lib_albums_offset` now take a prefix string rather than a char, which needed no new SQL — `v < 'Br'` counts what sorts before it exactly as `v < 'B'` did. Fixed bands: each letter divides the same width, so `Br` is in the same place every time. Selected band is drawn as a pill row left of the strip, so the gesture is visible rather than remembered. |
| R12 | ~~Index capture area too narrow~~ **done** | Split the drawn width from the caught width: `INDEX_W` stays 45, new `INDEX_TOUCH_W` is 72. Letters do not move; the thumb has 27 px more room to miss. The rows lose nothing, since their text is already clipped inside `INDEX_W`. |
| R13 | ~~Droplet further left~~ **done** | `FB_W - INDEX_W - 110`, was `- 78`. |
| R1 | ~~Multi-disc albums sort wrongly~~ **done** | Disc number now read from `DISCNUMBER` (FLAC), `disk` (MP4) and `TPOS` (ID3), sorting by disc then track. |
| R2 | ~~Tracks missing from an album~~ **done** | Root cause was the scanner's 256-byte path field, above. |
| R3 | ~~Output priority~~ **done, untested by ear** | 3.5 mm, then USB, then Bluetooth. |
| R5 | ~~A–Z index~~ **done** | Superseded by R12 and R13. |
| R6 | ~~Icons in quick settings~~ **done** | Bluetooth and Wi-Fi as buttons that colour when on, showing the connected device and SSID. Wi-Fi reads `wpa_cli`, not `iwconfig` — that binary is not on this firmware, which is why it used to read "not connected". |

## Parked

Not being worked on, by request. Listed so they resurface whenever the full
backlog is asked for, rather than being quietly lost.

| # | Item | Notes |
|---|---|---|
| P1 | Create playlists on the device | Needs text entry; today it is `Favourites` plus any `.m3u` dropped on the card. Either borrow the firmware's keyboard or offer a fixed set of names. |
| P2 | BBC radio | The player side is done and proven — HLS, MPEG-TS demuxing, AAC. BBC's own public HLS endpoints return 410 Gone and the remaining discovery route declares itself part of a content protection system, so this app does not go looking there. Paste a URL into `radio_stations.conf` and it plays. |
| RP4 | Tidal / Qobuz / anything backed by a HiBy account | Parked by request 2026-08-14 — not something this project cares about. Moved out of the "Replacing hiby_player" list rather than deleted, per this section's own point: proprietary integrations with their own API keys and DRM aren't rebuildable regardless, so it stays true whether or not anyone's tracking it. If `hiby_player` is ever fully replaced and these turn out to matter after all, some part of it has to stay running for just this. |
| S7 | Audiobook mod unification | Parked while three PRs to `yetisoldier/Hiby-R1-Audiobook-Mod` are out for review — no point porting S2's routing or S4's facts into a fork relationship that might still change shape. Its renderer is its own (`font.c`/`render.c`, not `text.c`), so this was never going to be a source-sharing job like S1 regardless; it would be a PR against `audiobook_app/` once the existing three land. Revisit after they're accepted. |

## Housekeeping

| # | Item | Notes |
|---|---|---|
| H1 | Strip the last of the keypress logging | Stays only until the headset's skip codes are known (U2); the output-loss and write-failure lines should stay for good, since they are how a silent failure gets diagnosed. |
| H2 | Second `DEV_HOOK` in `patch_firmware.py` | The music hook chain-loads the podcast hook from `libpodcast_hook.so.real`. That works and needs no flash, but a firmware with two preload slots would be tidier. |
| H3 | M4A seek granularity | Seeking rounds to the start of an access unit, about 23 ms at 44.1 kHz. Fine in practice; recorded so it is not rediscovered as a bug. |
| H4 | **Progressive JPEG cover art is refused, by design** | Turned out to be two of the three: Baltic and Blue Mars both carry progressive 2400×2400 covers, 4:2:0 subsampled — Y at full res plus two quarter-res chroma planes puts the real coefficient-array cost at ~16.5 MB, not the 34.5 MB a naive 3-channel-at-full-res estimate says. Either way it is well past `cover.c`'s 8 MB budget, and correctly so: this device already lost `hiby_player` to the OOM killer twice, once from exactly this. `scale_denom` cannot reduce it — the coefficient array is built at full resolution before any scaling happens. **Worked around for these three books**: re-encoded each embedded cover as baseline with `ffmpeg` (same pixels, no re-download, no quality loss — Baltic and Blue Mars checked side by side against the originals) and dropped the result in as `cover.jpg` beside the `.m4b`, which `art_for_track()`'s loose-file check already tries before touching the embedded picture, so no code changed. Not a general fix — the next progressive-covered book still needs the same by-hand step, or a real one: a decoder that can stream progressive scans (none available on-device), or a hand-rolled DC-only progressive thumbnail decoder (real Huffman/zigzag/MCU work, parsing untrusted input — a project of its own, not attempted here). |
| H6 | **The page-cache trim only covers MP4/M4B, not FLAC** | `mp4.c` drops what is behind the read cursor every ~512 access units, which is what keeps an eleven-hour audiobook from filling RAM (see the framebuffer DMA note in the audiobook commit). FLAC goes through `drflac_open_file()`, which exposes no fd, so a 372 MB hi-res FLAC still fills the page cache unchecked — the same fragmentation exposure that made `hiby_player`'s order-11 (8 MB) contiguous DMA allocation fail and bring the panel up dead. Fixing it means opening FLAC via `drflac_open` with our own read/seek callbacks so the fd is reachable. Not urgent — measured 15 MB still available while streaming the 96/24 album — but it is the same class of bug, only unfixed. |
| H7 | **On `snd_pcm_open` failure the queue is raced through, not stopped** | Seen live 2026-08-07 when the stock player still held the DAC: our app could not open ALSA, and `was_active && !audio_is_active()` fired every tick, so `play_index(cur_track + 1)` walked the entire album in seconds, logging a failure for each. It should stop and surface "no output" once, the way the output-lost path already pauses rather than skipping. |
| H8 | `hires` is inferred from the source, not from the format the device took | The worker sets `hires = dec_is_wide(d) && g_out_kind != 2` and sizes `frame_bytes` from it, but never consults what `pcm_open()` actually negotiated. If both hw: attempts *and* plughw-with-S24 failed, the last resort is plughw S16_LE and the worker would still write 4-byte samples into a 2-byte stream. Very nearly unreachable, and pre-existing, but `g_out_fmt` now records the negotiated format so `hires = (g_out_fmt != FMT_S16_LE)` would make it exact. Left alone deliberately rather than refactored blind — several call sites set `hires` and one computes it *before* the open to choose `want_fmt`. |
| H5 | Multi-file books don't expand per-file chapters | `ab_open_book()` splits a book by its embedded chapter track only when the book is a *single* file. A folder of several `.m4b` parts, each with its own chapter track, gets one chapter per file instead of the full list. Deliberate — expanding in place across files needs a second pass and every real book here is one file — but it is the obvious next thing if a part-per-file book turns up. |
| H9 | Nero-style `chpl` chapters in `.m4b` are not read | `mp4_chapters()` finds chapters exactly one way: the QuickTime text track, picked by its `hdlr` handler type being literally `text` (`mp4.c`). That is the mechanism all three books this was built and verified against use (see the audiobook-chapters commit's own on-device verification), and it is the shape ffmpeg and m4b-tool normally write. The other common convention -- Nero-style `chpl`, a flat chapter list in `moov/udta/chpl` rather than a real media track -- is not handled anywhere; nothing in `app/` references it. A file carrying *only* `chpl` opens and plays fine but reports zero chapters, so `ab_open_book()` falls through to treating the whole book as one unsplit file: no chapter list, no per-chapter bar, no chapter seeking. **Untested rather than known-broken** -- no `chpl`-only file has been tried, and many producers write both, in which case the text track is already found and nothing is missing. Cheap to settle before writing any code: dump one real `.m4b`'s atoms and see whether `chpl` is present alone or alongside a `text` track. If it does need implementing, `chpl` is a simpler parse than the text track already handled here (a version/flags byte, a count, then per entry a 64-bit start time in 100ns units plus a length-prefixed title) and would slot in behind the same `mp4_chapter_t`/`max` interface as a fallback for when the text-track pass returns 0. Related: H5 (multi-file books) and H3 (seek granularity). |

## Bugs

Issues that break or behave unexpectedly. Fix before shipping.

| # | Item | Notes |
|---|---|---|
| BG1 | ~~Brightness slider overshoot turns screen off~~ **done** | The slider starts at x=24, inside `EDGE_ZONE` (100px) — dragging it right on release satisfied the same conditions as the edge-swipe-back gesture, and `go_back()` on `SC_MENU` returning 0 exits the app outright. That exit, with whatever brightness the drag left behind, is what read as "the screen turns off." Fixed (47d8618) by excluding `qs_open` from the edge-swipe detection, the same way the scrub bar already was. Hardened further (effa921) so a release that picks up incidental vertical wobble mid-drag can't fall through to the unrelated "swiped back up" branch either — the live block already applied the value continuously; the release now does nothing when it lands mid-drag. |
| BG2 | ~~List scrolling still isn't smooth~~ **done** | Root cause wasn't redraw cost — it was that *anything* unconditional (the clock, art loading, status polling) could paint mid-drag using whatever fractional `scroll_px` happened to be at that instant, which looked exactly like continuous scrolling regardless of how rarely the drag itself raised `dirty`. Rebuilt (dfcf487 → effa921) to page a row at a time instead of sliding pixel by pixel: `scroll_to_px()` reports whether the visible row actually changed, `dirty` only fires on that tick, and rendering no longer reads `scroll_px` at all — rows draw at fixed positions with new content, so there's nothing fractional left for any other code path to paint. The "profile draw_ui() first" plan turned out to be solving the wrong layer; noted for next time something reads like a performance problem but might be a correctness one. |
| BG3 | ~~Two-level index: second level gone for some letters~~ **done** | Not the `fits` check — that was verified correct against real device logs (`jumpkey` traces) before touching anything. The actual bug: "anything that began on the strip ends on the strip" re-jumped on release using `touch_y`, which is set once at touch-down and never updated during a drag — only `live_y` tracks the live position. A subdivision selected mid-drag was correct the whole time and then reverted the instant the finger lifted, which read as "the second level is gone." Fixed in `cc8f21e`. |
| BG4 | ~~Two-level index: scrolling M→J snaps back to M~~ **done, same fix as BG3** | Same `touch_y`-vs-`live_y` bug: the live drag correctly tracked M→J, and releasing snapped back to wherever the finger originally touched down (M). `cc8f21e`. |
| BG5 | Reintroduce idle-lock, properly | Disabled entirely (`IDLE_DEFAULT_S` 120 → 0, `idle_lock_seconds = 0` pushed to `/usr/data/music.conf`) by explicit request while the crash and index work settle — it was one more variable making it hard to tell what a given report was actually describing (a real hang vs. the screen just being locked). Not abandoned: bring it back once BG3/BG4 and any further crash work are done, and reconsider the timeout value at that point rather than assuming 120s was right. |
| BG6 | Stock player blanks the panel on its own, outside this app | Confirmed from the log, not guessed: the screen goes black with no `key 116` and no `[music] locked` line anywhere before it — only the wake press afterward. That rules out both this app's own lock (now off, BG5) and an input-level screensaver (which would still log a key event when it fires). `hiby_player`'s own binary has a genuine, separate "standby" feature — the literal string `"the standby time has reached"` is baked in, distinct from `hiby_screensavers` (a visual clock/track overlay, not a blank) — but no exposed config file or `.ini` controlling its timeout was found. It may live under a "Standby" menu distinct from "Power management" (already disabled) and "Screen saver," or may not be user-configurable on this firmware at all. The wake fix (single press, real-backlight-state check) already handles it correctly regardless of cause. Worth a further look only if the blanking itself — not just recovering from it — becomes a real problem. |
| BG7 | ~~Artist name persists after backing out of album~~ **done** | Tapping an album row overwrites `cur_artist` with that specific album's own artist (`row->owner`) so the track query can disambiguate two artists sharing an album name — necessary, but nothing then knew whether `cur_artist` had been empty (browsing all Albums) or deliberately filtered (reached via Album artists → an artist) before that happened, so "back" from Tracks always requeried and redrew the header with the single album's artist stuck in place, even from the unfiltered list. Added `albums_artist`, set wherever Albums is actually *entered* as a browse target (both the direct "Albums" tap, which clears it, and an Album-artists row tap, which sets it to that filter) and restored into `cur_artist` in `go_back()`'s Tracks→Albums case before requerying. Also set at the `SC_PLAYING`→Tracks (queue-icon) transition, to the track's own artist, so a further "back" from that path doesn't inherit a stale value left over from an earlier, unrelated Albums browse. **Verified on-device, both directions**: Albums (unfiltered) → Led Zeppelin IV → back → header reads "Albums" again, not "Led Zeppelin"; separately, Album artists → Arctic Monkeys → an album → back → header still correctly reads "Arctic Monkeys" with all 4 albums, confirming the filtered path wasn't regressed. |
| BG8 | Apostrophes followed by big spaces in titles | "The Sun's gone dim and the sky's..." (track 8, Piano Works) displays with excessive spacing after the apostrophes. Likely a text-wrapping or line-breaking artifact in `split_title()` or the rendering code — the title may be breaking at the apostrophe and leaving trailing space on one line, then starting the next line with more space. Check how `split_title()` handles punctuation when wrapping to two lines. |
| BG9 | **Battery vs stock — measured, and the premise does not hold on the wired path** | Measured 2026-08-07, same file (`Abbey Road/07 Here Comes The Sun.mp3`), same route (`hw:0,0 … (exact)`), Bluetooth powered off so `bluealsa`'s SBC-XQ/LDAC encoding could not mask the difference. Our app is an LD_PRELOAD hook *inside* `hiby_player`, so both sides are the same pid and `/proc/<pid>/stat` compares like with like. **Idle floor 2%. Stock UI open, nothing playing: 2% — so stock's UI is free and its playing figure is genuinely playback. Stock playing: 24% (23% locked). Ours, same file: 9% (6% locked).** Over the floor that is ~22% for stock against ~7%/~4% for us: we are three to five times *cheaper*, not dearer. On a 16/44.1 FLAC ours is 6%/5% with decode alone at 2%. **Two honest limits.** (1) The report says *USB-C*, and adb owns the only USB port, so the USB path cannot be exercised from here — see BG12 for the specific mechanism that would make USB behave unlike the internal DAC. (2) Battery drain itself is not measurable while tethered: `POWER_SUPPLY_STATUS` reads `Full`, `CAPACITY` pins at 100 and `axp_battery/CURRENT_NOW` is a static config value, not a live sensor; CPU is the proxy. Also note the build has moved a long way since this was filed (`-O2`, the decode-priority fix). Keep open only until someone reads the log with an amp attached. |
| BG10 | Track names cut off ~1cm before right edge | Browsing albums (Piano Works is a clear example), track titles are truncated about 1cm before the screen edge, wasting ~40-50px of available space. Either the text rendering area has a fixed width that's too narrow, or there's excessive right-side padding. Check `draw_ui()` row rendering and the index strip's claimed width. |
| BG11 | ~~'In Time: The Best of R.E.M.' shows 33 tracks via Albums but none display~~ **done** | The original report had the two paths backwards: real device logs showed Artists → R.E.M. → Album returning 0 tracks, and direct Albums working. Root cause, confirmed against the on-device database: this album's `album_artist` is a single 0x00 byte on every row (not NULL, not a normal empty string), while `artist` is correctly "R.E.M." per track. `lib_tracks_for_album()` filtered on both `album LIKE ?` and `album_artist LIKE ?` in one query — no real artist name can ever LIKE-match a blank value, so the Artists path's `album_artist LIKE 'R.E.M.%'` matched nothing. The Albums-direct path only "worked" by coincidence: it leaves `cur_artist` empty, and an empty LIKE pattern degrades to `%`, which happens to match the blank field too. Fix: split query-building into `tracks_query()`, tried first with the artist filter (real disambiguation when two artists share an album name) and retried once without it if that returns literally zero rows. Verified on-device: both paths now return all 33 tracks. `TBD`. |
| BG15 | ~~Album Artists → 'Unknown' group is empty~~ **done** | Found live while verifying BG11 on-device. `lib_group()` labels the ~565 tracks with a blank `album_artist` as "Unknown" purely for display, but the tap handler in `music_hook.c` copied that literal label into `cur_artist` and passed it to `lib_albums()`, which ran `album_artist LIKE 'Unknown%'` — no row's `album_artist` is literally that text, so the group's albums list came back empty (all 51 blank-artist albums, including "In Time", were unreachable this way). Fix: added a `LIB_UNKNOWN_MARK` sentinel that `lib_group()` flags via the otherwise-unused `row->owner` field, and a shared `filter_clause()` helper (used by `lib_albums`, `lib_albums_count`, `lib_albums_offset`, `lib_letter_offsets`, `lib_prefixes`) that recognizes it and matches `(column = '' or column = char(0))` directly. The blank value is exactly one 0x00 byte — confirmed against the pulled device database — and SQLite's `length()`/`substr()` both report it as length 0 for that specific value, so a `substr(...)=char(0)` attempt matched nothing; only the direct equality does. Verified on-device: Album Artists → Unknown now lists all 51 albums, and tapping into one (Abbey Road) correctly shows all 17 tracks. `TBD`. |
| **BG12** | **Audio distortion + UI freeze via USB-C — does not reproduce on the internal DAC; one specific USB mechanism now suspected and pre-emptively fixed** | Original report: Barenboim/Batiashvili, ~3 minutes from the end, audio went "crunchy" and the on-screen controls froze while the hardware buttons still worked; the device was visibly warm. **Tested 2026-08-07 on the exact album** (`01 I. Allegro moderato.flac`, confirmed 96000 Hz / 24-bit / 19.7 min / 372 MB) — the whole album, screen locked, sampled every 5 minutes for 55 minutes, well past the point it was reported to fail. Opened `hw:0,0 96000 Hz 2 ch S24_LE (exact)`. **Result: nothing. Player CPU flat at 9% the entire run, decode 5–6% of one core, zero new underruns (5 in the log before, 5 after — and all five are "#1", i.e. one-offs from separate earlier sessions), pid unchanged so no crash. The battery thermistor *fell* from 33.0 C to 31.7 C.** There is no thermal problem and no shortage of decode headroom at 96/24 on the wired path. **So it is specific to USB, and there is one strong candidate.** `pcm_open()` only ever offered ALSA two formats, `FMT_S16_LE` and `FMT_S24_LE`. The internal CS43131 takes S24_LE — hence "exact" and cheap. A USB DAC commonly advertises **S32_LE** (or S24_3LE) and *not* S24_LE; when the exact open fails the very next candidate is `plughw:`, where libasound requantises **every sample in software**, which at 96/192 kHz on a 1 GHz in-order core is exactly the right shape: missed deadlines → the dry-buffer underruns that sound "crunchy", sustained load → warmth, and a starved UI thread beside the priority-raised (`-8`) audio thread → frozen screen while hardware keys, acted on from that thread's state, keep working. **Fixed pre-emptively in `23bca9f`**: the exact device is now tried a second time in S32_LE before the converting fallback, with the writer shifting by 0 rather than 8 for that format (dr_flac hands back left-aligned 32-bit; getting it backwards would play 48 dB quiet, not distort). Verified the internal DAC is unaffected — still S24_LE first attempt, still exact, same 5% decode. **Unproven on USB, and cannot be proven from here** (adb owns the only USB port). One session with the amp settles it: `adb shell grep -E "hw:\|plughw:\|exact\|CONVERTED\|underrun\|decode" /usr/data/music.log` — a `CONVERTED` line, or decode well above ~10%, confirms it. |
| BG13 | ~~Download progress overflows 32-bit `long` for files >21MB~~ **done** | Fixed by casting to `int64_t` before multiplication in `draw_download_marker()`: `(int64_t)bytes * 100 / total`, matching the existing safe pattern in `draw_progress_marker()`. The 32-bit signed overflow once `bytes` exceeded ~21.4MB was what produced the -25%→0%→25% sequence. `3176552`. |
| BG14 | ~~Episode list clipped to 40 entries per feed~~ **done** | Raised `MANIFEST_EPISODES` to 200 in `podsync_once.sh` and `MAX_ITEMS` to 220 in `podcast_hook.c` so the two sides don't independently re-clip. This is the documented middle ground — real but tolerable added sync time on a manual button press, rather than the minute-scale parse of a full 500-800 item feed. True full-archive browsing still needs lazy/paged loading rather than simply raising the cap further. `3176552`. |
| BG16 | ~~Dropdown doesn't drop down reliably~~ **done** | Diagnosed live with the user: added temporary logging of every touch release near the top of the screen (down_y, dy, whether quick settings opened), then had them swipe down repeatedly and read the log back. The pattern was immediate — every failed attempt landed at `down_y` 33-67, every success at 25-31. The trigger zone was `touch_y < STATUS_H` (32px), just under 4% of the screen's height, both for the live per-tick open and the release-based fallback for a quick flick delivered in one poll. Fixed by widening to a dedicated `QS_PULL_ZONE` (80px, past the worst observed miss of 67) at both trigger sites, with a guard (`!(vol_ticks > 0 && touch_y >= STATUS_H)`) so the widened zone doesn't fight the volume HUD's own drag zone (`STATUS_H..STATUS_H+VOL_H`) on the roughly 3s window after a volume key press when the two now overlap. Verified on-device by replaying the exact failing `down_y` values (45, 50, 65, 67) as scripted swipes — all now open the panel. The volume-HUD overlap guard is logic-reviewed but not separately re-verified live (couldn't get a synthetic volume-key press to register over adb on this device's key node). |
| BG17 | ~~Full brightness still turns screen off~~ **done** | Not an idle timeout or `hiby_player` standby (that line of investigation, including a live comparison against the stock "Backlight settings" screen, turned out to be a red herring from an initial misreading of the report — the user's actual observation was narrower and more literal: dragging the quick-settings slider to 100% blacks the screen out immediately, not after any delay). Root cause: `st_brightness_set()` clamped to `[1, max_brightness]`, and `max_brightness` reads 101 on this panel — but writing the literal 101 to `/sys/class/backlight/backlight_pwm0/brightness` blacks the screen instead of maxing it, almost certainly this PWM driver's duty-cycle register wrapping to 0% at its own period value rather than clamping at full-on. 100 is visually indistinguishable from 101 at this panel's brightness range, so capping the top of the range to `max - 1` costs nothing. Two other direct writes to the backlight node (`set_locked()`'s unlock restore, and the poll-based recovery when the player blanks the panel externally) bypassed `st_brightness_set()` entirely and could have replayed a cached 101 later even after the slider itself was fixed — routed both through `st_brightness_set()` too. Verified on-device: dragged the slider to the physical edge, `qs_bright` reaches 101 (slider still visually fills to 100%), but the sysfs node reads back 100, never 101. |
| BG18 | Need proper icon for volume | The volume overlay/control currently lacks a polished icon. Design and add a volume icon (speaker, slider, or similar) that matches the visual style of brightness and other quick-settings icons. *Human task.* |
| BG19 | ~~No ALAC support — Velvet Underground "Live 1969" albums fail to play~~ **done** | Was misdiagnosed as AAC — `ffprobe` on a real failing track showed `alac` at 714 kb/s, lossless; the UI's "AAC 16/44.1 kHz" footer reflects the stock scanner's generic per-container format code, not the actual codec. Wrote `alac.c`/`alac.h`, ported from Apple's own reference decoder (github.com/macosforge/alac — the codec files carry an Apache License 2.0 header, distinct from the repo's top-level APSL, consistent with this project's other permissive vendored code). `mp4.c` now recognizes `alac` sample-entry atoms (nesting a *second*, identically-named atom for the config, unlike `mp4a`/`esds` — the inner atom also carries its own 4-byte version/flags prefix before the `ALACSpecificConfig`, found by hex-dumping a real file). `audio.c` picks ALAC or AAC per `mp4_t.codec`, both filling one shared PCM buffer. **On-device symptom after first deploy**: every track failed identically (`mp4_next` returning a plausible-looking but wrong-feeling 32 bytes, `alac_decode` returning -1) — looked exactly like an MP4-parsing bug (`stsz_uniform` misread, or a `long`-width host/device split, both live suspicions). Spent a full diagnostic pass ruling both out: pushed a standalone MIPS binary (same `mp4.c`/`alac.c`, no player) directly to the device — it decoded the *identical* file perfectly, first try. That isolated the bug to how `audio.c` called the decoder, not the decoder or parser. Root cause: the shared PCM buffer (`AAC_MAX_FRAMES`, now `DEC_MAX_FRAMES`) was sized 2048 for AAC's frame length; ALAC's is 4096. `alac_decode` clamped to the caller's buffer instead of refusing outright, so it decoded a 4096-sample bitstream as if it were 2048 samples long — a sizing mismatch that surfaced as a deep Golomb-Rice bitstream error with nothing pointing back at the real cause. Fixed by widening the buffer to 4096 and making `alac_decode` refuse (return -1 immediately) if the caller's buffer can't hold a full frame, rather than silently corrupting the decode. **Verified on-device**: `1969: Velvet Underground Live With Lou Reed, Volume 1` track 1 plays cleanly end to end (`[audio] m4a ALAC 44100 Hz 2 ch, 4590 units`, no decode errors), confirmed by screenshot with the timer advancing. Host build re-confirmed byte-exact against `ffmpeg` after the fix (75,193,440 bytes identical; the ~2280-sample tail gap is `mp4.c` not implementing `elst` edit-list trimming, pre-existing and shared with AAC, not ALAC-specific — worth a future BG). **Real CPU measured, not estimated**: `/proc/<pid>/stat` before/after a clean 10-second sleep while playing gave 30 ticks at a confirmed (not assumed) 100 Hz `CLK_TCK` — 3% of one core for the whole `hiby_player` process, comfortably cheap on the 1 GHz single-core MIPS. All temporary diagnostic logging removed. |
| BG20 | ~~No headphone controls in audiobook app~~ **done** | The audiobook-aware key routing (`ab_play_chapter` on next/prev) turned out to already be correctly wired in `handle_keys()` for both the hardware-button and headset/remote sources — confirmed by reading, then by injecting a simulated remote `KEY_NEXTSONG_` directly on the wired-remote input node, which correctly advanced the chapter. The real, confirmed bug was the "Crucially" part: `button_locked` swallowed *every* key unconditionally, headset remote included, contradicting the explicit requirement that only this unit's own touchscreen and physical buttons should go dead when locked. Fixed by gating the swallow on `src == KEYS_BUTTONS`; `KEYS_REMOTE` (the wired inline remote and any AVRCP Bluetooth device) now bypasses it. **Verified on-device, decisively**: with the stronger lock engaged, a simulated hardware-button `KEY_PREVSONG_` had no effect (chapter unchanged, confirmed via screenshot after unlocking) while a simulated remote `KEY_NEXTSONG_` *did* advance the chapter (`[music] chapter 5/17 Chapter 2 Total Defence`, logged while still locked) — same lock state, opposite outcomes, exactly the intended split. |
| BG21 | ~~Audiobook button capture area misaligned with buttons~~ **done** | Found the drift: the speed-ring's draw position uses offset `mid - 96 - 70` (pulled in from an old `128` when the skip rings were brought closer together, per the draw code's own comment), but the tap handler's hit test was never updated and still centered its zone on `mid - 128 - 70` — 32px away from where the ring is actually drawn, enough for taps on the visible right half of the button to land in the skip-back zone instead. Fixed by matching the offset. **Verified on-device**: tapping the speed ring at its drawn position changed the display from "1.0×" to "1.1×" instead of skipping back. |
| BG22 | ~~PEQ profile picker shadow doesn't extend to top~~ **done** | `draw_sheet()`'s dim-behind-the-sheet loop started at `CONTENT_Y` (below the status bar and header), leaving that strip undimmed for every bottom sheet, not just the profile picker. Changed the loop to start at `0`. **Verified on-device**: opening the profile picker now dims the status bar and album art at the very top of the screen along with everything else. |
| BG23 | ~~Volume button slider changes are not smooth (jumps around)~~ **done** | Root cause in `audio_volume_step()`'s Bluetooth path: each press wrote its delta into a single pending slot (`bt_vol_pending_delta = delta`), *overwriting* rather than accumulating. The background thread only drains that slot every ~100ms plus however long the `amixer` round-trip itself takes — comfortably longer than the gap between two real button presses — so every press but the last one before a drain vanished from what actually reached the mixer, while the optimistic on-screen `g_vol` had already counted all of them. The readback that followed then snapped the display down to match what only the last step actually did — the "jumps around" symptom. Fixed by accumulating same-direction pending deltas instead of overwriting. Code-reviewed and consistent with the documented mechanism, but not exercised on real Bluetooth hardware this session — no headset was connected to reproduce the actual mixer race live; worth a live re-check next time one's paired. |
| BG24 | ~~Audiobook skip buttons (-10s / +10s) have gaps in the circles~~ **done** | `draw_skip_arc()` stamps the ring as a series of overlapping radius-2 dots along the arc. `fill_circle`'s integer rounding (`(int)(sqrt(r²-dy²)+0.5)`) makes a radius-2 dot's outermost row vanish entirely — `sqrt(4-4)+0.5` rounds down to a width of 0 — so each dot's real solid extent was smaller than its nominal radius suggested, and at the old 40-step spacing (~3.6px between centers) that could fall short of covering the gap between dots, especially where the arc's direction was steepest. Thickened the dots to radius 3 (only the single outermost row vanishes, not three) and increased the step count to 64 (~2.6px spacing), giving comfortable overlap even in the worst case. **Verified on-device**: a 4×-zoomed crop of the rendered ring shows a fully solid stroke with no notches anywhere around it. |
| BG25 | ~~Cover art gives up after one candidate~~ **done, not yet verified on-device** | Two real bugs, not one. First: only conventional filenames (`cover.jpg`/`folder.jpg`/etc.) were tried, so an album with a single differently-named JPEG (e.g. `Aequora.jpg`) showed a blank panel until the file was renamed by hand — not a reasonable thing to ask of a library. `art_for_track()` is now `art_candidate()`, an ordered iterator: conventional names first, then any other `.jpg`/`.jpeg` in the folder (case-insensitive extension match, AppleDouble `._` sidecars skipped, sorted for a stable order), then the embedded picture. Second, and the reason a simple reorder was not enough: the old code stopped at the first *existing* file, but a file existing is not the same as art that *decodes* — `cover.c` deliberately declines large progressive JPEGs and over-size images to avoid the OOM kill it documents, and a PNG saved as `.jpg` fails outright. Either used to produce a blank panel even when good embedded art sat a candidate further down. The worker (`music_hook.c`) now walks the iterator until `cover_load()` actually returns bitmap data. Verified with a native host build against six synthetic album folders (conventional name, odd-name-only, several images together, no images, dotfile sidecars, mixed-case extension) — all six selected correctly. Not yet exercised on the device itself. |
| BG27 | ~~**UI locks up with the screen and backlight still on**~~ **closed, not a code bug** | Closed 2026-08-15 at the reporter's direction: caused by their own setup during testing, not by the app. Original investigation notes dropped rather than kept as a live lead. Note for anyone who sees something similar: a *frozen last frame with adb still alive* is a different and known thing -- the graphics DMA leak on repeated `killall hiby_player` restarts during a dev loop, which a reboot clears. |
| BG26 | Battery percentage inaccurate after restart from double-lock | After the device has been in double-lock mode for several hours and then restarts, the battery percentage shown in the app is incorrect (e.g., shows 60% when actual is 85%) until the user navigates into the HiBy stock UI. The app is likely not refreshing the battery state on startup or after sleep. Check: (1) whether the battery sysfs node is being read on app start, (2) whether there's cached state from before the lock that isn't being invalidated, (3) what the stock UI does that refreshes the value. |
| BG28 | ~~**Lots of tracks don't appear in library**~~ **done** | The report's premise was wrong in a useful way: the stock scanner had *not* indexed these files — it cannot. Measured the whole card against the index (4839 real audio files vs 4842 rows): exactly **15 files missing, every one of them over the scanner's 255-byte path field**, and nothing missing for any other reason. Two albums, both entirely invisible because their *folder* prefix alone ate the budget — 246 bytes for the Petrenko Beethoven 9 (which repeats "Berliner Philharmoniker, Kirill Petrenko" three times) and 251 for the Rautavaara, leaving 9 and 4 bytes for a filename. With zero indexed tracks there is no seed path, so `sweep_album_folder()` could never recover them either. Fixed by renaming, per the documented approach: dropped the redundant repeated artist from both album folders, and stripped the 88-character artist prefix each Rautavaara *filename* also repeated (keeping the leading track number). Worst-case stored paths went 374→241 and 392→210 bytes. Reversible mapping written to the card at `/usr/data/mnt/sd_0/bg28_rename_plan.tsv`. **Rescan trigger, answering an R10 open question:** a reboot does *not* rescan (index byte-identical at 4842 rows across one), and the card cannot be unmounted while the player holds it. What works is the stock **Music → gear → Update database**, which took ~4.5 minutes and took the index to 4857 — exactly +15. Re-measured afterwards: **0 files missing.** Follow-up filed as BG40 for the durable fix. |
| BG29 | **Lots of artwork doesn't appear** | Example: Substance 1987 by New Order shows blank cover panel. Cover may exist in the album folder or embedded in the file. Check: (1) whether `cover_load()` is failing on certain file formats or image types, (2) whether `art_candidate()` is missing valid candidate paths, (3) whether the fallback chain (embedded → folder → blank) is working correctly. Related to BG25 (fallback logic). |
| BG30 | ~~**Wrong artist name in now playing**~~ **to be tested by human** | Two real, separate bugs here, both fixed now. (1) The Now Playing/mini-player artist display preferred `q_artist` (the album's artist, snapshotted at play time) over `t->artist` (the track's own tag) whenever `q_artist` was non-empty — wrong for any album where they differ, e.g. a compilation. Flipped the priority: the track's own artist now wins whenever it has one. (2) **The actual reported bug** — confirmed live: choosing a different album left the *previous* album's artist showing indefinitely on the Now Playing screen. Root cause: the `SC_ALBUMS` row-tap handler in `music_hook.c` only updated `cur_artist` from `row->owner` *if it was non-empty* (`if (row->owner[0]) ...`) — so an album with no `album_artist` tag at all (common; see the BG11 comment in `library.c`, which already documents this exact fragility) left `cur_artist` holding whatever a previous, unrelated album or artist browse had set. Made the assignment unconditional; `row->owner` empty now correctly clears `cur_artist` to match, and `lib_tracks_for_album()` already retries without an artist filter when one finds nothing, so this doesn't break album disambiguation. Not verified on-device — needs a human to reproduce the original repro (switch between a couple of albums, including at least one with no album_artist tag, and confirm the Now Playing artist always matches what's actually playing). |
| BG31 | **Artist name appears at top of album menu** | Example: Lisa Batiashvelli appears as a header when browsing albums, the same way BG7 did. Should show album title or facet, not artist. See screenshot. This may be related to the `cur_artist` persistence issue fixed in BG7. |
| BG32 | ~~**Loud high-pitched tone in left ear on Bluetooth**~~ **closed -- hardware fault in the user's Sony WH-1000XM4s, not this app** | Recurred four times live across one session despite two separate code fixes (an EQ filter-state safety net, then tightening its threshold), each deployed and verified running, neither stopping it. Eventually ruled out everything software could explain: happened with EQ and MSEB both off (so no code in `eq.c` was executing at all), across different decoders/formats (FLAC, MP3, M4A) and both music and an audiobook, growing gradually from inaudible to full rather than switching on -- a shape no dropout, scheduling stall or codec-selection bug produces, since none of those integrate. Two real, defensible code changes came out of the investigation anyway and are kept (see the `audio.c` commits): the decode thread no longer runs at raised scheduling priority over Bluetooth's own LDAC encoder process, and an unverified `start_threshold` tweak was reverted back to the prior known behavior. Neither is claimed to have fixed this. Closed on the user's own determination after their headset's own pause behavior (audio continuing briefly after pause, consistent with something draining on the bluealsa/LDAC side rather than in this app's decode path) and pattern across sessions pointed at the headset hardware rather than the software. Per-channel peak/clip diagnostics added during this investigation (`eq_stats_t`) are left in place -- harmless, EQ-gated logging -- should a similar report recur while the EQ is actually active. |
| BG33 | **WiFi glyph in dropdown is mis-shaped** | The Wi-Fi icon in the quick-settings dropdown does not render correctly. Add signal strength indicator (0–4 bars or similar) to show connection quality alongside the current connected SSID. |
| BG34 | ~~**No battery symbol in now playing**~~ **to be tested by human** | Both Now Playing route lines (regular player and audiobook) showed "route · 85%" as plain text with no icon. Added the same `draw_battery()` glyph the status bar already uses, in the same text-then-icon order, right of the percentage. |
| BG35 | ~~**No battery percentage for Bluetooth device in dropdown**~~ **already done, entry stale** | Already implemented -- the Bluetooth quick-settings row (`draw_quick_settings()`) shows codec, a battery icon and the percentage next to the device name, sourced from `bt_batt_cached`/`bt_codec_cached` (read via XAPL on a background thread). The comment there even says it moved from the Now Playing route line to here. No code change made; backlog just never got updated when this landed. Human check: confirm it actually renders correctly with a real headset connected. |
| BG36 | ~~**24/192 WAV stutters over Bluetooth**~~ **to be tested by human** | Same root cause as BG41, fixed by the same change: hi-res audio (88.2/96/176.4/192 kHz) opened straight over Bluetooth made bluealsa's own PCM plugin resample it in software at the full source rate, in-thread, which is exactly hypothesis (1)/(2) here. `audio.c`'s `bt_target_rate()` now caps what's requested (192/96 -> 48 kHz, 176.4/88.2 -> 44.1 kHz) and the worker decimates the samples to match before they ever reach ALSA, so the resample bluealsa still does is working from a quarter (or half) as much data. Bit-perfect Bluetooth was never the goal -- no codec on this device carries more than 96 kHz of real information regardless. Needs the same live check as BG41: play a 24/192 file over Bluetooth and confirm the stutter is gone. |
| BG37 | ~~**List scroll position not restored when going back**~~ **case 2 fixed, case 1 unconfirmed -- to be tested by human** | Two cases reported. **(2) Albums list -> Album tracks -> back: genuinely never worked, not a regression.** Checked git history (`git log -S"albums_scroll"`) -- no such mechanism has ever existed. BG37's original fix only ever covered SC_ARTISTS -> SC_ALBUMS (`artists_scroll_saved`/`_px_saved`); the `default:` case in `go_back()` handling SC_TRACKS -> SC_ALBUMS has done an unconditional `reset_scroll()` since the earliest commit touching that line. Fixed the same way as the original: `albums_scroll_saved`/`_px_saved`, saved at the SC_ALBUMS row tap (before entering Tracks), restored in `go_back()`'s `default:` case with the same total-clamp BG37 already uses. Verified live (David Bowie's *"Heroes"*, `Albums -> tap -> Tracks -> back`): correct data, no crash, no blank list -- but only at the degenerate scroll=0 case, see below. **(1) Artists/facets -> Album -> back: could not confirm a regression.** Re-read `artists_scroll_saved`'s full save/restore path line by line -- unchanged from what was verified working earlier this session, and `reset_scroll()` itself doesn't touch it. Live-tested (Alice Sara Ott -> back -> Artists): correct, no crash. **Neither case could be tested at an actually-scrolled position**: synthetic touch (drag swipes, single taps on the A-Z index strip) does not register scroll on this device -- confirmed repeatedly this session, matches the project's own earlier finding (U4/R9: an injected gesture arrives entirely within one ~33ms poll, so the app never sees it as a real drag). Everything above is either static analysis or a scroll=0 round-trip, which cannot by itself distinguish "restored correctly" from "reset to top by coincidence." **Needs a human with a real finger**: scroll down meaningfully in Artists (or Album artists/Genres), tap an artist, back out, confirm the position; then scroll down in Albums, tap an album, tap a track, back out twice, confirm both restores. If case (1) still reproduces with a real scroll, the mechanism itself needs a second look -- code review alone did not find how, but it wasn't ruled out either, only unconfirmed. |
| BG38 | ~~**EQ settings don't survive reboot**~~ **to be tested by human** | Confirmed: `save_conf()`/`load_conf()` had no EQ keys at all, so nothing ever wrote which profile was active or whether EQ was on. Added `eq_on` and `eq_profile_path` to `music.conf`, applied at startup right after `load_conf()` reads them, and saved immediately at every mutation site (both enable/disable toggles, the profile picker, and the first-visit auto-pick). `eq_switch_to()` already tolerates a stale/missing path gracefully (loads an empty profile rather than crashing), so a deleted profile or a card swap degrades safely. **Part 2, fuzzy-matching a profile to whichever headphone connects**: `bt_poll()` (the existing background thread that already reads codec/battery so the UI thread never shells out) now also reads the device name and, on a genuinely new connection, case-insensitively substring-matches it against each `EQProfiles/*.txt` name in either direction -- "Sony WH-1000XM4" the device matches a profile saved as "WH-1000XM4" or vice versa. A minimum-length guard keeps a short/generic profile name from matching almost anything by accident. The match itself only ever hands a winning path across through the existing `bt_lock`, the same handoff shape already used for pending Bluetooth volume -- `eq_switch_to()`/`save_conf()` still only ever run on the main thread, since `eq_cur`/`eq_cur_path` have no lock protecting them and calling those from the background thread would have been a new, real data race. Re-matches once per new connection, not continuously, so a manual override during the same connection isn't clobbered on the next ~5s poll cycle. **Bug caught live and fixed**: the name-check was gated behind `audio_using_bt()`, which reflects whether audio is *actively routing* over Bluetooth right now, not whether a device is connected -- reconnecting the Sonys with nothing actively playing through them yet never triggered a re-match, so it silently stayed on whatever profile (Jabra, in the repro) had been manually picked in between. Moved the name-check out from behind that gate; `st_bt_name()` already answers "is something connected" on its own via the same `bluealsa-cli list-pcms` mechanism `bt_sink_connected()` uses for routing, so it now runs every ~5s cycle regardless of playback state. Needs a live re-test: disconnect the Sonys, pick the Jabra profile manually, reconnect the Sonys -- confirm it now switches back on its own. |
| BG39 | **Bluetooth volume control is unreliable and jerky** | Volume up/down button presses via Bluetooth produce unpredictable results: sometimes pressing down jumps the volume up instead, changes are inconsistent (unpredictable % increments), and the on-screen animation jumps around erratically. Related to BG23 (which fixed volume slider accumulation) but this is specifically about hardware button presses over Bluetooth. Check: (1) whether the button press → mixer write → readback cycle is racing, (2) whether the display is updating from stale readback values, (3) whether `audio_volume_step()` is correctly handling Bluetooth deltas. |
| BG40 | ~~**Player controls spacing is uneven**~~ **done** | Moved the transport row (play/pause, previous, next) on the regular Now Playing screen down from `by + 58` to `by + 70`, confirmed live. Found and fixed a real bug along the way: `bar_y()` (used by the seek-bar tap zone and by `draw_scrub_strip()`, the lightweight redraw used while actively dragging) had its own separate formula, 14px above what the main draw path actually used for the bar's resting position -- meaning the bar visually jumped 14px the instant you touched it to scrub, and jumped back on release, and the tap zones for both the seek bar and the transport buttons were biased 14px high all along (masked by generous tolerance bands, not by being correct). `bar_y()` is now the one shared value the main draw path, the scrub-strip redraw, and the tap handler all call, so it cannot drift again. |
| BG41 | ~~**Intermittent audio hiss over Bluetooth during playback**~~ **to be tested by human** | Reported live: on Piano Works (192 kHz FLAC) over the WH-1000XM4s with PEQ on, quality degraded partway through (sounded like a codec step-down to a lower bitrate) and stayed degraded until manually rewinding. The `music.log` on-device (`/usr/data/music.log`) shows why: `pcm_open()` was requesting Bluetooth's PCM device at the *source's* native rate every time, and bluealsa's own PCM plugin silently resamples whatever it's given down to what the a2dp link actually carries -- in this thread, since it's a linked-in ALSA I/O plugin, not a separate process. `[audio] decode X% of one core` climbed to ~69-70% on that combination (decode + PEQ's own cascade, both still running at the full 192 kHz, plus the resample) versus the ~16-17% the EQ was benchmarked at for CD-quality audio -- enough to produce the underrun also visible in the log, which is exactly the kind of hiccup a headset's adaptive bitrate reacts to. Root cause matches this project's own long-standing suspicion in BG36 almost exactly, just triggered by PEQ pushing it over the edge rather than decode alone. Fixed in `audio.c`: `pcm_open()` now caps the rate it requests for Bluetooth specifically (`bt_target_rate()` -- 192/96 kHz to 48, 176.4/88.2 kHz to 44.1, everything else untouched) and reports what it actually negotiated via a new `g_out_rate`; the worker loop folds that cap into the decoded samples itself via plain box-car decimation *before* EQ sees them, so EQ's own cost drops by the same factor instead of staying pinned to the source rate. Restricted to clean 2x/4x source-rate multiples specifically so this is exact integer decimation with no fractional resampler and no state carried across chunks. Position/duration tracking (`done`/`g_pos_ms`) stays keyed to the un-decimated source frame count so it isn't affected. **Not yet verified on real hardware**: I don't have a way to judge audio quality by ear, so this needs a live listen-test on Piano Works over the WH-1000XM4s with PEQ on -- watch `/usr/data/music.log` for the `decode X%` figure dropping substantially at the same time, and listen for whether the box-car's mild (non-brick-wall) anti-aliasing is actually inaudible in practice, not just reasoned to be. |
| BG42 | **No fallback for albums the stock scanner cannot index** | Renumbered from a duplicate BG40 -- two unrelated entries had ended up sharing that number; this one (split out of BG28) predates "player controls spacing," which is referenced elsewhere as BG40 and kept that number. Split out of BG28, which was fixed by renaming files rather than by code. The scanner's path field is 255 bytes and anything longer is silently absent from the index — and when *every* track of an album is over the limit there is no seed path, so `sweep_album_folder()` (which recovers individual missed files within an otherwise-indexed album) never runs. Such albums are completely invisible with no indication anything is missing. Renaming works but is manual, needs a host, and has to be redone whenever new files land. Proposed fix: a folder-walk fallback that finds directories containing audio files with zero corresponding index rows and surfaces them as albums. Measured as cheap — only ~470 directories at depth ≤3 on a 176 GB card, ~5 s including adb overhead, and a native on-device walk would be faster. The real cost is not the walk but the plumbing: album lists are SQL-backed with paging, counts and an A–Z index, so injecting non-SQL albums touches `lib_albums`, `lib_albums_count`, `lib_albums_offset`, `lib_letter_offsets` and `lib_prefixes`. Consider caching the walk result rather than repeating it per list. A cheaper interim option: detect the condition and warn, rather than fully integrating the results. |
| BG43 | ~~**Albums list goes blank after Playing -> back -> back**~~ **to be tested by human** | Reported live: Artists -> Alice Sara Ott -> Piano Works -> tap a track -> back -> back landed on a blank Albums list instead of Alice Sara Ott's albums. Root cause found in `/usr/data/music.log`: `index_prepare()` was scoping the A-Z strip query to `cur_artist = "Alice Sara Ott (piano)"` -- the *album_artist* column's value -- while the Albums list itself is grouped by the *artist* column, whose value for this same album is the plain "Alice Sara Ott". `albums_artist` (which the `SC_TRACKS -> SC_ALBUMS` back-case restores `cur_artist` from) is supposed to hold the artist-facet's own group name, but BG7's queue-restore fix in the `SC_PLAYING` back-case unconditionally overwrote it with `q_artist` (also the album_artist column) even when Tracks was reached via a completely normal Albums browse, where `albums_artist` already held the right value. Added `played_from_browse`, set when Tracks is reached via a normal track tap and cleared at the mini-player's direct jump into Playing (BG7's actual scenario -- no Albums browse to fall back on) -- only the latter now lets `q_artist` overwrite `albums_artist`. |
| BG44 | ~~**Increasing playback speed on audiobooks stutters**~~ **fixed and verified live** | Two causes, both fixed. **(1)** Every tap of the speed control ran a full `wsola_init()`, zeroing the input ring, overlap accumulator and `prev_tail` reference, even though only `Ha` depends on speed (`N`/`Hs`/`Lr`/`delta`/`win[]` depend only on rate). Added `wsola_set_speed()`, which updates `Ha` in place with no flush; `audio.c` now only does the full re-init when rate or channels actually change. Removes the ~40ms re-prime gap `wsola.h` documented per tap. **(2) The critical one.** `wsola_step()`'s onset search was exhaustive: 2*delta+1 = 1323 candidates per step, each correlated over Lr = 882 samples with *three* 64-bit accumulators, at 50 steps/sec -- **175 million MACs and 66,150 double sqrt+divides per second**, on a 1 GHz in-order MIPS with no hardware FP. Measured live at **93% of one core** with a continuous underrun storm (~10 buffer-empties/sec, thousands per session) on both Bluetooth and the 3.5mm jack, so it was never Bluetooth-specific. Three reductions: the reference energy `eb` is invariant across candidates and was hoisted out of the loop (a third of the inner cost, zero semantic change); the correlation is decimated 4:1 (`CORR_DECIM`, safe because it locates pitch-period alignment and a 80-400 Hz fundamental is 110-550 samples at this rate, so the surface is smooth over a 4-sample step and all three sums scale together); and the candidate scan is coarse-to-fine (`CORR_COARSE`, then a full-resolution refine around the winner -- the one genuine approximation, same trade SoundTouch's "quick seek" makes). **23.5x fewer MACs, 3.9x fewer sqrt.** Verified before deploying with a host-side A/B harness against the old exhaustive search (identical output lengths, RMS within 0.05%, identical max-discontinuity, pitch preserved at 220 Hz across 0.8x-2.0x) and ~13x faster on the host. **On-device after the fix: decode 93% -> 17% at 1.2x, 35% at 1.4x, and zero underruns across a full session including live speed changes.** |
| BG45 | ~~**Podcast episodes appear in Music browsing**~~ **done, verified against the index** | The stock scanner indexes `/Podcasts` along with everything else, so every feed showed up in Library's own Music section as an "album". Confirmed against a copy of the live database: 43 episodes, and **8 distinct `album` values, not the 5 first noted** -- Norsken svensken och dansken (7), Språket (4), The Bugle (5), The Incomparable Mothership (5), The Weekly Show with Jon Stewart (4), Vulcan Hello (6), Zwischen Hamburg und Haiti (1), plus 11 episodes whose album is the literal string `Unknown`. Fixed by excluding the path prefix from every browsing query in `library.c`. Two findings while doing it. First, the spread is wider than this entry assumed: `lib_group_count`, `lib_group_offset` and `lib_albums_offset` also query MEDIA_TABLE, and the file's own comment warns the counts must agree with the lists or an A-Z jump lands in the wrong place -- so they needed the filter too, not just the five originally listed. Second, the trailing LIKE wildcard is also printf's conversion character and nearly every query here is built with `snprintf`, so the predicate needs two spellings (`PODCAST_EXCL_SQL` / `_FMT`); the first build caught this as `-Wformat` errors and would otherwise have emitted nonsense SQL. Verified on the real index: albums 293 -> 286, album artists 123 -> 122. The arithmetic is 7 not 8 because `Unknown` is also the album of one real music track, so it correctly survives -- the fix removes no actual music. Not yet run on the device. |
| BG46 | ~~**Podcast cover art stretched**~~ **stretch fixed and verified; per-episode art is a separate, bigger, unstarted feature -- see BG51** | Two real root causes found, one fixed here. **(1) Stretch, done**: `cover_load()` (`cover.c`) box-filtered the source directly to `px*px`, mapping source width and height independently -- a non-uniform stretch whenever the source wasn't square. Album/book covers are conventionally square so this never showed; podcast artwork routinely isn't. Fixed with a center-crop to the smaller dimension before the box-filter runs (skips the top margin's rows rather than seeking, since scanlines can't be read backward; the side margin folds into the column mapping at no extra cost). Verified on-device against Språket's real cover -- proportions read correctly now, no squash. **(2) Embedded-art cache collapse, also fixed, but turned out not to be what was reported**: `art_candidate()`'s cache key (`art.c`) was the containing folder for every slot including slot 0 (the embedded picture) -- correct for an album's shared folder image, wrong for any per-file embedded art (a podcast feed's episodes routinely differ; so can a compilation's per-track art). Fixed by keying slot 0 on the track path instead. **But checked directly on-device (`adb shell ls .../Språket/`) and there is no per-episode image file anywhere in that feed's folder, only one shared `cover.jpg`** -- the episode tested has no embedded ID3 picture either (art_candidate's slot 0 falls through to that same folder cover for it). So what the user actually meant by "Språket has per-episode cover art" is almost certainly the RSS feed's own per-item `<itunes:image>` tag, which this app never fetches at all -- a sync-script feature, not an app-side cache bug. Split out as BG51. |
| BG47 | ~~**No skip or speed control on the podcast player**~~ **done, verified live** | Was reusing the plain-track transport's dead prev/next triangles (a podcast episode is a one-entry queue, see `podcast_mode`'s comment, so they always no-op). Added, in `music_hook.c`: (1) the same -30/-10/+10/+30 skip set the standalone podcast app used (`podcast_hook.c`'s real layout, checked directly rather than guessed -- "10s for missing a sentence, 30s for skipping an ad"), drawn with this app's own `draw_skip_arc()` (already used by the audiobook screen) rather than that app's rectangular buttons; (2) a speed control ported from the audiobook screen's own ring control and cycling behaviour, backed by a new `pod_speed_permille` kept separate from `ab_speed_permille` on purpose. `POD_SKIP_OFF1`/`OFF2`/`pod_speed_x()` are shared between the draw code and the tap handler so the hit zones can't drift from what's drawn, same convention `bar_y()` established. **Verified on-device**: speed ring tap moved 1.0x -> 1.1x, +30 arc moved the clock forward and the seek bar with it. |
| BG48 | ~~**Podcast bitrate missing next to the format label on first play**~~ **done, verified live** | `ab_file_bitrate_kbps(t->path, t->dur_ms)` used `t->dur_ms`, which only ever comes from a saved resume record -- zero for any episode never played before, i.e. every episode the first time, so the bitrate calc silently had nothing to divide by. Same screen already computes a live `dur` (from `audio_dur_ms()`, falling back to `t->dur_ms`) for the position bar a few lines above; switched the bitrate calc to read that instead. Resolved together with BG49 once BG49's real cause (below) was found and fixed -- both were downstream of the same always-zero duration. **Verified on-device**: "MP3 128 kbps" now shows on first play, no prior listen needed. |
| BG49 | ~~**Podcast seek bar isn't filled and doesn't respond to taps**~~ **done, verified live** | Root cause found, and it wasn't specific to podcasts: MP3 duration on this app was never decoder-reported at all -- `dec_open()` deliberately leaves the frame count at 0 for MP3 rather than scan a VBR file whole (see its own comment) -- a *library* MP3's duration instead comes from `audio_probe_dur_ms(path, bitrate_bps)`, and that function's MP3 branch can't estimate anything without a bitrate, which for a library track comes from the SQL index's own ID3 read at scan time. A podcast episode was never scanned, so there was no bitrate anywhere to hand it. Fixed in `podcast.c`: a new `pod_mp3_kbps()` reads the *first* MPEG frame header directly (skipping any ID3v2 tag first, since a podcast MP3's tag is often sizeable with embedded cover art and scanning through it risked a spurious sync-word match) to get a real bitrate straight from the file -- same "no full decode" trade `audio_probe_dur_ms()`'s own database-bitrate estimate already makes. `pod_probe_dur()` calls it once per episode in `pod_load_episodes()`, caching the result in `pod_eps[i].dur_ms` the same way a resumed episode's saved duration already was. Also fixed a second instance of the identical bug found while in this code: `pod_save_current_pos()` used `audio_dur_ms()` directly (always 0) to decide whether to mark an episode `POD_FINISHED`, so that branch could never fire -- given the same `dur<=0` fallback the draw/seek code already used. **Verified on-device**: the bar fills orange proportional to position, the remaining-time clock shows a real countdown instead of "-0:00", and tapping the bar seeks. |
| BG50 | ~~**Podcast show-notes text too small**~~ **done, verified live** | `pod_draw_notes()`'s text size and line height were 20% too small per the request -- bumped `TEXT_PX_SMALL` (22px) to 26px and the 30px line height to 36px to match. Verified on-device with a real notes body (Swedish diacritics included) -- reads clearly, wraps correctly, no overlap. |
| BG51 | **Fetch and show per-episode podcast art from the feed's own `<itunes:image>` tag** | Split out of BG46 once that ticket's actual cause was checked on-device and ruled out: there is no per-episode image file anywhere in a synced feed's folder today, so what BG46 originally described ("Språket has per-episode cover art") can only be the RSS feed's own per-item `<itunes:image href="...">`, which nothing in this pipeline currently fetches -- not this app, and not `.podsync/podsync_once.sh`, the sync script that populates each feed's folder. This is sync-script-and-beyond work, not the app-side art-cache bug BG46 turned out to be: `podsync_once.sh` would need to pull each item's image URL out of the feed XML alongside the audio enclosure it already fetches, save it as a per-episode sidecar (e.g. the episode's basename plus an image extension, matching the `.txt` show-notes sidecar convention `pod_load_notes()` already reads), and `podcast.c`/`pod_load_episodes()` would need to look for that sidecar and prefer it over the feed's shared `cover.jpg` when present -- `art_candidate()`'s existing slot-1+ folder-image fallback in `art.c` already handles "prefer a specific file, fall back to the folder" for regular albums and could plausibly be extended rather than duplicated. Not started. |
| BG52 | **Blindverkostung has no cover art -- its "cover.jpg" is actually a PNG** | Found live: `file` on the pulled `/data/mnt/sd_0/Podcasts/Blindverkostung - Das heitere Interpretenraten/cover.jpg` reports `PNG image data, 1400 x 1400, 8-bit/color RGBA` -- whatever fetched it (`.podsync/podsync_once.sh`) saved the server's actual bytes under a `.jpg` name regardless of real content type, and this app's cover pipeline (`cover.c`) only ever dlopens `libjpeg.so.9` -- there is no PNG decoder anywhere in the binary, and no system `libpng` exists on-device to dlopen instead (checked: `find /usr/lib /lib -iname '*png*'` is empty). `x_read_header()` on non-JPEG bytes fails and `cover_load()` returns NULL, same as any other undecodable file, which is why the episode shows no art at all rather than a wrong one -- consistent with `art_candidate()`'s existing behaviour, not a new bug in that logic. The episode's own MP3s (checked: no `APIC` string in the first 200KB of one) carry no embedded picture either, so there's no fallback available through the existing pipeline. Two ways to actually fix it: (1) a minimal PNG decoder (would need to vendor an inflate/zlib implementation plus PNG chunk parsing -- real new scope, comparable to `alac.c`'s addition), or (2) fix it at the source -- have `podsync_once.sh` sniff/convert non-JPEG cover downloads to JPEG before saving, which would also stop future feeds from hitting the same wall. Likely affects other feeds too, not just this one -- worth a quick scan of every synced feed's `cover.jpg` for its real file type before deciding which fix to do. Not started. |
| BG54 | ~~**podcast_mode stuck true forever after playing one episode -- regular tracks showed the podcast transport**~~ **done, verified live** | Found live testing seek: played a Beatles MP3 after playing a podcast episode, and Now Playing showed the podcast screen's -10/+30 skip arcs, speed ring and info icon instead of the regular prev/next transport, with "Språket" (the last-played feed) still showing as the subtitle line under "The Beatles." Root cause: `play_index()`, `play_station()` and `ab_play_chapter()` each clear `radio_mode`/`audiobook_mode` when starting their own kind of playback, matching the established one-mode-at-a-time convention, but none of them cleared `podcast_mode` -- so once set by `pod_play_episode()`, nothing ever turned it back off. Added `podcast_mode = 0` to all three. Verified on-device: after playing a podcast episode then a regular Beatles track, Now Playing correctly shows the plain prev/next transport, "1 of 13", and full format details (`MP3 16/44.1 kHz 320 kbps`), no podcast UI or stale feed name. |
| BG53 | **Backward MP3 seeks are slow enough to look broken** | Reported as "the bar jumps and then returns to its original position." Measured directly on-device with sequential screenshots after a single tap: the position display does **not** revert -- it freezes at the pre-seek value for several seconds while the seek is in progress, then jumps straight to the correct target and continues forward normally from there (three screenshots after one backward tap-seek: frozen at the old position, then landed exactly on the expected target, then +2s further on from there -- no bounce back). Root cause: MP3 has no seek table built (`dec_open()`'s own comment explains why -- `drmp3_init_file()` deliberately skips the frame-count/index pass that would need scanning the whole file up front), so `drmp3_seek_to_pcm_frame()` falls through to its brute-force path (`dr_mp3.h`), which for a *backward* seek rewinds to the start of the stream and decodes forward from zero to reach the target -- real decode work proportional to how far into the file the target is, run synchronously inside `audio_seek_ms()`'s caller, the single audio worker thread, which is why the position clock and playback both stall for the duration rather than just the seek being merely inaccurate. A seek near the end of a long episode/track pays for decoding almost the entire file. Not a correctness bug -- the eventual position is right -- but the multi-second stall with zero UI feedback reads exactly like "broken" to anyone tapping and expecting an instant jump, especially the first time (no indication a seek is even in progress). Two independent angles worth pursuing: (1) a real fix -- build a lightweight seek table once per file open (dr_mp3 exposes `drmp3_calculate_seek_points`) so subsequent seeks become O(1) lookups instead of a full re-decode, at the cost of an upfront scan when a track/episode first opens; (2) a UI-only mitigation regardless -- show some "seeking..." affordance the moment a seek is issued rather than leaving the position frozen with no explanation, so the multi-second wait doesn't read as unresponsive even before the real fix lands. Not started. |
| BG55 | **Frame around cover art in miniplayer** | The cover art in the miniplayer has a visible frame/border that shouldn't be there. Affects all file types (music, audiobooks, podcasts). Check: (1) whether `draw_cover()` or similar is adding a stroke/border on the miniplayer rendering path, (2) whether a clipping or antialiasing boundary is creating the appearance of a frame, (3) whether this is a miniplayer-specific rendering or shared with Now Playing. |
| BG56 | ~~**Audiobook progress bar shows book progress instead of chapter progress in miniplayer**~~ **done, build clean, not yet verified on-device** | Confirmed: the mini player's R32 position bar reads `audio_dur_ms()`/`audio_pos_ms()` directly, both file-scoped -- for a multi-chapter book that's the whole file (often the whole book), not the chapter actually playing. The full Now Playing screen already computes this correctly (`ab_chapter_bar_y()`'s block: chapter duration from `ab_book.chap[i].dur_ms`, position as `audio_pos_ms() - chap->file_start_ms`) -- the mini player's bar just never got the same treatment. Applied the identical chapter-relative math there, gated on `audiobook_mode`; non-audiobook playback is unchanged. Device was unreachable to verify live at time of fix. |
| BG57 | ~~**Tapping currently-playing audiobook reopens the book instead of showing Now Playing**~~ **done, build clean, not yet verified on-device** | Confirmed: the `SC_AUDIOBOOKS` row tap unconditionally called `ab_load_book()` (rescans chapters) and forced a real file reopen (`ab_playing[0]=''`, `queue_n=0`) plus `ab_resume_book()` (reseeks to the last *saved* position) regardless of whether the tapped book was already the one playing -- discarding live mid-book progress in favor of whatever was last saved. Fixed by comparing the tapped `ab_book_t.dir` against `ab_book.dir` (the currently-open book's own folder, already tracked) when `audiobook_mode` is set: a match jumps straight to `SC_PLAYING` with no reload at all, matching the mini player's own tap-to-open behaviour; anything else keeps the original open/resume path unchanged. |
| BG58 | **Audiobook chapter skips last 7 minutes at 1.2x speed** | Reported live: Baltic's second-to-last chapter (epilogue) skipped the final ~7 minutes when playing at 1.2x speed. Did not occur at 1.0x speed. **Investigated without reproducing -- no code-level cause found, needs a live listen with `/usr/data/music.log` open, not another guess.** Checked and ruled out, by reading rather than assuming: (1) `ab_follow()` (the chapter-boundary display logic) compares `audio_pos_ms()` against `ab_book.chap[].file_start_ms`, both fixed content-time values with no speed term anywhere in the comparison -- it only ever updates which chapter number is *displayed*, never seeks or skips audio itself. (2) `done`/`g_pos_ms` (`audio.c`) are incremented from `got_src`, the raw decoded-source-frame count returned by `dec_read()` *before* WSOLA ever sees the chunk -- position tracking is provably decode-side and speed-independent, not derived from WSOLA's stretched output. (3) The gapless file-rollover path (`audio_take_advance()`/`adv`) only fires on a genuine end-of-file from the decoder, unrelated to wall-clock or speed. (4) `wsola_drain()` is called in a `for(;;)` loop until it reports nothing left, every chunk -- no fixed-size truncation that could silently drop buffered output. (5) `Ha` (the one thing a speed change touches) is computed as an integer division (`Hs * speed/1000`), which does introduce a small per-step rounding bias, but that's a fraction of a percent, nowhere near large enough to account for losing ~12% of a chapter. None of these explain a 7-minute loss, and the bug needs actual audio -- something I can't verify without hearing it. Next step if picked up again: reproduce with `music.log` open and check for an underrun storm or a `decode X%` spike in the same window BG44 originally diagnosed by, near the point the audio actually cuts, since a real decode-side stall (not a tracking-math bug) is the one mechanism not yet ruled out here. **Re-tested live 2026-08-19 on Baltic's actual penultimate chapter at 1.2x: did not reproduce.** Left open rather than closed -- one clean run doesn't rule out whatever the original report hit (could be state-dependent, e.g. only after a long session, a specific chapter/file boundary, or a speed change made mid-chapter rather than set from the start). |
| BG63 | ~~**Settings screen layout and styling issues**~~ **done, verified live** | Three fixes, refined after live feedback caught two follow-on issues: (1) Toggle switches (Power button lock, Bypass DSP on USB) and value text (Auto shutdown, Accent colour) now center on the *full* divider-bound block for that setting -- including its two-line description below the title -- via `draw_toggle_switch_h(fb, y, on, block_h)` and an explicit `block_h/2 - TEXT_PX_SMALL/2` offset, not just the 72px title slice (a first pass centered only on `ROW_H`, which still read as sitting too high against the whole card). (2) Auto shutdown's time value drawn via a new `draw_right_col()` (a `draw_right()` variant taking an explicit colour) in `COL_ACCENT` instead of the fixed `COL_DIM`. (3) Accent colour's value text renders directly in the accent colour it names, via the same `draw_right_col()` -- an earlier attempt added a separate colour-swatch circle next to plain text instead, which wasn't what was wanted. |
| BG60 | **Large progressive audiobook covers (2400x2400+) still show no art** | Found live fixing Norse Mythology's missing cover: its embedded MP4 cover is a 2400x2400 *progressive* JPEG, needing ~33 MB for its coefficient array to decode -- well over even BG46's raised 14 MB budget (deliberately kept conservative: this device has a real prior OOM-kill history from a 3000x3000/~54 MB progressive cover, and a naive further raise risks the same crash on any card with less free RAM than this session happened to have). What actually fixed Norse Mythology was manual, off-device: extract the embedded cover, re-encode it as a *baseline* (non-progressive) JPEG with `ffmpeg` on a host machine, drop it in as the book's folder `cover.jpg` -- baseline JPEGs stream scanline-by-scanline and never hit the progressive memory guard regardless of size. This app has no JPEG *encoder*, only a decoder (`cover.c` dlopens `libjpeg.so.9` for decode only), so it can't do that re-encode itself on-device -- every other audiobook with a similarly large progressive cover (plausible: 2400x2400 looks like it might just be Libation's/Audible's normal export size, not a one-off) will hit the identical wall. No on-device fix is obviously available without either raising the memory budget further (real crash risk, see above) or adding a JPEG encoder (real new scope, comparable to BG52's PNG-decoder gap). Worth checking whether other Libation-sourced books share this cover size before deciding whether it's common enough to solve properly. |
| BG59 | **Bluetooth headphone volume buttons didn't adjust volume in the audiobook player** | Reported live, then corrected: not audiobook-specific and not silent -- "the slider popup appears but the volume doesn't adjust," meaning a key event clearly *was* arriving (the on-screen percentage moved), the actual output level just never changed. That pointed away from `handle_keys()` (ruled out: no mode/screen gating on the volume keys there) and toward the two-stage Bluetooth volume path -- `audio_volume_step()` optimistically bumps `g_vol` immediately, then the real mixer write happens later, on the background `bt_poll` thread, via `audio_bt_volume_service()`. **Likely root cause found and fixed**: `worker()`'s per-chunk gain calculation (`audio.c`) unconditionally set `vol = 100` (i.e. "skip software scaling, the hardware mixer handles it") for *any* Bluetooth connection, without checking whether `find_bt_mixer()` had actually found a real AVRCP mixer control (`bt_mixer[0]`) -- some headsets never negotiate AVRCP absolute volume at all, in which case `bt_mixer` stays permanently empty and `audio_bt_volume_service()`'s write is a silent no-op forever (confirmed by reading it: `if (!bt_mixer[0]) return;`). In that case volume control did *nothing* by design: not the mixer (never found), and not software gain either (skipped because Bluetooth is assumed to always have one) -- exactly the reported symptom. Fixed by only forcing `vol=100` when `bt_mixer[0]` is actually non-empty; otherwise falls back to the same software gain scaling the wired path already uses, so volume control keeps working either way. Also added a log line for the previously-silent "no AVRCP mixer found at all" case, so this doesn't have to be re-diagnosed blind next time. Build clean, not yet verified against a real Bluetooth headset (need one connected to confirm audible volume actually changes now). |
| BG61 | ~~**Tapping a row in Albums/Artists sometimes opened the row below**~~ **done, verified live** | BG2 made list rows page instead of slide, so a row's drawn position never reflects the in-progress drag amount (`scroll_px`), only the whole-row `scroll` count. The tap handler was never updated to match -- it still added `scroll_px` when turning a tap's y-coordinate into a row index, so with a fractional drag still pending, a tap could resolve one row lower than what was actually drawn under the finger. Removed the stray `+ scroll_px` from both the main row-tap handler and the press-and-hold handler. |
| BG62 | ~~**Wrong Spotify cover art fetched for an ambiguously-titled album**~~ **done, verified live** | R23's Spotify fallback trusted free-text search's top result unconditionally, which is fine for ordinary titles but picked the wrong release for a live/reissue with a title that doesn't match cleanly. Added a word-overlap check (`titles_resemble()`) that requires most of the searched album's significant words to appear in a candidate's name, walking up to 5 results instead of trusting the first one. |
| BG64 | ~~**Only 3 of 37 tracks show for an album with mixed `album_artist` tags**~~ **done, verified live** | Arnold Schönberg box set: 34 tracks tagged `album_artist="Berliner Philharmoniker, Kirill Petrenko"`, 3 (the Violin Concerto, featuring a soloist) tagged with the longer `"...Kirill Petrenko, Patricia Kopatchinskaja"`. The Albums list's `max(album_artist)` picks the longer string; the track query's one-directional `LIKE 'that-value%'` then matched only the 3 rows that start with it. Fixed with a two-directional match, correctly handling the stock DB's own trailing-embedded-NUL-per-string quirk (`substr(album_artist, 1, length(album_artist)-1)`, not `rtrim`/`char(0)` -- both silently no-op here, since this build's `char(0)` reports `length()` 0). |
| BG65 | ~~**Fast taps during/right after a list fling could still select the wrong row**~~ **done, verified live** | BG61 fixed a tap arriving mid-coast; a narrower race remained where `inertia_active` clears (velocity decays under its arming threshold) in the same tick a fast tap lands, before the list is genuinely still. Added `list_settled_ticks`, requiring ~100ms of no coasting before a tap is trusted as a selection. Keyed on `inertia_active` alone, not `list_dragging` -- an earlier attempt kept it on `list_dragging` too, which goes true on *any* touch-down including an ordinary tap, and swallowed every tap in Albums/Artists outright. |
| BG66 | ~~**Lists still didn't reliably tell a tap from a swipe on a static list**~~ **done, verified live** | The tap/swipe classifier was pure distance (18px at any point during the touch, checked continuously from touch-down), with no time component -- a fast, decisive tap with a little sensor jitter easily exceeded 18px and read as a scroll, and a slow deliberate small drag could stay under it and read as a tap. Added a time-aware threshold: a touch still within ~150ms of going down gets a more generous 30px allowance before being called "moved" at all; past that window, the original 18px still applies. Matches the standard mobile disambiguation (a scroll is sustained motion, a tap is quick), rather than distance alone. |
| BG67 | **Bluetooth volume reverts to an earlier level on every track/album change** | Diagnosed with temporary logging (`alog` at every `g_vol` touch point) rather than guessing: `g_vol` itself was never wrong -- it correctly held the user's last-set level (e.g. 93%) straight through a track change. The revert was downstream, in bluealsa: `pcm_open()` opens a brand new bluealsa PCM connection for every track, and bluealsa resets its own mixer to a default level on each new connection, independent of the AVRCP-adjacent control this app tracks in `bt_mixer`. `audio_bt_volume_service()` always reads that mixer back and trusts it (correct on its own -- the headset's own hardware buttons move it without an event ever reaching this app), so the first readback after a track change silently overwrote `g_vol` with bluealsa's reset default. Fixed by re-pushing the last-known volume onto the fresh stream's mixer in `worker()` right after `pcm_open()` succeeds, before that first readback can happen -- closing the gap where the level briefly belonged to bluealsa's default rather than the user's choice. Build clean, pushed for live verification. |
| BG68 | **Bluetooth volume jumps down mid-ramp when pressing volume up repeatedly** | Reported as "feels like two volume controls" -- diagnosed with the same temporary-logging approach as BG67. `g_vol` accumulated presses correctly and cleanly (five presses -> a clean 100), but what actually reached the mixer was a single *relative* amixer step summing all the accumulated deltas (`"35%+"`), computed against bluealsa's own current raw value on whatever internal curve it uses -- not against `g_vol`'s linear accumulation. Confirmed live: that one batched `35%+` step read back at 65% right after `g_vol` had reached 100, an outright drop after raising the volume. BG23's original accumulate-instead-of-drop reasoning was sound, but applying the *sum* as a relative step was the actual bug -- doing the same seven presses one at a time (each its own small relative step) would not have hit this, only batching multiple into one did. Fixed by having `audio_volume_step()` push `g_vol`'s own already-correct absolute value as the mixer target instead of a relative delta, removing `bt_vol_pending_is_delta`/`bt_vol_pending_delta` entirely (every write, from the slider or the buttons, is an absolute set now) -- keeps BG23's coalescing of quick presses into one write, but the real mixer can no longer diverge from what's on screen. Build clean, pushed for live verification. |
| BG69 | ~~**USB mode and Power button sliders drawn over top bar while scrolling in Settings**~~ **done, build clean, pushed for live verification** | The initial Settings-scroll-clipping pass converted every `draw_text`/`fill_rect` call to a `_clip` variant bounded at `CONTENT_Y`, but the two toggle switches (Power button lock, Bypass DSP on USB) were only guarded with `if (ry + lock_h > CONTENT_Y) draw_toggle_switch_h(...)` -- a row-height check that skips the *whole* draw call once fully out of bounds, but doesn't handle a row straddling the boundary, since `draw_toggle_switch_h()`'s actual pixels are centered within the row (`top = y + block_h/2 - h/2`), not flush with the row's nominal top -- so a toggle could still paint above `CONTENT_Y` while its row was only half-scrolled past it. Fixed with three new clipped primitives (`fill_circle_clip`, `fill_pill_clip`, `draw_toggle_switch_h_clip`), used unconditionally in place of the guarded unclipped call. Build clean, pushed for live verification. |
| BG70 | **Miniplayer cover art updates when browsing other albums** | Confirmed root cause: R46's album-detail screen showed the *browsed* album's cover by calling `art_request()` -- the exact function the mini-player and Now Playing already trusted for the *playing* track's own cover, since `art_bits` is one shared buffer. Opening any album other than what's actually playing overwrote it immediately, so the mini-player showed the wrong cover the entire time that screen stayed open, not just after leaving it -- a go_back()-time restore (R46's first attempt at this) only patched the "after" half. Fixed properly: a completely independent `view_art_bits`/`view_art_request()`/`view_art_worker()`/`view_blit_art_clip()`, used only by the album-detail screen's own cover, with its own redraw trigger (`view_art_seq()`) -- the two states can no longer share a buffer, so there's nothing left to restore on the way out (that hack was removed). Build clean, pushed for live verification. |
| BG71 | **Queue button in miniplayer navigates to album instead of queue** | Confirmed root cause: the queue button (and swipe-back from Now Playing) both went through `go_back()`'s `SC_PLAYING` case, which copies `queue[]` into `tracks[]` and lands on `SC_TRACKS` -- a reasonable "doubles as the queue" shortcut before R46 turned that screen into a rich cover/bio album page, and before shuffle existed to make the two genuinely diverge. Fixed with a new dedicated `SC_QUEUE` screen (plain list, no cover) for the queue button specifically: rows come from `queue_display_index()`, which walks `shuffle_order[]` when shuffle's on rather than plain array order, so it's the *actual* upcoming play order, not just the album's own track order. podcast_mode's queue button is untouched -- its "queue" is genuinely just the feed's episode list, which the old path already showed correctly. Swipe-back from Now Playing still lands on the album page as before (that gesture means "back to browsing", not "show the queue"), but a live sub-bug surfaced while fixing this: that path never called `view_art_request()` (BG70's new, separate album-detail cover loader), so it showed whatever album had last been *browsed* rather than the one actually playing -- fixed by requesting the right cover explicitly on arrival, the same way tapping an album row in Albums already does. Build clean, pushed for live verification. |
| BG72 | ~~**Backing out to Albums after playing from the Queue shows the queued track's own artist instead of "Albums"**~~ **done, build clean, pushed for live verification** | Found live testing BG71: playing "Oasis" from the new `SC_QUEUE` screen, then backing all the way out to Albums, showed "Oasis" as Albums' own title/filter instead of the plain Albums list. Root cause: BG71's `SC_QUEUE` tap-to-play handler never set `played_from_browse = 1`, so `go_back()`'s pre-existing BG7 logic (`if (!played_from_browse) snprintf(albums_artist, ..., q_artist)`) treated it like a genuine artist-scoped playback start and overwrote `albums_artist` with the queued track's own artist -- every other "start playback outside a real Albums browse" call site already sets this flag, `SC_QUEUE`'s was just new and missed it. Fixed by setting `played_from_browse = 1` alongside the existing `play_index(qidx)` call. Build clean, pushed for live verification. |
| BG73 | ~~**'Play Next' queue behavior broken: album tracks contaminate queue, wrong album shown, backing out shows wrong state**~~ **done, verified live** | Root cause: `queue_insert()` (the one function behind both "Play next" and "Add to queue") only ever set the queue's own recorded identity (`q_artist`/`q_album`) from a cold start (`queue_n == 0`) -- queuing a track from a *different* album while something was already playing left the old album's remaining tracks sitting in the queue behind it, and q_artist/q_album stuck on the *original* album forever after, which is what made the album-detail view (reached by swiping back from Now Playing) show stale, wrong context -- exactly the "queue and album aren't cleanly separated" the report itself diagnosed. Fixed with a dedicated `queue_play_next()` (BG73's exact requirement, distinct from plain `queue_insert()`'s append/splice behaviour which "Add to queue" keeps): drops everything queued after the current track and refreshes q_artist/q_album/a new `q_is_playlist` flag to the played-next track's own album -- *unless* the queue's origin is a playlist (`browsing_is_playlist`, set wherever SC_TRACKS is entered from SC_PLAYLISTS vs SC_ALBUMS, captured into `q_is_playlist` at queue-creation time), in which case the rest of the playlist is left alone to resume after it, matching the requirement's explicit playlist exception. Also closed a narrower gap: `browsing_is_playlist` could go stale if a playlist was merely browsed (not played) then the user swiped back to view the actually-playing queue -- fixed by resyncing it from `q_is_playlist` at that exact point (go_back()'s SC_PLAYING case), since that view *is* unambiguously the queue. **Verified live**: played Oasis "Hello", browsed to Led Zeppelin IV while it played, "Play next"-ed "Black Dog" -- Now Playing immediately showed "[Led Zeppelin IV]" and "1 of 2" (Oasis's other 11 tracks correctly dropped from the queue), and advancing into Black Dog then swiping back to view the album correctly showed Led Zeppelin IV, "2 tracks", 8:20 total. One follow-on bug surfaced and fixed in the same pass: the album-detail view's cover art came from `tracks[0]`, which used to be safe (a queue was always one album, so any entry's path resolved to the same cover) but now can be a leftover from an album the queue has already moved past -- switched to `tracks[cur_track]`, the entry actually playing. **Second follow-up, reported live after the above**: swiping back from Now Playing after a Play Next still presented the (now correctly-labelled) queue as if it were a single album page -- one title, one cover -- which is inherently misleading once that queue's first row can be from an album the header no longer names. Added `queue_mixed`, set the moment `queue_play_next()` drops an album's leftovers for a played-next track from elsewhere (never set for a playlist's own queue, which was already an arbitrary mix by nature), cleared on every fresh `play_from_list()`. go_back()'s `SC_PLAYING` case now checks it first and routes to the plain `SC_QUEUE` list (no title, no cover) instead of the rich album page whenever it's set. **Verified live**: after the same Oasis -> Play Next Led Zeppelin scenario, swiping back from Now Playing now lands on a plain "Queue" screen listing Hello/Black Dog, not a fake album page; the ordinary (non-play-next) rich album page is untouched, since it's the same `play_from_list()` path already covered by BG69-BG72's live verification. Playlist-resume behaviour was implemented per spec but not separately live-tested this pass. |
| BG74 | **Bluetooth speaker controls don't work** — narrowed 2026-08-27, two concrete hypotheses, not yet tested | Transport controls (play/pause, next, previous) from Bluetooth speakers/remotes are not recognized. **Key constraint, established before any further guessing: headset AVRCP controls demonstrably work.** That rules out the whole class of "the mechanism is missing" explanations -- BlueZ's `input` plugin creates its virtual control device through `/dev/uinput`, so `CONFIG_INPUT_UINPUT` is plainly present in the stock kernel and the plugin is loaded and functioning. (Raised and discarded in the same pass: a custom-kernel investigation flagged missing `INPUT_MISC`/`INPUT_UINPUT` as a silent cause of exactly this symptom -- true in general, disproved here by headphones working. Recorded so it isn't re-proposed.) It also isn't a stale ticket predating the feature: AVRCP input support landed 2026-07-31 (`f27983a`), BG74 was filed 2026-08-21. **So the question is specifically why a speaker differs from a headset**, and `scan_inputs()` suggests two candidates. (1) **Name matching.** We only ever open an input node whose name contains the literal substring `AVRCP` (`music_hook.c`, the `strstr(name, "AVRCP")` test against `/proc/bus/input/devices`). That is what BlueZ names the node for an AVRCP *Controller* peer, which is what headphones are. A speaker or remote that instead presents as **Bluetooth HID** (BG74's own original wording says "HID control commands") gets a node named after the device itself, with no `AVRCP` in it -- never opened, so its buttons do nothing while audio works normally. (2) **Unhandled keycodes.** We cover 163 `NEXTSONG`, 164 `PLAYPAUSE`, 165 `PREVSONG`, 119 `PAUSE`, 200 `PLAYCD`, 201 `PAUSECD`, 207 `PLAY` -- but not 166 `KEY_STOPCD`, plausible on a speaker's transport row, and nothing guarantees a speaker picks the same subset a headset does. **Both are settled in about a minute with the device and a paired speaker, no code changes:** `cat /proc/bus/input/devices` and see whether a new node appears at all and what it is called; if one does, read raw events off it while pressing the speaker's buttons and see which codes arrive. That distinguishes (1) from (2) outright -- and if *no* node appears for the speaker, both are wrong and the peer implements neither AVRCP CT nor HID, which would make this not-fixable rather than unfixed. Do that before writing code. Related to R39 (USB-C controls). |
| BG75 | ~~**Album artwork doesn't scroll off when going back from Now Playing — broken for short albums (2–3 tracks)**~~ **done, build clean, pending re-verification** | Root cause: the "jump past the artwork" call (`scroll_to_px(ART_PX)`, R46's own follow-up, in `go_back()`'s `SC_PLAYING` case) trusted its own comment that this "clamps against a short album's real content" -- it doesn't. Inside `scroll_to_px()`, anything past `tracks_max_px()` takes the rubber-band branch (damped to a third of the overshoot), correct for a live drag fighting to overscroll but wrong for a one-shot jump: it landed on neither a clean top-of-tracklist nor the true content end. Reproduced live on a real 3-track album (Bartók's Violin Concerto No. 2 -- header alone is 622px, only 216px of track rows beneath it, `tracks_max_px()` is 114 against an ART_PX of 480). A first attempt clamped the target to `tracks_max_px()` (no gap below the last track, but the cover stayed mostly uncleared) -- confirmed live as still wrong. Asked directly which side of the trade-off was wanted: **always land with the title at the top, gap below the last track and all**. Fixed by bypassing `scroll_to_px()` entirely for this one call (`scroll = ART_PX / ROW_H; scroll_px = ART_PX % ROW_H;`) -- no clamping, no rubber-band, since this is a one-shot placement, not a drag with something to damp against. Build clean, pushed. Re-verification against the same Bartók album pending -- interrupted mid-navigation by the pop-glitch report below (BG76), which is a related but distinct bug on the very same gesture. |
| BG76 | ~~**Swiping back makes the album artwork "pop down" weirdly before the screen changes**~~ **done, verified live** | Root cause: `edge_active` (the flag marking a touch as a recognised left-edge back-swipe) only latches once horizontal travel exceeds 4px -- for a touch that started inside `EDGE_ZONE`, the first few ticks before that threshold trips were still wide open to `list_dragging`, which reads whatever incidental vertical wobble a real swipe has as a genuine scroll position update. The album-detail screen's "continuous" redraw (R46, so the cover slides smoothly with a drag) then painted that transient, wrong scroll position on screen for a frame or two before `edge_active` latched -- the pop. Fixed by holding `list_dragging` off for exactly as long as the gesture is still ambiguous: started in the edge zone, not yet recognised as an edge-swipe, and horizontal travel so far hasn't fallen behind vertical. Confirmed fixed live. |
| BG77 | ~~**Clearing the queue, then swiping back, shows a mangled "album" page missing every track Clear dropped**~~ **done, build clean, pending live verification** | Reported live testing R52. `queue_mixed` (BG73's flag distinguishing "this queue is still one clean album" from "it isn't, present it as a queue not an album page") was only ever set by Play Next's truncation -- Clear (R52) and plain "Add to queue" both left it false, so backing out of Now Playing after either one still ran the rich single-album-page path in `go_back()`'s `SC_PLAYING` case, showing the real album's title/cover over whatever tracks actually remained in `queue[]` rather than the real full album. Centralized the fix instead of patching each call site separately: `queue_insert()` (the one function both Play Next and Add to Queue funnel through) now sets `queue_mixed = 1` itself whenever it touches a non-playlist queue, and R52's Clear handler does the same when it truncates one. `queue_play_next()`'s own separate `queue_mixed = 1` (redundant now that `queue_insert()`, which it always calls, sets it centrally) was removed. **Verified live** for the Add-to-Queue half specifically: played Oasis, "Add to queue"-d Led Zeppelin's "Black Dog" from a different album, confirmed "1 of 13" on Now Playing, and swiping back correctly landed on the plain Queue screen ("13 tracks · 55:10 total", Black Dog present at the end) instead of a fake album page. The Clear-then-back half uses the identical code path and mechanism, verified working for the same root cause, but wasn't separately re-tested against a live Clear tap this pass. |
| BG78 | **Brightness doesn't survive screen lock** | The screen brightness setting is not preserved when the device locks and then unlocks. Either the brightness reverts to a default/previous level, or the setting is lost entirely. Check: (1) whether brightness state is saved to persistent storage (likely `music.conf` or `settings.txt` per R40), (2) whether it's restored on lock/unlock transitions, (3) whether the lock timeout or deep-suspend logic clears the in-memory brightness value without restoring from disk. Related to BG12 (thermal throttling) and lock/wake state management. |
| BG79 | ~~**Volume in audiobook doesn't survive a pause**~~ **done, build clean, pending live verification (device disconnected)** | Root cause: BG67's original fix for the same underlying issue (bluealsa resets its own mixer to a default level on every *new* PCM connection, independent of `bt_mixer`/`g_vol`, and `audio_bt_volume_service()` always trusts a mixer readback -- correct on its own, since a headset's own hardware buttons move it without ever generating an event this app sees) was applied at only one of `worker()`'s five `pcm_open()` call sites in `audio.c` -- the initial per-track open. It never covered the pause/resume paths, and audiobooks are exactly where those get exercised: `PCM_IDLE_CLOSE_TICKS` releases the actual output device after a period paused (to save power -- see that code's own comment on a book left paused overnight flattening the battery in ~9 hours), so resuming reopens PCM fresh, a brand new Bluetooth connection, and the volume-revert bug all over again. Music rarely surfaces this since tracks change often enough that the one covered call site usually catches it; a paused audiobook can sit for hours. Fixed by extracting the re-push logic into a shared `bt_repush_volume()` helper (checks `g_out_kind == 2`, re-pushes `g_vol` via `bt_vol_pending`/`bt_vol_pending_abs` under `bt_vol_lock`) and calling it after every Bluetooth-reachable `pcm_open()` success: the original site (replacing its inline block), the "output came back while paused" reconnect (~worker() line 1510), the actual pause/resume-after-idle-close path (~line 1562 -- the direct cause of this ticket), and the mid-file format-change reopen (~line 1744, not gated by output kind so it can also happen over Bluetooth). The USB-only output-changed reopen (~line 1592, guarded `g_out_kind != 2`) was left untouched. Build clean in both repos. Not yet live-verified -- needs a Bluetooth audiobook paused long enough to trigger the idle PCM close, then resumed, to confirm volume holds. |
| BG96 | ~~**Large album loads take 17s+ (182-track box set)**~~ **done, verified live (0.43) -- real fix, not just a cold-cache wait** | Reported live ("17s is an enormous regression"). Ruled out first: database location -- confirmed `scanned_media.db`/`music_index.db`/the original stock db are all on `/usr/data` (`ubifs`, internal flash), same as always, nothing moved to the SD card. Actual cause, confirmed by direct query: **0 of 182 tracks in this album had an entry in `index.c`'s own per-track cache** (`track_index` in `music_index.db`), so every browse fell through to the slow, uncached path (`tag_read()` + `audio_probe_dur_ms()`, opening every file fresh) -- not a warm-up delay, a cache that could **never** warm up. Root cause: `index.c`'s scan pass read its file list from `STOCK_DB_PATH` unconditionally -- the same frozen-forever database `library.c`'s `lib_open()` was fixed to stop preferring under RP1/RP6 (this file's own BG entry), but `index.c`'s scan side was never updated to match. A file `scanner.c` discovers that the frozen stock table never knew about (this box set, added to the card after `hiby_player` stopped scanning) was invisible to `index.c`'s own scan forever, no matter how many times "Scan library" ran. Fixed with the identical preference order `lib_open()` already uses: `scanner.c`'s own database first if it has rows, `STOCK_DB_PATH` only as a fallback before a device's first scan. **Verified live**: first pass after the fix wrote all 4748 tracks (previously wrote 0 every pass); direct query confirmed all 182 of the box set's tracks now cached. |
| BG97 | ~~**Skip-back/skip-forward icons in Podcasts/Audiobooks**~~ **done, verified live (0.43)** | Requested: replace the app's own hand-drawn "dots along an arc" skip-arrow approximation (`draw_skip_arc()`) with real arrow-rotate icons, and add the missing "s" suffix ("10s"/"30s", was bare "10"/"30"). Vendored the user's own Font Awesome `arrow-rotate-left`/`arrow-rotate-right` SVGs through the existing `gen_icons.py` pipeline (`icon_skip_back`/`icon_skip_forward`, 56x56, grouped as a family for consistent sizing since they're mirror images). Replaced all four `draw_skip_arc()` call sites (audiobook +/-10s, podcast -10s/+30s) with `draw_icon()`, then removed the now-dead function entirely. Text size followed up live ("make the 10s a bit smaller"): `TEXT_PX_BODY` (30px) -> `TEXT_PX_SMALL` (22px) for the overlaid seconds label, better proportioned inside the 56px icon. **Verified live** on the audiobook player: clean, recognizable rotate-arrow icons with correctly-sized "10s" centered inside; podcast player shares the identical code path (same icons, same `draw_icon()` calls) so expected identical, not separately re-screenshotted after the text-size follow-up. |
| BG95 | ~~Switching from an audiobook to a podcast episode (or generally between the two) can silently stop playback~~ **done, fixed and verified live** | Reported live: resumed an audiobook (Norse Mythology), then navigated to Podcasts and started an episode -- it looked like playback silently stopped/paused itself with no user action. Root cause traced to `audio.c`: a podcast episode with a real saved resume position seeks on open via `pod_play_episode()`, and without a bound MP3 seek table yet, `dr_mp3` falls back to a brute-force full decode of every frame from the start of the file to the target -- for a resume point well into a long episode, minutes of silent decode with the transport showing "playing" the whole time, which is exactly the "stopped without me doing anything" symptom. A first fix attempt (re-init `d->mp3` on a manually-seeked `FILE*`) turned out broken -- `drmp3_init()` always resets to byte 0 for its own ID3/APE tag scan, silently discarding the seek -- replaced with a synthetic single-point `drmp3_seek_point` bound via the public `drmp3_bind_seek_table()`, landing the seek in one step with nothing left to brute-force. Separately fixed the freeze this same bug caused when switching tracks *during* the stall: `audio_stop()` blocks the UI thread in `pthread_join()` waiting for the old worker, which was stuck inside that one uninterruptible brute-force call. **Verified live** with precise timing: a resume seek lands and plays at a clean, sustained 1.0x real-time rate from the first sample, across two real device sessions. |
| BG103 | ~~**Long-press on a multi-disc track opens the sheet for the wrong track**~~ **fixed, verified live 2026-09-03** | Reported live on "Substance 1987" (New Order): "hold down on Perfect Kiss" opened the Play Next/Add to Queue sheet for a different track a couple of rows off -- plain tap-to-play on the same album was correct, only the press-and-hold sheet picked the wrong one. Root cause: R56 replaced the old flat `(content_y - header_h) / ROW_H` row math with `track_index_at()` specifically so disc banners are accounted for, and updated the tap-to-play handler to use it -- but never updated the *separate* press-and-hold handler a few hundred lines away, which still did the exact old flat division by hand. Tap-to-play and long-press-for-sheet were two independent copies of "which row is this y in" arithmetic that only one of them kept in sync with disc banners -- exactly the class of bug `track_index_at()`'s own comment warns about (BG2/BG61), just in a second call site R56 didn't know needed the same fix. Confirmed both R56 (never previously verified live at all) and this hold-menu path were broken for a genuine multi-disc album, not just BG86's playlist case. Fixed by routing the press-and-hold handler through `track_index_at()` too, so there is only one place this arithmetic exists for `SC_TRACKS`. Investigation note: initially suspected a tagging issue (checked 4, then all 24 tracks' DISCNUMBER/TRACKNUMBER -- all correct) before the user redirected to "it's a UI issue," which led straight to this. |
| BG104 | ~~**Continuous visible flicker on one specific album's page**~~ **worked around and fixed at the source, 2026-09-03** | Reported live on "Everything Must Go" (Manic Street Preachers): "continuously light flickering" on the album-detail screen, persisting at higher brightness. Investigation ruled out several candidates before finding the real one: four `fbgrab` screenshots taken ~0.3s apart while the page sat open came back byte-identical (no framebuffer content is actually changing, ruling out a redraw/animation bug); the backlight's PWM frequency reads 30kHz (`/sys/module/pwm_backlight/parameters/pwm_freq`), far above what a human eye can perceive regardless of brightness, ruling out simple PWM-visibility theories; this session's own R70 playlist changes to this exact screen's draw code were re-audited and confirmed a provable no-op for a plain (non-playlist) album. A same-session unrelated finding -- this album's auto-fetched `cover.jpg` was itself a photo of the physical jewel case, not a cropped front cover, traced to a bad Last.fm/Spotify match -- briefly looked like it might explain the flicker too (a specific-color-values panel dithering theory), but replacing it with the real cover art (1415x1405, from the MusicBrainz Cover Art Archive) did **not** fix the flicker, ruling that out too; nor did brightness, nor did comparing against another mostly-white cover ("The Holy Bible - 10th Anniversary") that never flickers. What actually fixed it, confirmed live: manually downscaling that same replacement cover to 500x500 before pushing it. Root cause not pinned to an exact line -- `cover_load()`'s box-filter downsampler in `cover.c` is the prime suspect (it has a real prior bug in this area, BG46's upscaling row-averaging fix) at a large reduction ratio, but the mechanism producing a *visible, continuous* flicker from a one-shot decode was never fully traced. Fixed at the point of entry instead of chasing the exact mechanism further: new `cover_downscale_max(jpeg_path, max_dim)` in cover.c (decodes via libjpeg, box-filter resizes preserving aspect ratio -- no crop, since this rewrites the real cover.jpg on disk, not a thumbnail -- then re-encodes and write-then-renames over the original; a no-op if already within bounds), using compress-side libjpeg symbols dlsym'd lazily and independently of the existing decode symbols so a hypothetical build missing them can't take `cover_load()` down too. Wired into both Last.fm/Spotify fetch call sites (`art_worker()`/`view_art_worker()` in music_hook.c) via a new `FETCHED_COVER_MAX_DIM` (800), since a network-fetched cover is exactly the case with no control over source resolution. The resize/re-encode algorithm itself was validated locally first (compiled standalone against Homebrew's libjpeg-turbo, not the device's libjpeg9, but the same standard API): correct output dimensions and a clean, uncorrupted image for both a landscape source (the real 1415x1405 replacement cover, downscaled to 800x794) and a synthetic portrait one (1200x2000 -> 480x800), plus a correct no-op on an already-small source. **Not yet live-verified end-to-end** (i.e. via an actual triggered Last.fm/Spotify fetch on-device) -- that requires a real fetch to occur naturally; the manual on-device fix (replacing this album's cover with a pre-shrunk 500x500 image) is what's confirmed fixed, not the new automatic downscale path itself. |
| BG102 | ~~**Bluetooth device list: tap silently does nothing**~~ **fixed, 2026-09-03** | Reported live: "I can't tap to select a bluetooth device I want to pair with. it's like the tap doesn't get picked up on." Root cause: `SC_SETTINGS_BT`'s scan-result list is a small, bespoke, non-scrolling draw loop that predates `vis_rows()`'s own mini-player accounting (see that function's comment on exactly this class of problem) and never got folded into it -- so with something already playing (the mini player showing, very much the common case when pairing a *new* headset), device rows past the fifth or sixth could draw into the same screen area the mini player owns. The mini player's own tap zone is checked earlier in the tap-dispatch chain and takes priority globally regardless of which screen is showing, so those rows looked tappable but any tap there actually went to the mini player instead. Fixed with `bt_scan_max_visible()`, shared by the draw loop and the tap handler (same "one boundary, not two copies of it" reasoning as `MINI_BTN_R`/BG21 and `track_index_at()`/BG2/BG61) -- a device not actually visible above the mini player is now never listed as if tappable, rather than drawing an invitation the app can't honour. **Known remaining limit, not fixed here**: this list still doesn't scroll, so a device beyond what fits (fewer slots with the mini player showing) is simply not offered at all rather than reachable another way -- real scrolling would need the same `off`/`scroll` treatment every other list on this screen already has, not attempted in this pass. |
| BG101 | ~~**Podcast episodes not showing for a long feed title**~~ **fixed, 2026-09-03** | Reported live: "Transmissions in the podcast client still not showing episodes" -- the exact `POD_NAME_LEN` truncation bug from the reverted R65 work, resurfacing on its own since it's a general bug, not something specific to search/subscribe. "Transmissions - The Definitive Story of Joy Division & New Order" is exactly 64 characters; `POD_NAME_LEN` was 64, and `snprintf()` reserves a byte for the NUL, so `pod_scan_feeds()` silently truncated the name it read back from the real (untruncated) folder on disk. `pod_load_episodes()` then built its `opendir()` path from that truncated name, which doesn't exist, and got 0 episodes back with no error -- indistinguishable from an empty feed. Fixed by raising `POD_NAME_LEN` to 128 in `podcast.h` -- real feed titles run longer than album names, so this gets genuine headroom rather than another number to eventually hit. |
| BG100 | ~~**Brightness drops on wake, BG95 regression**~~ **fixed, 2026-09-03** | Reported live: "brightness has started going down to 50% again between screen wake ups" -- BG95's own symptom, back. `music.log` showed the actual mechanism directly rather than needing to guess: repeated pairs of `[music] locked` lines ~200ms apart (well inside `DOUBLE_PRESS_MS`'s 400ms window, and far too consistent to be hardware bounce) on effectively every lock, meaning this user's own habitual double-click on power is genuinely reaching the button-lock double-press handler every time, not just occasionally. That handler deliberately forces `set_locked()` to re-run its full body even when already locked (needed so a double-press can still toggle `button_locked` on top of an already-locked screen) -- but `set_locked(1)`'s own lock branch unconditionally re-read the backlight into `saved_brightness` every time it ran, and on the *second* call the backlight was already at zero from the first, silently clobbering the real value with ~0. Every wake after that used the clobbered value (or `DEFAULT_BRIGHTNESS`, 43 -- close enough to "50%" as a rough description) instead of the level actually in use before locking. Two fixes, not one: (1) `set_locked(1)` now only overwrites `saved_brightness` when the backlight still reads something real, so a redundant re-lock of an already-dark screen can't clobber it in the first place; (2) the double-press *unlock* path (`button_locked` going false) now applies BG95's own `last_lit_bright` safety net before calling `set_locked(0)`, exactly like the "dark for any reason" single-press wake path already does -- BG95 only ever covered one of the two unlock routes, and this user's habit reaches the other one on every cycle. (2) is the general-purpose fix (protects against any future way `saved_brightness` might go wrong); (1) closes the specific mechanism found live. |
| BG99 | ~~**Auto theme mode never actually reaches Grey**~~ **fixed, 2026-09-02** | Reported live: "I never see the 'grey' theme when everything is set to auto." Root cause matched the report exactly -- R45's Auto mode (`apply_theme()`) only ever chose between Light and Dark by time of day; Grey was a real, manually-selectable theme with no path into Auto at all, at any hour. Fixed by giving Grey the twilight band: `auto_theme_effective()` (renamed from `is_daytime()`, now returns `THEME_LIGHT`/`THEME_DARK`/`THEME_GREY` directly) uses a fixed 45-minute window straddling both the sunrise and sunset estimate -- Grey inside that window either side, Light for the rest of the day, Dark for the rest of the night. 45 minutes fixed, not a fraction of day length, since the reason for a transition theme (easing between a bright and a dark palette) doesn't scale with how long the day is. Sanity-checked against the device's own real config (`tz_index = 13`, UTC+1, DST-unaware per R45's own documented limitation) against real UTC+2 wall-clock time at the moment of the report (19:56 CEST) -- lands about 31 minutes from the calculated sunset for that date/latitude, inside the new window, consistent with what was expected to be seen. |
| BG98 | ~~**0.44 regression: wake-from-lock feels fractionally slower**~~ **fix implemented, not yet verified live** | Reported live as a subtle feel, not a hard reproduction -- "I have the feeling the wake from sleep is now fractionally slower in 0.44." Traced to BG90's own fix: the audio worker thread's periodic Bluetooth-reconnect check (`bt_sink_connected()`, forking `bluealsa-cli` every ~5s while playing over wired output) has no way to know the screen is locked, since it runs on a different thread than the one that tracks `locked` -- so it keeps firing the whole time a locked-screen listening session runs. A subprocess spawn landing at the exact moment the power button is pressed genuinely competes with the wake-up redraw for the single core, which is exactly a "fractionally slower wake," not a placebo. Fixed with `audio_set_screen_locked()` (`audio.h`/`audio.c`), called from `set_locked()`, so the worker thread's poll now skips the check entirely while locked and resumes its own tick count once unlocked -- nothing to gain from finding a Bluetooth reconnect while the screen is dark anyway. Builds clean on both targets; needs a live A/B (lock while playing over the 3.5mm jack, wake repeatedly, compare feel against 0.43) once the device is available -- the original report was a "feeling," not a measured delta, so confirming the fix will be similarly subjective. |
| BG94 | ~~**Settings: "Power button lock"/"Bypass DSP on USB" toggle switches ignore taps, only the label works**~~ **done, verified live (0.42) -- the real root cause behind BG93's item (4)** | Reported live immediately after 0.41 shipped BG93's tolerance-widening as a best-effort guess for the same underlying symptom. This time reproducible and precise: these two rows (and "Auto shutdown", same shape, not yet reported but fixed alongside them) are **taller than `ROW_H`** -- each carries a two-line description under its label, via `lock_h`/`usbbypass_h`/`autooff_h` in the draw code (`set_row_usbbypass_y() - set_row_lock_y()`, etc. -- 136px, not 72). The toggle switch itself is drawn *centred within that taller block* (`draw_toggle_switch_h_clip(fb, ry, ..., lock_h, ...)`), sitting lower on screen than a plain-`ROW_H` center would place it -- but the tap handler hit-tested against plain `ROW_H`, so the switch's own pixels fell partly or wholly outside its tap zone. The label at `ry+20` sits well within the shorter zone regardless, which is exactly why only the label ever worked. Fixed by computing the same `lock_h`/`usbbypass_h`/`autooff_h` in the tap handler that the draw side already used, and hit-testing against those instead of `ROW_H`. **Verified live**: tapped the "Power button lock" switch directly (not the label) at its actual drawn position -- toggled off, then on again, both registering correctly. BG93's `DRAG_MIN` widening (18->24px) is left in place -- harmless, and plausibly still a real, if smaller, contributor -- but this is the actual fix. |
| BG93 | ~~**Main Menu EXIT button / standalone back-restart, USB icon size, Settings tap tolerance**~~ **done, verified live (0.41)** | Three items requested together for 0.41. (1) **Main Menu's "EXIT" removed in both builds** -- in the hooked build it only ever duplicated the edge-swipe gesture (both call `go_back()`, both hand control to `hiby_player`'s launcher), in standalone it did nothing a user would want. (2) **Standalone: edge-swipe-back at Main Menu restarted the whole app**, reported live and looking exactly like a crash-restart -- root cause: `go_back()`'s `SC_MENU` case unconditionally returned 0 ("leave the app"), which in the hooked build correctly hands back to the launcher but in standalone just made `standalone_main.c`'s own `for(;;) music_entry();` loop re-enter immediately. Fixed with `g_is_standalone` (set once from `is_hiby_player()` at the top of `music_entry()`): Main Menu is now a true root with nowhere to go in standalone, unchanged in the hooked build. **Verified live**: same process PID before and after an edge-swipe at Main Menu in the standalone build. (3) **USB quick-settings icon bumped 26x26 -> 32x32** to match Wi-Fi/BT visually -- confirmed live, looks right now. (4) **Settings "tap capture area too small"** -- extensive live investigation (precise synthetic taps at exact row boundaries, every Settings sub-screen: main list, Wi-Fi, Bluetooth, Theme, Timezone, USB) never reproduced an actual hitbox/draw-geometry mismatch; every row's tap zone matched what was drawn exactly, and the Wi-Fi/BT screens are deliberately non-scrolling so scroll-offset math wasn't it either. Shipped `DRAG_MIN` (tap-vs-drag threshold) widened 18 -> 24px as a best-effort tolerance increase, documented honestly as not a confirmed root-cause fix. |
| BG92 | ~~**Podcast sync appears to crash the player**~~ **fix shipped, root cause confirmed via strace; the full-device freeze that accompanied it once is not fully explained** | Reported live as a crash-and-restart. Diagnosed with `strace -f` attached to the live `library_standalone` process while reproducing: no crash, no signal, no segfault -- the app was alive the whole time. `curl` (`podsync_once.sh`'s bundled static binary) was blocked in `poll()` mid-connect to one feed's host (`feeds.theincomparable.co`), and never resumed for the rest of the trace. Root cause: `podsync_once.sh`'s shared `get()` helper applied `--max-time 900` (15 minutes) to *every* fetch through it -- reasonable for an actual episode download (large file, plausibly slow), wildly excessive for fetching a feed's small RSS/cover, and since feeds are fetched sequentially with nothing else running concurrently, one slow/stalled host leaves the entire sync looking dead for up to 15 minutes, which reads exactly like a crash to a user who isn't going to wait that long. Fixed by splitting the helper: `get()` (feed RSS + cover images) now uses `--connect-timeout 10 --max-time 30`, and a new `get_episode()` keeps the original generous timeout for the one call site that actually needs it (episode downloads). Pushed directly to the SD card (`.podsync/podsync_once.sh` is content, not rootfs -- takes effect on the next sync, no reboot). **Honest gap**: the specific reproduction that prompted this investigation also left `adbd` itself completely unresponsive (`adb reboot` timed out) -- a plain userspace `curl` stall blocked in `poll()` should not do that on its own, and nothing in the strace trace or a post-recovery `dmesg` explains the full-system freeze specifically (the kernel ring buffer only covers since the forced reboot, so whatever caused it is gone). The timeout fix directly addresses the confirmed, reproducible symptom (sync looks hung/crashed); it is not yet confirmed to be the whole story behind the one full freeze. Worth watching for a repeat. |
| BG91 | ~~**Tapping "Substance" (Joy Division) also played tracks from "Substance 1987" (New Order), interleaved**~~ **done, verified live** | Reported live, visible directly in `music.log`'s own play sequence (Joy Division track 4, then New Order track 4, back to Joy Division track 5...). Root cause: `tracks_query()` (`library.c`) matched `album` with the same trailing-wildcard `LIKE 'album%'` pattern the artist/genre facets use deliberately (a compound value like "X, Y" still matching on the shorter "X" -- see this function's own header comment on the album_artist half) -- a reason that has nothing to do with album titles, which have no such shorter/longer relationship to each other. "Substance" and "Substance 1987" are simply two different albums that happen to share a prefix, and the wildcard matched both. Fixed with exact equality instead, still tolerant of the stock DB's embedded-trailing-NUL quirk (this file's own header comment) by comparing against both the plain value and the value with one real NUL byte appended -- built in C with an explicit `sqlite3_bind_text` length rather than SQL's `char(0)`, which the same header comment already found doesn't behave as a real NUL byte in this SQLite build. **Verified live**: queried the on-device scanned_media.db directly with the new exact-match logic -- 17 rows for "Substance", all Joy Division, no New Order tracks. |
| BG90 | Settings screen scrolling isn't smooth | Reported live, not investigated. Deprioritised behind the standalone-boot work (RP1) -- worth checking whether it's specific to Settings (its row count has grown a lot this session: theme/light-theme/timezone/reindex/Wi-Fi/BT/USB/scan rows all added recently, `settings_content_rows()` now walks a much taller list per frame than when this screen was first built) or a symptom of something list-scrolling shares more broadly. |
| BG89 | ~~**Artist page photo/bio work fine within a session but are gone after a full device reboot**~~ **done, build clean, pending live verification (device disconnected)** | Reported live. Confirmed not a same-session caching problem first (revisiting the same artist reappears instantly, no reload) -- isolated specifically to a *full device reboot*, not an app restart. Root cause: `fclose()` after writing the bio file (and the `rename()` landing the downloaded photo) only flushes to the OS's own page cache, not to the physical SD card -- this app's own deploy-time reboots already know to `sync` first (see `deploy.sh`'s own comment on exactly this class of loss), but a user-initiated reboot has no reason to wait for a background artist-page fetch's writes to actually reach the card, and the FAT-family filesystem these cards format as has no journal to replay lost writes from afterward. A fetch that completed and updated the screen moments before an abrupt reboot could plausibly still be sitting unflushed. Fixed with one `sync()` call at the end of `artist_art_worker()`, once per fetch (not once per write) since a fetch is already a multi-second network operation next to which one sync is noise, and this only ever runs on a background thread. Build clean in both repos. Not yet live-verified -- the device was disconnected for this whole diagnosis and fix; needs a real fetch, a real (not app-level) reboot, and a re-visit to confirm. |
| BG80 | ~~**Cache final image of audiobook player to prevent apparent reset during progress load**~~ **done, build clean, pending live verification (device disconnected)** | Root cause, found by static read-through (no `ab_book_loading`/loading-flag mechanism exists at all -- confirmed nothing in `music_hook.c` shows a placeholder state): `ab_play_chapter()` calls `audio_play()` (which synchronously zeroes `g_pos_ms`/`g_dur_ms` on the caller's own thread, before the new worker thread has even started) immediately followed by `audio_seek_ms()` (which only queues the target -- `g_seek_to_ms` -- for the worker to pick up). Opening a fresh decoder and PCM connection is not instant, especially over Bluetooth (see BG79's own findings on that connection's cost), so for however long that takes, `audio_pos_ms()` genuinely reads 0/stale rather than the resumed position, and the Now Playing draw path computed the chapter/book progress bars straight from it -- reading as the player having reset to the chapter's start before snapping to the real position once the worker caught up. Rejected the ticket's own suggested approach (cache/restore a framebuffer snapshot): it would paper over the position jump but introduce a worse one of its own whenever the *previous* screen was a different book -- title, cover and chapter would flash stale before catching up, which is a more visible bug than the one being fixed. Fixed at the actual source instead: added `audio_seek_pending_ms()` (`audio.c`/`audio.h`), returning the still-outstanding target of the most recent `audio_seek_ms()` call (-1 once the worker has applied it), and had the audiobook draw path in `music_hook.c` prefer it over `audio_pos_ms()` whenever it's non-negative -- so the very first frame after tapping into a chapter already shows the correct resumed position instead of the chapter start, no snapshot or placeholder needed, and it falls through to the live position automatically the moment the two agree. Build clean in both repos. Not yet live-verified -- device disconnected throughout; needs tapping into a book (ideally over Bluetooth, where the open is slowest) and watching the very first rendered frame for whether the progress bars already show the resumed position or briefly show the chapter start. |
| BG81 | **Skipping in podcast causes light crunching in audio** | Plausible root cause found, not fixed -- needs live confirmation before touching decoder internals. BG53 built a background per-track MP3 seek table (`mp3_seektable_worker()`, `drmp3_calculate_seek_points()`) specifically so seeks land accurately; a seek performed *before* that background build finishes falls back to dr_mp3's cruder default seek path, which is more prone to exactly this kind of artifact at the seek point (MP3's Layer III bit reservoir means a seek that doesn't prime enough decoder state ahead of the target can produce a brief glitch on the first frames after landing). The build is proportional to file length -- a typical 3-4 minute music track's table finishes almost instantly, but a 30-90 minute podcast episode's takes far longer on this CPU, making it much more likely a user's skip (podcasts default to Β±10s/30s taps, often used early and repeatedly) lands before the table is ready. This would explain why the symptom is reported for podcasts specifically and not music. Not fixed: the real remedy (speeding up the build, or making dr_mp3's pre-table seek prime more decoder state around the target) means either profiling the seek-table build's actual wall-clock cost on-device or reading into dr_mp3's own seek internals, and I don't want to touch decoder seek behavior speculatively without being able to listen for whether it actually helped. Left open. |
| BG82 | **Switching from music to podcast without restart causes audio skipping** | Likely moot as originally described, not fully verified. This ticket's own premise ("two apps' audio pipelines," "blocker for unified Library L5") describes the *pre*-unification world -- confirmed there is now exactly one process (`hiby_player`), one hook (`libpodcast_hook.so`), one `audio.c`, and one ALSA device (`plughw:0,0`) for everything. `pod_play_episode()` calls the identical `audio_play()` every music track uses, which itself always calls `audio_stop()` (synchronous join) before starting the new decode thread -- the same sequential stop-then-start every ordinary track change already goes through, mechanically no different from switching between two differently-formatted music tracks (which the existing format-change-reopen path already handles routinely). No separate podcast-specific PCM path, device, or leftover decoder state found. What static review can't confirm is the actual audible result -- needs someone to play music, switch straight to a podcast, and listen for the reported skip/glitch to close this out for real. |
| BG83 | **Played and remaining times in podcasts don't update like in audiobooks** | Investigated, inconclusive -- static review found no code-level divergence to point at. The Now Playing numeric-time/progress-bar block (`music_hook.c` ~line 3851) is the exact same code for podcasts and music, both reading live `audio_pos_ms()`/`audio_dur_ms()` every redraw with a per-second dirty-trigger (~line 7800) that isn't gated on mode. Episode duration (`pod_probe_dur()`, `podcast.c` line 205) is precomputed once per episode from the file itself (MP3 header bitrate + `audio_probe_dur_ms()`, or the M4A moov atom) the same way audiobook chapter durations are precomputed by the library scan -- not fetched live from an unreliable source. Nothing found that would make one update and the other not. Left open; needs an actual side-by-side watch of a podcast vs. an audiobook Now Playing screen to see what specifically differs (bar not moving? clock text frozen? only on certain episodes?) before a real root cause can be chased. |
| BG84 | ~~**Volume bug fixes not applied to podcasts**~~ **done -- confirmed already moot, no change needed** | Checked the premise directly: there is exactly one `audio.c` in this repo, shared by music and podcasts alike -- `pod_play_episode()` calls the very same `audio_play()`/`worker()` every music track goes through, not a separate pipeline. This app's Library/Podcasts merge (L5) had already happened by the time this ticket was written; nothing here is a distinct "podcast's own audio.c" to port fixes into. BG67/BG68/BG79's volume fixes (all in `audio.c`'s shared `worker()`) apply to podcast playback automatically as a result. No code change made -- nothing to fix. |
| BG85 | ~~**Adding to queue changes the name of the currently playing album**~~ **done, build clean, pending live verification** | Root cause: `queue_play_next()` (BG73) overwrote `q_album`/`q_artist` with the played-next track's own album the instant it was queued -- but that track isn't playing yet, only next in line, and the Now Playing screen draws the track name from the actually-playing entry (`t->name`) separately from the album line (`q_album`, `music_hook.c` line 3799) -- so the album line switched to the *new* album immediately while the track name (and the audio) stayed on the old one, for however long the current track had left. This was actually BG73's own confirmed-working behaviour at the time ("Now Playing immediately showed '[Led Zeppelin IV]'" while "Hello" was still playing) -- reasonable for that ticket's narrow verification, but reads as a real mismatch from the angle this ticket reports it from. `queue_insert()`'s plain "Add to queue" was already unaffected (only ever sets `q_album`/`q_artist` from a cold start, `queue_n == 0`) -- no change needed there. Fixed by deferring: added `q_artist_pending`/`q_album_pending`/`q_pending_at` and a `queue_apply_pending()` helper that commits them into `q_artist`/`q_album` only once `cur_track` actually reaches the played-next track's index -- called from both `play_index()` (manual skip/tap-to-play) and the natural gapless-advance path in the main loop (the two distinct places `cur_track` can move to it), and reset to "nothing pending" at every fresh-queue-start site (`play_from_list()`, `pod_play_episode()`, `ab_play_chapter()`) so a stale pending from a previous queue can't misfire against an unrelated later one. Build clean in both repos. Pending live verification: same Oasis/Led Zeppelin Play Next scenario as BG73 -- confirm the album line stays "Oasis" (or whatever's actually playing) until playback genuinely reaches Black Dog, not the instant it's queued. |
| BG86 | ~~**Selecting tracks in p.u.l.s.e list has tap detection offset**~~ **done, build clean, pending live verification** | Root cause: R56's disc banners (`tracks_multi_disc()`) gate purely on whether tracks' `disc` fields differ, with no check for whether this is actually a single album being browsed. A playlist mixing tracks pulled from several source albums/discs (like p.u.l.s.e) tripped that check just the same as a genuine multi-disc album, inserting banners between essentially unrelated songs wherever their carried-over `disc` numbers happened to change -- shifting every row below by `DISC_BANNER_H`. The draw loop and `track_index_at()` (tap resolution) were never actually inconsistent with each other -- both walk the identical layout -- so this wasn't a hit-test math bug; it read as one because nobody browsing a playlist expects disc banners to appear at all, so a tap aimed at "the Nth track" (counting only tracks) landed on whatever the banners had actually shifted into that slot. Fixed by gating `tracks_multi_disc()` on `!browsing_is_playlist` as well -- disc banners now only ever appear for a genuine album browse, never a playlist, restoring flat, predictable `ROW_H`-per-row spacing there. Build clean in both repos. Pending live verification against the actual p.u.l.s.e playlist. |
| BG87 | ~~**Back from queue shows old album screen with mismatched data**~~ **done, build clean, pending live verification** | Root cause: `albums_scroll_saved`/`albums_scroll_px_saved`/`albums_artist` are written only at the moment an Albums row is actually tapped (`music_hook.c` ~line 7151). The "back out of a mixed queue" path (`go_back()`'s `SC_QUEUE` case, `queue_via_back`) reused them to restore Albums -- correct for the *unmixed* case, where SC_TRACKS can only ever have been reached by that exact tap, but not for a mixed queue: Play Next can splice a track in from the Artist Page, a playlist, or Recent (none of which touch these fields), and any unrelated album tapped later in the same session (just to browse, not necessarily played) silently overwrites them -- so backing out of a mixed queue could land on Albums filtered/scrolled to a completely different, unrelated browse, read live as "the album screen looks stale/mid-scroll, doesn't match what's playing." There is no reliable "this queue's own Albums context" to reconstruct for a mixed queue, so rather than restoring possibly-unrelated state, this now lands on a clean, unfiltered, top-of-list Albums view (`cur_artist` cleared, `scroll`/`scroll_px` zeroed) -- confidently right instead of confidently wrong. The unmixed queue-button path (`queue_via_back` false, returns straight to Now Playing) is untouched. Build clean in both repos. Pending live verification: build a mixed queue (Play Next from a different album), swipe back from Now Playing to the plain Queue screen, back again, confirm Albums opens clean rather than on stale/mismatched state. |

| BG88 | ~~**Back navigation stuck in loop between artist page and album**~~ **done, build clean, pending live verification** | Root cause confirmed by static read-through: `tracks_from_artist_page` (R55) is set to 1 only when tapping an album row on the artist page, and was cleared at the two *other* SC_TRACKS entry points (SC_PLAYLISTS, SC_ALBUMS taps) -- but never cleared in `go_back()`'s own `default:` case, the one place that actually consumes it to route back to `SC_ARTIST_PAGE`. Sequence that loops: artist page -> tap an album (flag=1) -> back (flag still 1, lands on artist page) -> artist page's own `go_back()` "not from the list" branch sends back to that *same* album (a `SC_TRACKS` landing that had nothing to do with the artist page's list) -> back again hits the `default:` case, sees the stale flag still set, and bounces to `SC_ARTIST_PAGE` a second time instead of continuing out to Albums/Artists -- repeating forever. Fixed by clearing `tracks_from_artist_page` in the `default:` case at the moment it's read, matching the "set once, cleared at every consuming/competing entry point" pattern the other SC_TRACKS-origin flags (`browsing_is_playlist` etc.) already follow. Build clean in both repos. Pending live verification: tap into an artist's album from the artist page, back out twice, confirm it reaches Albums/Artists instead of bouncing. |

| BG89 | **Switching USB modes not reliable** | Switching between USB modes (storage, charge, debug/ADB) is not working reliably. The device may not respond to mode changes, or may switch inconsistently. Check: (1) whether USB mode detection and switching logic is working correctly, (2) whether the system is properly communicating mode changes to the USB controller/kernel, (3) whether there's a race condition or timing issue in the mode-switch sequence, (4) whether the Settings UI correctly reflects the currently-active USB mode. Related to RP5 (misc system settings) and R39 (USB-C controls). May require system-level investigation if the issue is in the firmware/kernel rather than the app. |

| BG90 | **Bluetooth headphone connection doesn't immediately switch audio output** -- **fix implemented, not yet verified live (device unavailable)** | Two real, separate bugs found by direct code inspection (`audio.c`). (1) **Priority was backwards**: `pcm_open()`'s own comment said "whatever is plugged into the jack wins, then USB, then Bluetooth" -- the opposite of what this ticket specifies. Fixed: USB > Bluetooth > jack, with the jack check dropped entirely from the priority computation (it never outranks either now, it's just what "wired" plays out of when neither applies). (2) **No live re-check for Bluetooth mid-playback**: the ~1s output-poll in `worker()` only ever called `usb_card()` (a cheap readlink); `bt_sink_connected()` was deliberately left out of it, per its own comment, because it forks a `bluealsa-cli` subprocess and doing that every second would be a real ongoing cost on this single core. That left Bluetooth connecting mid-playback to wait for the next track (or any other event that reopens the PCM), exactly the reported symptom. Fixed by polling `bt_sink_connected()` too, but only once every 5 passes of the existing poll (~5s) and only while output is still "wired" (`g_out_kind == 0`) -- USB or Bluetooth already active means there's nothing for it to preempt either way, so it's skipped then. Builds clean; needs a real headset-connect-mid-playback test once the device is back. |

| BG91 | **Audiobook playback speed doesn't survive reboots** -- **fix implemented, not yet verified live (device unavailable)** | Ticket's premise didn't fully hold up under inspection: `pod_speed_permille` (podcast's own speed) isn't actually persisted anywhere either -- it only *looks* like it survives because it's a plain `static` that outlives an episode change within one running session, the exact same way `ab_speed_permille` already does for audiobooks. Neither has ever been written to `music.conf`. Fixed both for real parity: new `ab_speed_permille`/`pod_speed_permille` keys, loaded in `load_conf()` (clamped to the same 800-2000 permille range `audio_set_speed()` enforces), saved on every tap of either screen's speed-cycle control. Builds clean; needs a real reboot test once the device is back. |
| BG92 | **Seek should be disabled while quick-settings dropdown is visible** -- **fix implemented, not yet verified live (device unavailable)** | Root cause found directly: the `scrub_active` computation in `music_hook.c`'s touch-handling loop was missing the `!qs_open` guard every *other* gesture check right around it already has (edge-swipe-back, title long-press) -- a touch landing in the progress bar's y-range started a scrub regardless of whether the quick-settings panel was open over top of it. One-line fix: added `!qs_open` to the condition. Since `SC_PLAYING` is the single shared Now Playing screen for music/podcast/audiobook alike, this one fix covers all three. Builds clean; needs a live check (open quick-settings, try to scrub) once the device is back. |
| BG93 | **Volume control is erratic and stopped working for Bluetooth** -- **fix implemented, not yet verified live (device unavailable)** | Traced to a regression in this session's own earlier BG59 amixer-backoff fix (`audio_bt_volume_service()` in `audio.c`). `bt_read_pct()` failing even once -- a momentary D-Bus timeout, a race with a write that had just landed, not necessarily the headset actually being gone -- cleared `bt_mixer[0]` immediately, and the backoff logic added for BG59 then treated that exactly like "never found a mixer at all," throwing volume control into the same up-to-60-second backoff a real disconnect gets. One transient hiccup could read as "stopped working" for up to a minute, matching the reported symptom closely. Fixed with a small consecutive-failure counter (`bt_read_fail_count`, `BT_READ_FAIL_LIMIT = 2`): only clears the mixer after two failed reads in a row, not one, so a lone blip costs nothing beyond skipping that poll's readback while a genuine disconnect (which fails consistently) is still caught just as reliably. The "erratic/inconsistent jumps" half of the report wasn't independently reproduced or root-caused beyond this -- the general (non-Bluetooth) volume-drag code was read and looks sound, no second bug found there. Builds clean; needs a live check (extended Bluetooth listening session, watching for the volume control silently going dead) once the device is back. |

| BG94 | **Volume button input has excessive lag when adjusting with physical buttons** -- **fix implemented, not yet verified live** | Ticket's own hunch (4) was right: this is BG93's backoff schedule from the *pending-request* side, not the read-failure side BG93 itself fixed. `audio_bt_volume_service()` only re-runs `find_bt_mixer()` on its own timer (`bt_mixer_next_try`) -- correct for an unattended background probe, but a volume request that arrives while `bt_mixer` is still empty (freshly connected, or landing between two scheduled retries) just sits in `bt_vol_pending` doing nothing until that timer comes back around on its own -- up to the full 5s early-retry interval, or 60s once backoff has settled further out. Wired volume and the on-screen slider both go through the exact same `bt_vol_pending` mechanism, so this should reproduce on the slider too under the same connection-timing conditions, not just hardware buttons -- the ticket's own suggested comparison should confirm that once tested. Fixed in `audio.c`: both `audio_volume_set()` and `audio_volume_step()` now reset `bt_mixer_next_try` to "now" whenever they mark a new pending request and no mixer is currently known, so a real button press or slider drag always gets an immediate retry attempt instead of waiting out a schedule that was only ever meant to bound an idle probe's own frequency. Builds clean on both targets; needs a live test with a Bluetooth headset reconnecting from cold (the exact window this fix targets) once the device is available. |
| BG95 | **Brightness should persist across sleep/wake cycles, not reset to pre-sleep value** | When the device goes to sleep/lock and then wakes up, the brightness level should remain at the level it was set to before sleeping. Currently, it appears to reset to (or revert to) the brightness level from before sleep, causing an unwanted change. This is related to BG78 (brightness not surviving reboots) but specific to sleep/wake cycles. Check: (1) whether the brightness value is being saved when sleep is triggered, (2) whether it's being restored correctly on wake, (3) whether there's a separate code path for sleep that differs from the reboot path, (4) whether the sleep/wake logic is inadvertently calling the brightness reset code. Brightness should be sticky across all state transitions (sleep, lock, reboot). |

## Regressions

Issues that used to work but no longer do. Fix before shipping.

| # | Item | Notes |
|---|---|---|
| RBG1 | ~~**Bluetooth headphone controls no longer work**~~ **to be tested by human** | Tested while listening to Bora Gisladottir. Headphone controls (play/pause, next, previous from AVRCP headset) stopped registering after previously working (BG20). Root cause: BlueZ tears down and recreates the AVRCP input node on every headset reconnect, reusing the same `eventN` path. `scan_inputs()`'s "already have this device" check only compares that path string, so once it had `event4` recorded it never looked again -- the old fd was left open, silently dead, and `handle_keys()` had no way to notice or recover. `handle_keys()` now returns -1 when its fd hits a true EOF or a real read error (not `EAGAIN`, which is normal on a nonblocking poll with nothing queued), and the caller closes that fd and drops it from the tracked list, so the next periodic `scan_inputs()` (every 90 frames) picks the live device back up under the same name. Needs a live reconnect test: pair/connect a headset, use its buttons once to confirm the baseline works, disconnect and reconnect it, confirm the buttons still work afterward -- that second half is what was broken. |
| RBG2 | ~~**Bora Gisladottir album art missing**~~ **done** | Root cause found and fixed alongside the cover-art priority reorder (see BG43's neighboring commit): `art_candidate()`'s new embedded-picture-first ordering returns `ART_SKIP` for slot 0 on any track with no embedded picture, but its only caller was treating any non-zero return as "no candidates left," breaking the walk before it ever reached the folder-image fallback -- silently losing art for every track relying on a folder `cover.jpg`, Bora Gisladottir's among them. Fixed the caller to treat `ART_SKIP` as "keep walking." Confirmed live: `EQProfiles`/cover-art code path aside, the Parametric EQ screen -- see RBG3 -- was checked in the same session and rendered correctly on this build. |
| RBG3 | ~~**EQ profile picker shows only Cancel button**~~ **done** | Could not reproduce on the current build: opened Parametric EQ -> Profile live on-device and both profiles (Jabra Elite 4 Active, Sony WH-1000XM4) listed correctly above Cancel. `ep_scan()` and both picker-opening call sites (`music_hook.c`) were read end to end and are unconditional -- nothing gates them on Bluetooth state, contrary to the original suspicion. `/data/mnt/sd_0/EQProfiles/` has always had both `.txt` files present, and the on-device log shows `ep_scan()` finding both at every past startup. Almost certainly a symptom of the same broken half-finished build that caused RBG1/RBG2 (see the session note on stashing out that WIP before this deploy), not a bug in the code as committed. |
| RBG4 | **Tap detection offset in album/artist lists** | Regression: tap hit detection is offset vertically — pressing the item ABOVE the desired one selects it instead. Affects album lists, artist lists, and other row-based lists. Indicates `tap_row()` or row layout calculations (`row_y()`, `ROW_H`, or similar) have been changed or misaligned. Check: (1) row drawing position vs. tap hit-test zone alignment, (2) recent changes to row height or spacing calculations, (3) whether Y-coordinate offset is consistent across all lists or varies by context, (4) related to the `bar_y()` alignment fix in BG40 — may indicate a similar drift elsewhere. **Critical for usability.** |
| RBG5 | **0.44: wake-from-lock feels fractionally slower than 0.43** -- **user unconvinced by measurement so far, re-flashing 0.43 to A/B directly** | Reported live as a feel, not a hard reproduction. Investigated via direct instrumentation (`music.log`, both `music_hook.c` and `audio.c`, all `CLOCK_MONOTONIC`) rather than reasoning from the diff alone: (1) `wake latency N us` from the moment a power press is recognised as a wake to the first real frame reaching the panel, broken into `set_locked` / `predraw` / `draw+pan` thirds; (2) `bt_sink_connected() took N ms`, timing the specific subprocess BG90 added to the worker thread's poll (the original suspect, given it was the one thing BG90 changed on this exact path). Findings across ~9 real wakes: total latency is a tight ~290-310ms band every time; **all of it (272-292ms) is inside `set_locked(0)` itself**, and splitting that further showed **essentially all of that is the bare `FBIOBLANK`/`FB_BLANK_UNBLANK` ioctl** (275-291ms) -- the kernel call that physically powers the LCD panel back on. Brightness restore is under 1ms; the rest of the loop before drawing is under 1ms; `draw_ui()` + the pan ioctl together are ~18-19ms. No wake in the sample overlapped a `bt_sink_connected()` call in time, and even where they came close, the 18-24ms that call costs is within the ~20ms of natural sample-to-sample jitter already present in the panel-unblank cost alone. Conclusion reached from this data: the measured path shows no evidence of a 0.44-introduced regression, since the dominant cost (`FBIOBLANK`) is unchanged code with no diff between the two versions on this ioctl or its call site. That conclusion was **not accepted** -- flashing an actual 0.43 build side-by-side is the next step, not further reasoning from 0.44 alone. Open questions this doesn't rule out: whether 0.43 was calling `FBIOBLANK`/`set_locked(0)` from a different, previously-hotter code path with something else warmed up in the CPU cache or scheduler state at that moment (unlikely but not eliminated); whether the perceived difference is elsewhere entirely (screen content, LED behaviour, audio resuming) rather than the raw wake-to-visible latency this instrumentation targets; whether 0.43 itself varies release to release in a way "0.43" as a single reference point doesn't capture. The wake-latency/`set_locked`/`bt_sink_connected` logging added for this investigation is left in place (cheap, ~1 log line per wake) so a 0.43 build could have the identical lines back-ported for a true instrumented A/B rather than a feel-only one, if that ends up being wanted after the plain side-by-side. |

## Wishlist

Nice-to-have features, low priority, the kind of thing that would be great to have someday if the mood strikes.

| # | Item | Notes |
|---|---|---|
| ~~W1~~ | ~~ALAC support~~ **done, see BG19** | Shipped and verified on-device; kept here only as a pointer since BG19 has the full story. |
| W2 | **SoC suspend — sequence recovered from stock, not yet implemented** | **Do not write `mem` to `/sys/power/state` cold: it hangs this device.** Tried live 2026-08-08 with an RTC wakealarm armed and *verified* in the future (guarded so it would refuse to suspend otherwise). The panel blanked, the backlight stayed lit, and nothing brought it back — not the power button, not the RTC alarm at +40s, not a host-side `adb kill-server`. Backlight-on-but-black is the tell: the sequence got as far as blanking the display and then stalled, so it never reached a state any wake source could act on. That is a hang *inside* suspend, not a sleep we failed to wake from. Cost two hard reboots to establish. **The stock 1.6 player does it properly, and the order is recoverable** — `strings usr/bin/hiby_player` (binary order ≈ source order) gives: `PM: sleep` → `PM: early suspend` → `PM: sd0_msg %s` → `/usr/bin/bt_suspend` → `PM: suspend` → `echo mem > /sys/power/state` → `PM: resume` → `PM: late resume` → `/usr/bin/bt_resume` → `PM: sleep out`. So Bluetooth is torn down *completely* first, and rebuilt after. `bt_suspend` is a shell script: `hciconfig hci0 down`, kill `bluetoothd` and `brcm_patchram_plus`, remove `messagebus.pid`, `echo 0 > /sys/class/rfkill/rfkill0/state`, then `sleep 1` — carrying the emphatic stock comment *"if invoke this script in c with system(), must sleep for a while!!!!!"*, i.e. they hit a race here and the delay is load-bearing — then kill `bluealsa` and `dbus-daemon`. **That almost certainly explains our hang**: when we wrote `mem` we had `bluealsa`, `bluetoothd` and `bt-agent` all live with a connected WH-1000XM4, *and* the playback PCM still open. **The catch before anyone implements this**: `bt_resume` is not cheap — it re-runs `brcm_patchram_plus` with `sleep 5`, then `hciconfig up`, `bluetoothd`, `bt-agent`, `bluealsa`, with further sleeps, so a wake costs on the order of ten seconds of Bluetooth rebuild. Suspend is therefore only worth entering after a genuinely long idle, and the wake will not feel instant. Worth measuring what the cheap wins (PCM release, panel powerdown, slower poll loops) already recover before paying that price. Unresolved: what `PM: sd0_msg` does with the SD card. **Two further findings worth acting on before writing any code.** (1) Stock 1.6's `usr/resource/set_functions.json` ships `"standby":0` — HiBy *disabled* the standby feature in the shipping firmware, while leaving `"powersave_switch":1` and `"sleep_switch":1` on. A vendor shipping the suspend path switched off is weak but real evidence that it is not dependable on this hardware, which fits our hang. The audiobook mod we run flips `standby` 0→1 (one of its five `set_functions.json` changes), so the entry should be reachable in the stock Settings menu on this device. (2) That gives a safer test than ours: enable Standby in the stock Settings and let *HiBy's own code* drive the suspend rather than writing `mem` by hand. **But the question is already settled — suspend demonstrably works on this device.** `~/Downloads/hiby_player` (md5 `a401b0812e6a67df570d148131044dda`, 6.2 MB, statically linked, distinct from stock's 4.9 MB) is **`open_hiby_player`**, a third-party replacement player, and it contains a working implementation: `power_suspend: entering mem sleep at t=%u` … `/sys/power/state` … **`power_suspend: returned from mem sleep at t=%u`**. It returns, so it resumes. Its sequence, in binary order, is exactly three steps: **(a)** `/usr/bin/bt_suspend`, **(b)** write `/sys/class/graphics/fb0/blank` — the *sysfs* node, not the `FBIOBLANK` ioctl — **(c)** write `mem` to `/sys/power/state`; and `/usr/bin/bt_resume` on the way back. **Both of the first two steps were missing from our failed attempt**: we wrote `mem` with the screen lit and `bluealsa`/`bluetoothd`/`bt-agent` live with a connected headset, which is almost certainly the hang. Its settings model is also worth copying: one idle timer (`idle_shutdown_enabled` + `idle_shutdown_minutes`) plus a boolean `idle_suspend_enabled` that selects what happens when the timer fires — suspend, or `/sbin/poweroff` (its UI string is literally "Suspend instead of power off"), which is why `/sbin/poweroff` sits next to the suspend code. Settings persist to `/usr/data/open_hiby_player_settings.txt`. Also of note: it carries `bt_control: wedged daemon detected, attempting recovery`, so the Bluetooth stack wedging around this is a real hazard it had to engineer around. Next step is to replicate (a)–(c) behind our existing `Idle sleep` timeout and verify against `/sys/class/graphics/fb0/blank` existing on this firmware. |
| W5 | **Rockbox theme support on Now Playing** — **built, then reverted 2026-08-14** | Was implemented (`bmp.c`/`wps.c`, a Settings row, `theme_apply()`), shipped disabled pending a rendering bug ("don't load, or look awful"), then removed entirely rather than fixed, ahead of the 0.1 combined release. Undiagnosed at removal time. If revisited, start from this description rather than the old source, which is gone. Feature request 2026-08-09. Investigated with `~/Downloads/.rockbox` (already known to be a real Rockbox port for this exact hardware -- `rockbox-info.txt` says `Target: hibyr1`, `-DHIBY_R1`, binary `rockbox.r1`, so the theme dimensions and touch semantics are already proven to fit this screen, not a porting guess). A theme is a small bundle: a `.cfg` pointing at a `.wps` text file plus its images and a font. The `.wps` is Rockbox's "While Playing Screen" language -- `cabbiev2.wps` (a full-featured default theme) is 115 lines: viewports (`%V`/`%Vl`), conditionals (`%?tag<a|b|c>`), scrolling text (`%s`), touch regions (`%T`, each with an x/y/w/h and an action name like `play`/`next`/`shuffle`/`playlist`), progress/volume bars (`%pb`/`%pv`), and single/multi-letter metadata tags (`%it` title, `%ia` artist, `%pc`/`%pp`/`%pe` position/total, and so on) that read like the exact fields our own Now Playing screen already tracks. Images are plain `.bmp`, and cabbiev2's ship at `480x800x16` -- our screen's own resolution, and `x16` almost certainly means the same RGB565 our framebuffer and cover-art pipeline already use, which would make blitting them close to free. Fonts are Rockbox's own compact bitmap `.fnt` format (`RB12`-tagged header seen in `35-Adobe-Helvetica.fnt`), not TrueType -- a second, smaller parser next to the one `text.c` already has for the embedded TTF. So the shape of the work is a `.wps` tag parser, a `.bmp` decoder (materially simpler than the JPEG decoder already in `cover.c`), a `.fnt` decoder, and a renderer that walks the parsed tag list each frame evaluating conditionals against our own live playback state instead of Rockbox's, plus wiring `%T` regions into the existing touch dispatch so tapping the theme's own play/skip/shuffle graphics drives our actual playback functions. Scope should be the tags that actually appear in the shipped themes (title/artist/album, position/duration, play/pause/shuffle/repeat state, album art, progress and volume bars) with unrecognized tags skipped rather than failing the whole theme, matching how `art_candidate()` and `lib_format_name()` already degrade gracefully rather than erroring out. Not started. A genuinely buildable feature -- the format is small, documented, and every asset in it is simpler than something this codebase already parses -- but a real one, not an afternoon: expect a dedicated `wps.c`/`wps.h`, a Settings row to pick a theme (matching the request's "drop onto the SD card, apply in Settings" framing) that scans a themes folder and lists `.cfg` files found there, and enough of the tag language to make `cabbiev2` itself render as the first real test rather than a synthetic one. |
| W6 | **Newer Linux kernel (5.10 / 6.6) on the R1** — **evaluated 2026-08-15: feasible, but the payoff is empty. Not recommended.** | Full write-up: [NOTES-newer-kernel-feasibility.md](NOTES-newer-kernel-feasibility.md). Evaluated against the 18 GB Ingenic SDK bundle in `~/Downloads/ingenic-hiby` (official X1600 SDKs for kernel 4.4.94, 5.10 **and 6.6**) plus the stock `r1.upt`. **Feasibility is good.** The R1 reports itself as `ingenic,x1600_halley6_module_base` — it is Ingenic's own Halley6 reference design, so SoC bring-up is vendor-supported and the 6.6 SDK ships `dts/x1600/halley6_v20.dts`. Of the 28 out-of-tree modules in `/module_driver`, **19 have source**: 11 are Ingenic SoC drivers present in the SDK under upstream-style source names (`ingenicfb.c`, `ingenic_mmc.c`, `asoc-aic.c` — *not* their built names `soc_fb`/`soc_msc`/`soc_aic`, which is what made a first pass wrongly conclude they were missing), and 8 are mainline drivers rebuilt out-of-tree, identifiable by retained upstream authorship (Carlo Caione's `axp20x` regulator, Phil Blundell's `gpio-keys`, Luotao Fu's `leds-pwm`, Simon Vogl's i2c bit-banging). Only **9 modules (~112 KB) are genuinely bespoke**, and of those: `codec_cs43131` **should not be rewritten** — mainline's `cs43130.c` lists `cirrus,cs43131` explicitly and is a functional superset (HiBy's exposes four combined filter names; mainline exposes the same space as two orthogonal controls plus hardware volume, DSD volume, nonoversample, high-pass, de-emphasis and a full DSD DAPM graph, 2708 lines against HiBy's 20 KB). Six more are hours of work each because **the board is fully documented in its own firmware**: HiBy configures everything through insmod parameters rather than a device tree, so `/module_driver/*.sh` is effectively the DTS in plain text (every I²C bus, GPIO, regulator rail, the CW2015 battery model curve, and the inline-remote ADC thresholds), and `x1600_hiby_r1_sound_card.ko` names every DAI link (`x1600-i2s` / `x1600-i2s-pcm` / `cs43131.3-0030` / `cs43131-hifi`) with the SDK's `boards/halley6.c` as a template. Panel timings were recovered from `lcd_lg35583.ko` as an `fb_videomode` struct (480×800, margins 24/24/8/14, hsync 24, vsync 5, ~62 Hz ≈ 28.3 MHz pixel clock); only its short SPI init sequence still needs disassembling. The two awkward ones (`sa_hgl_dma`, a 2D DMA blitter `hiby_player` depends on, and `sa_config_module`) **disappear entirely if the firmware runs our Library app instead of `hiby_player`** — we mmap `/dev/fb0` and page-flip directly. **But the payoff is empty, and that is why this sits in Wishlist rather than Roadmap.** Both benefits originally claimed were checked and neither survived: (1) **exFAT is already present** — the kernel `uImage` header says "Not compressed" but the payload is self-decompressing (gzip at +16992); decompressed it carries a full backported exFAT driver with inode cache, sysfs version attributes and read-only error fallback, alongside ntfs/vfat/msdos/fuse; (2) **WiFi is capped by silicon** — the firmware blobs name a **BCM43438** (single-band 2.4 GHz b/g/n 1×1, the Raspberry Pi Zero W part), so no kernel can add 5 GHz or 802.11ac, and WPA3 — the one plausible win — is already there and lives in userspace regardless (`wpa_supplicant` v2.9 with SAE and OWE built in). What remains is driver security fixes on a device whose only untrusted input is podcast RSS. Against that sit two measured costs: a stock 6.6 kernel is **5,533,357 bytes against a 5,242,880-byte kernel partition (5.5% over)**, and RAM is already ~1.5 MB free of 56.9 MB on a device with OOM history — mainline has not shrunk since 4.4, so a newer kernel plausibly makes this device *worse*. **5.10 was evaluated separately as the easier target and rejected**: its kernel is 5,290,726 bytes, only **0.9% over** the partition (versus 6.6's 5.5%) so it essentially fits, and it keeps full x1600/halley6 board support plus an identical `cs43130.c` (2706 lines vs 2708) with the same `cirrus,cs43131` entry — but mainline's `hynitron_cstxxx.c` only landed in ~6.3, so on 5.10 the touch controller regresses from "have source" to bespoke (ten modules, not nine), and decisively **5.10 reaches end of upstream support in December 2026, roughly four months after this evaluation** (6.6 runs to December 2029). Since the only surviving rationale for upgrading at all is ongoing security fixes, porting onto a branch that stops receiving them defeats the purpose — so if this is ever done, 6.6 is the correct target despite needing ~240 KB more trimming. **The real long pole is not the drivers but bring-up debugging**: the cmdline says `console=ttyS2` and nobody has confirmed that UART reaches accessible pads; the A/B flash layout (`kernel`+`kernel2`, `rootfs`+`rootfs2`) and Ingenic's USB `cloner` recovery at least make mistakes non-fatal. **If picking this up:** the cheap first move is a GPL source request for the nine bespoke modules — they all declare `license=GPL`, and they have two addressees, since four are authored by `leeo@smartaction.com` ("SmartAction", apparently the ODM behind the board) and others by `ringsd@hiby.com`. Tools written during the evaluation and kept: `tools/upt_extract.py` (ISO9660 reader + rootfs reassembly, md5-verified, works without the device attached) and `tools/ko_modinfo.py` (`.modinfo` reader, no cross-binutils needed). |
| W7 | **Shrink the `sa_hgl_dma` graphics pool from 6 MB — potential ~6 MB RAM win, parked** | Full write-up: [NOTES-kernel-optimisation.md](NOTES-kernel-optimisation.md). `sa_hgl_dma.ko` (HiBy's DMA blitter, used by stock `hiby_player`, not by our app's own draw path) reserves **6 MB** via the module parameter `sahd_hgl_mem_size` for a screen whose entire framebuffer is 768 KB — eight screens' worth of scratch, on a device with only ~1.5 MB free at rest. Measured live 2026-08-15: `MemAvailable` is **19,840 kB with the pool open vs 26,336 kB closed** — a real, reproducible **~6.2 MB delta**. The pool is opened once by `hiby_player` at boot and held for its whole lifetime, **including while our Library app is running** (it's a hook inside `hiby_player`, so it inherits the same fd — verified `count=1` with our Main Menu on screen). Recovering even half of this would roughly double the device's current free-memory headroom. **Parked, not abandoned, because testing it needs a flash**: the parameter is runtime-writable (`perm=0644` in `.modinfo`) but the allocation happens lazily in `sahd_open()`, and `killall hiby_player` cannot be used to test a new value — after a restart the player never reopens `/dev/sa_hgl_dma` at all (almost certainly the known graphics-DMA leak leaving `dma_request_channel` failing on reopen), so the sysfs value has to be set *before* the player starts at boot to actually be exercised. That means either editing `sahd_hgl_mem_size=6291456` directly in `sa_hgl_dma.sh` inside the firmware image, or adding a small init script between `S11module_driver_default` and `S92_03_start_music_player` that writes the sysfs value — the same injection mechanism `patch_firmware.py` already uses for `S90adb`. Next step when picked back up: make that firmware change, flash, boot straight into a value below 6 MB, and watch `/usr/data/music.log` for `(hgl_fb: E) Map hgl dma memory failed` while exercising album art / list scroll / the EQ response curve — the things that actually drive the blitter. If `hiby_player` is ever dropped in favour of our own app (see W6's note on the same point), this 6 MB is recoverable outright with no tuning needed at all. |
| W3 | Rebase on 1.8b2 firmware | Update the app and firmware patches to work with 1.8b2. Track what changed in the stock player, bootloader, and system binaries. May uncover new optimization opportunities or require migration of existing workarounds. |
| W4 | ~~Add adb to 1.8b2~~ **narrower than assumed — adb is already there** | Checked directly against a real 1.8b2-based `.upt` (a "game boy" themed build downloaded 2026-08-14, not built by us): its rootfs already ships `/usr/bin/adbd`, `adbon`, `adboff`, `/sbin/adbserver.sh`, `usb_adb_enable.sh`, and `/etc/init.d/T90adb` (dispatching to `S310adb` or `S440adb` depending on which USB gadget interface the kernel exposes). Nothing needs bundling or adding to the ramdisk. **Not auto-started at boot**: `rcS` only runs `/etc/init.d/S??*`, and `T90adb` doesn't match that glob (wrong letter), so it never fires on its own. It *is* reachable at runtime, though — `hiby_player`'s own binary calls `/sbin/adbserver.sh` directly, gated by the same `USB working mode` setting (`VG_LISTVIEW_USB_WORKING_MODE` in the binary's strings) already reverse-engineered for the audiobook-mod firmware, where `System → USB working mode → Auto or Device` is what makes adb come up live. Same mechanism here, unverified only in the sense that it hasn't been watched happen on this specific build yet — the code path is there, the toggle hasn't been flipped and confirmed by eye. What's actually still open, now that "does adb exist" is answered: (1) confirm the toggle really brings `adbd` up on this specific 1.8b2 build, not just that the code path exists: `adb devices` after setting the mode; (2) decide whether that's good enough as-is or whether a boot-time wrapper is still wanted for reliability, the same reasoning that produced `hiby-podcast-app/tools/S90adb` for a different firmware (auto-retry, gadget-not-enumerated recovery) rather than depending on a menu toggle surviving every boot. **Unpacking note for whoever picks this back up**: this firmware's `.upt` uses a third chunk-layout variant neither `mod` nor `stock` (`OTA_V0`) in `hiby-podcast-app/tools/patch_firmware.py` recognises — lowercase `/ota_v0/...` paths, a differently-named digest file (`ota_md5_rootfs..<14-hex-chars>`), rootfs chunks named `rootfs.squashfs.<md5-prefix>` rather than `ROOTFS_S.<md5-prefix>`. Verified readable (reassembled and unsquashed the real rootfs, md5 matched the manifest exactly) with a one-off script, not via the existing tool — `patch_firmware.py` would need a third `detect_format()`/`read_images_*()` branch to patch this firmware line for real, not just inspect it. |

## Done

Playback of FLAC, MP3, WAV and M4A — the whole library. Gapless. Bit-perfect
output on the internal DAC. Album art, including art embedded in FLAC, MP3 and
MP4. Browsing by album artist, album, artist and genre, with an A–Z index that
can be tapped or dragged. Track numbers read from the files' own tags. Two-line
titles. Mini player, queue, hold-to-queue, playlists. Radio over both direct MP3
and HLS. Output routed across 3.5 mm, USB and Bluetooth, with the Bluetooth
codec and the headset's battery on Now Playing, pausing when the output goes
away and resuming by itself when it returns. Screen lock, edge-swipe back
with an indicator, tap-to-seek and scrubbing, swipe-down quick settings for
brightness and the two radios, hardware keys including the headset's own AVRCP
buttons, and a volume overlay that can be dragged.

**Audiobooks**, as a section of the main menu, scoped strictly to `/Audiobooks`
by a folder walk that never touches the stock index. Books split into their
real chapters — read from the text track an `.m4b` carries beside its audio, so
the titles are the book's own ("Part 2: Areophany") — reachable from the same
bottom-right control the queue uses. Two progress bars, the book's above a
scrubbable chapter; −10s/+10s in place of prev/next; pitch-preserving speed
from 1.0× to 2.0×, which also rescales both countdowns. Chapters of one file
move by seeking rather than reopening, so a jump into hour nine is instant.
